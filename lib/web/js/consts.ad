# lib/web/js/consts.ad — part of the modular web engine (extracted from jsengine.ad).
# constant tables: value tags, token kinds, AST node kinds, regex opcodes.
#
# Behavior-preserving split: Adder is a single-compilation-unit language
# (merge_programs merges all imported files into one Program), so every
# public top-level name here resolves globally across the merged engine.

# ====================================================================
# Value tags.
# ====================================================================
TAG_UNDEF: int32 = 0
TAG_NULL:  int32 = 1
TAG_BOOL:  int32 = 2
TAG_NUM:   int32 = 3
TAG_STR:   int32 = 4
TAG_OBJ:   int32 = 5
TAG_BIGINT: int32 = 6      # arbitrary-precision integer (v_ref = bigint record id)
TAG_FREE:  int32 = 7       # a value cell on the GC free list (v_ref = next free handle)

# Object kinds.
OK_PLAIN: int32 = 0
OK_ARRAY: int32 = 1
OK_FUNC:  int32 = 2
OK_SYMBOL: int32 = 3       # a Symbol primitive (typeof -> "symbol"; unique identity)

# ====================================================================
# Token kinds.
# ====================================================================
TK_EOF:    int32 = 0
TK_NUM:    int32 = 1
TK_STR:    int32 = 2
TK_IDENT:  int32 = 3
# keywords
TK_VAR:    int32 = 4
TK_LET:    int32 = 5
TK_CONST:  int32 = 6
TK_FUNCTION: int32 = 7
TK_RETURN: int32 = 8
TK_IF:     int32 = 9
TK_ELSE:   int32 = 10
TK_WHILE:  int32 = 11
TK_FOR:    int32 = 12
TK_DO:     int32 = 13
TK_BREAK:  int32 = 14
TK_CONTINUE: int32 = 15
TK_TRUE:   int32 = 16
TK_FALSE:  int32 = 17
TK_NULL:   int32 = 18
TK_UNDEFINED: int32 = 19
TK_TYPEOF: int32 = 20
TK_INSTANCEOF: int32 = 21
TK_IN:     int32 = 22
TK_NEW:    int32 = 23
TK_THIS:   int32 = 24
# punctuation / operators
TK_LPAREN: int32 = 30
TK_RPAREN: int32 = 31
TK_LBRACE: int32 = 32
TK_RBRACE: int32 = 33
TK_LBRACK: int32 = 34
TK_RBRACK: int32 = 35
TK_SEMI:   int32 = 36
TK_COMMA:  int32 = 37
TK_DOT:    int32 = 38
TK_COLON:  int32 = 39
TK_QUESTION: int32 = 40
TK_ASSIGN: int32 = 41
TK_PLUS:   int32 = 42
TK_MINUS:  int32 = 43
TK_STAR:   int32 = 44
TK_SLASH:  int32 = 45
TK_PERCENT: int32 = 46
TK_LT:     int32 = 47
TK_GT:     int32 = 48
TK_LE:     int32 = 49
TK_GE:     int32 = 50
TK_EQ:     int32 = 51
TK_NE:     int32 = 52
TK_SEQ:    int32 = 53
TK_SNE:    int32 = 54
TK_ANDAND: int32 = 55
TK_OROR:   int32 = 56
TK_NOT:    int32 = 57
TK_BAND:   int32 = 58
TK_BOR:    int32 = 59
TK_BXOR:   int32 = 60
TK_BNOT:   int32 = 61
TK_SHL:    int32 = 62
TK_SHR:    int32 = 63
TK_USHR:   int32 = 64
TK_INC:    int32 = 65
TK_DEC:    int32 = 66
TK_PLUSEQ: int32 = 67
TK_MINUSEQ: int32 = 68
TK_STAREQ: int32 = 69
TK_SLASHEQ: int32 = 70
TK_PERCENTEQ: int32 = 71
TK_ARROW:  int32 = 72
TK_CLASS:  int32 = 73
TK_EXTENDS: int32 = 74
TK_SUPER:  int32 = 75
TK_TRY:    int32 = 76
TK_CATCH:  int32 = 77
TK_FINALLY: int32 = 78
TK_THROW:  int32 = 79
TK_STATIC: int32 = 80
TK_ELLIPSIS: int32 = 81
TK_REGEX:  int32 = 82          # regex literal; tk_str = source sid, tk_str2 = flags sid
TK_VOID:   int32 = 83          # `void` unary operator
TK_DELETE: int32 = 84          # `delete` unary operator
TK_SWITCH: int32 = 85          # `switch`
TK_CASE:   int32 = 86          # `case`
TK_DEFAULT: int32 = 87         # `default` (switch)
TK_OREQ:   int32 = 88          # |=
TK_ANDEQ:  int32 = 89          # &=
TK_XOREQ:  int32 = 90          # ^=
TK_SHLEQ:  int32 = 91          # <<=
TK_SHREQ:  int32 = 92          # >>=
TK_USHREQ: int32 = 93          # >>>=
TK_QDOT:   int32 = 94          # ?.  optional chaining
TK_QQ:     int32 = 95          # ??  nullish coalescing
TK_QQEQ:   int32 = 96          # ??= nullish assignment
TK_OROREQ: int32 = 97          # ||= logical-or assignment
TK_ANDANDEQ: int32 = 98        # &&= logical-and assignment
TK_STARSTAR: int32 = 99        # **  exponentiation
TK_STARSTAREQ: int32 = 100     # **= exponentiation assignment
# Template-literal tokens. Each carries tk_str = COOKED chunk sid and
# tk_str2 = RAW chunk sid (verbatim source, for String.raw / tag.raw).
TK_TMPL_FULL: int32 = 101      # `no substitutions`  -> whole template, one chunk
TK_TMPL_HEAD: int32 = 102      # `head${            -> chunk before the 1st subst
TK_TMPL_MID:  int32 = 103      # }mid${             -> chunk between substitutions
TK_TMPL_TAIL: int32 = 104      # }tail`             -> chunk after the last subst
TK_PRIVATE:   int32 = 105      # #name  -> class private identifier (tk_str = "#name")
TK_BIGINT:    int32 = 106      # 123n  -> BigInt literal (tk_str = source digits sid, sans the `n`)

# ====================================================================
# AST node kinds.
# ====================================================================
ND_NUM:    int32 = 0
ND_STR:    int32 = 1
ND_TRUE:   int32 = 2
ND_FALSE:  int32 = 3
ND_NULL:   int32 = 4
ND_UNDEF:  int32 = 5
ND_IDENT:  int32 = 6
ND_THIS:   int32 = 7
ND_ARRAY:  int32 = 8
ND_OBJECT: int32 = 9
ND_PROP:   int32 = 10
ND_FUNC:   int32 = 11
ND_CALL:   int32 = 12
ND_NEW:    int32 = 13
ND_MEMBER: int32 = 14
ND_INDEX:  int32 = 15
ND_ASSIGN: int32 = 16
ND_BINOP:  int32 = 17
ND_LOGICAL: int32 = 18
ND_UNARY:  int32 = 19
ND_UPDATE: int32 = 20
ND_TERNARY: int32 = 21
ND_VAR:    int32 = 22
ND_DECLTOR: int32 = 23
ND_EXPRSTMT: int32 = 24
ND_BLOCK:  int32 = 25
ND_IF:     int32 = 26
ND_WHILE:  int32 = 27
ND_DOWHILE: int32 = 28
ND_FOR:    int32 = 29
ND_FORIN:  int32 = 30
ND_RETURN: int32 = 31
ND_BREAK:  int32 = 32
ND_CONTINUE: int32 = 33
ND_FUNCDECL: int32 = 34
ND_EMPTY:  int32 = 35
ND_PARAM:  int32 = 36
ND_ARROW:  int32 = 37
ND_TRY:    int32 = 38
ND_THROW:  int32 = 39
ND_CLASS:  int32 = 40
ND_METHOD: int32 = 41
ND_SUPER:  int32 = 42
ND_SPREAD: int32 = 43
ND_CLASSDECL: int32 = 44
ND_ARRPAT: int32 = 45          # array destructuring pattern
ND_OBJPAT: int32 = 46          # object destructuring pattern
ND_PATELEM: int32 = 47         # one element of a pattern (n_str=name, n_a=default, n_b=nested pattern)
ND_REGEX:  int32 = 48          # regex literal (n_str = source sid, n_a = flags sid)
ND_AWAIT:  int32 = 49          # `await expr` (n_a = operand); valid inside async fns
ND_LABELED: int32 = 50         # `label: stmt` (n_str = label sid, n_a = body)
ND_SWITCH: int32 = 51          # `switch(disc){cases}` (n_a = disc, n_b = first ND_CASE)
ND_CASE:   int32 = 52          # one case (n_a = test expr or -1 for default, n_b = first stmt, n_next = next case)
ND_OPTCHAIN: int32 = 53        # optional-chain boundary wrapper (n_a = inner chain expr)
ND_FOROF:  int32 = 54          # `for(x of iter) body` (n_a = target decl/expr, n_b = iterable, n_c = body, n_op = is_decl)
ND_TMPL:   int32 = 55          # template literal / tagged template. n_a = parts chain (ND_TMPLPART), n_b = tag expr or -1, n_c = substitution count
ND_TMPLPART: int32 = 56        # one template part. n_op=0 quasi (n_str=cooked sid, n_d=raw sid); n_op=1 subst (n_a=expr); n_next=next part
ND_YIELD:  int32 = 57          # `yield expr` / `yield* expr` (n_a = operand or -1, n_op = 1 => delegate yield*)
# ---- ES modules (import / export) ----
ND_IMPORT: int32 = 58          # `import ... from "spec"` (n_str = specifier sid, n_a = ND_IMPSPEC chain or -1 for side-effect import)
ND_IMPSPEC: int32 = 59         # one imported binding. n_op: 0=named, 1=default, 2=namespace. n_str = imported name sid (named); n_d = local name sid; n_next = next.
ND_EXPORT: int32 = 60          # `export ...`. n_op: 0=decl (n_a=inner decl stmt); 1=named list (n_a=ND_EXPSPEC chain); 2=default (n_a=expr-or-decl); 3=re-export from (n_str=specifier, n_a=ND_EXPSPEC chain or -1 for `export *`).
ND_EXPSPEC: int32 = 61         # one exported binding. n_str = local name sid; n_d = exported name sid; n_next = next.
ND_PRIVNAME: int32 = 62        # bare class private name `#x` (only valid as LHS of `in`; n_str = "#x")
ND_BIGINT: int32 = 63          # BigInt literal `123n` (n_str = source digits sid, sans the `n`)
ND_HOLE:   int32 = 64          # array-literal elision (a `,,` hole); reads as undefined, skipped as an assignment target
ND_SEQ:    int32 = 65          # comma sequence expression `a, b, c`: eval n_a (for side effects), then n_b; value is n_b. Left-associative.
ND_NEWTARGET: int32 = 66       # `new.target` meta-property — the constructor of the active construct call, else undefined.

# Binary operator codes (also used for compound-assign).
OP_ADD: int32 = 0
OP_SUB: int32 = 1
OP_MUL: int32 = 2
OP_DIV: int32 = 3
OP_MOD: int32 = 4
OP_LT:  int32 = 5
OP_GT:  int32 = 6
OP_LE:  int32 = 7
OP_GE:  int32 = 8
OP_EQ:  int32 = 9
OP_NE:  int32 = 10
OP_SEQ: int32 = 11
OP_SNE: int32 = 12
OP_BAND: int32 = 13
OP_BOR:  int32 = 14
OP_BXOR: int32 = 15
OP_SHL:  int32 = 16
OP_SHR:  int32 = 17
OP_USHR: int32 = 18
OP_INSTANCEOF: int32 = 19
OP_IN:   int32 = 20
OP_POW:  int32 = 21          # ** exponentiation

# Unary operator codes.
U_NEG:    int32 = 0
U_POS:    int32 = 1
U_NOT:    int32 = 2
U_BNOT:   int32 = 3
U_TYPEOF: int32 = 4
U_VOID:   int32 = 5
U_DELETE: int32 = 6

# Control-flow completion (evaluator unwinding).
CTL_NONE:   int32 = 0
CTL_RETURN: int32 = 1
CTL_BREAK:  int32 = 2
CTL_CONTINUE: int32 = 3
CTL_THROW:  int32 = 4

# Native builtin ids. Host-object natives use ids >= HOST_NATIVE_BASE and are
# dispatched through the embedder-registered hook (js_set_host_dispatch).
NAT_CONSOLE_LOG: int32 = 1
NAT_PARSEINT:    int32 = 2
NAT_PARSEFLOAT:  int32 = 3
NAT_ISNAN:       int32 = 4
NAT_STRING:      int32 = 5
NAT_NUMBER:      int32 = 6
NAT_BOOLEAN:     int32 = 7
NAT_EVAL:        int32 = 8    # eval(src) — nested program evaluation (indirect)
NAT_MATH_FLOOR:  int32 = 10
NAT_MATH_CEIL:   int32 = 11
NAT_MATH_ROUND:  int32 = 12
NAT_MATH_ABS:    int32 = 13
NAT_MATH_SQRT:   int32 = 14
NAT_MATH_POW:    int32 = 15
NAT_MATH_MIN:    int32 = 16
NAT_MATH_MAX:    int32 = 17
NAT_MATH_TRUNC:  int32 = 18
NAT_JSON_STRINGIFY: int32 = 20
NAT_JSON_PARSE:  int32 = 21
NAT_OBJECT_KEYS: int32 = 22
NAT_ARRAY_ISARRAY: int32 = 23
NAT_ERROR:       int32 = 24
NAT_TYPEERROR:   int32 = 25
NAT_RANGEERROR:  int32 = 26
NAT_SYNTAXERROR: int32 = 27
NAT_REFERENCEERROR: int32 = 28
NAT_OBJECT_ASSIGN: int32 = 29
NAT_OBJECT_VALUES: int32 = 30
NAT_ARRAY_CTOR:  int32 = 31
NAT_ARRAY_OF:    int32 = 32
NAT_REGEXP:      int32 = 33
NAT_MATH_SIGN:   int32 = 34
NAT_MATH_CBRT:   int32 = 35
NAT_MATH_HYPOT:  int32 = 36
NAT_MATH_LOG:    int32 = 37
NAT_MATH_LOG2:   int32 = 38
NAT_MATH_LOG10:  int32 = 39
NAT_MATH_EXP:    int32 = 40
NAT_MATH_SIN:    int32 = 41
NAT_MATH_COS:    int32 = 42
NAT_MATH_TAN:    int32 = 43
NAT_MATH_RANDOM: int32 = 44
NAT_MATH_ASIN:   int32 = 306
NAT_MATH_ACOS:   int32 = 307
NAT_MATH_ATAN:   int32 = 308
NAT_MATH_ATAN2:  int32 = 309
NAT_OBJECT_ENTRIES:     int32 = 45
NAT_OBJECT_FROMENTRIES: int32 = 46
NAT_OBJECT_FREEZE:      int32 = 47
NAT_ISFINITE:    int32 = 48
NAT_NUMBER_ISINTEGER: int32 = 49
NAT_NUMBER_ISNAN:     int32 = 50
NAT_NUMBER_ISFINITE:  int32 = 51
NAT_SET_TIMEOUT:      int32 = 60
NAT_SET_INTERVAL:     int32 = 61
NAT_CLEAR_TIMEOUT:    int32 = 62
NAT_CLEAR_INTERVAL:   int32 = 63
NAT_QUEUE_MICROTASK:  int32 = 64
NAT_REQUEST_ANIM_FRAME: int32 = 65
NAT_CANCEL_ANIM_FRAME:  int32 = 66
# Date: constructor + static Date.now() + instance getters (all UTC-based).
NAT_DATE:             int32 = 70
NAT_DATE_NOW:         int32 = 71
NAT_DATE_GETTIME:     int32 = 72
NAT_DATE_GETFULLYEAR: int32 = 73
NAT_DATE_GETMONTH:    int32 = 74
NAT_DATE_GETDATE:     int32 = 75
NAT_DATE_GETDAY:      int32 = 76
NAT_DATE_GETHOURS:    int32 = 77
NAT_DATE_GETMINUTES:  int32 = 78
NAT_DATE_GETSECONDS:  int32 = 79
NAT_DATE_GETMS:       int32 = 80
NAT_DATE_VALUEOF:     int32 = 81
NAT_DATE_TOISO:       int32 = 82
NAT_DATE_TOSTRING:    int32 = 83
# Promise (Promise/A+ shaped; reactions scheduled as MICROTASKS on the #178
# deferred-callback queue). Per-instance state (which promise a resolver is
# bound to; a combinator's shared cell + element index) is carried on the
# native function OBJECT via obj_p_a/obj_p_b, recovered through native_self.
NAT_PROMISE:          int32 = 90    # the Promise constructor (executor)
NAT_PROMISE_RESOLVE_FN: int32 = 91  # resolve(value) passed to executor/thenable
NAT_PROMISE_REJECT_FN:  int32 = 92  # reject(reason) passed to executor/thenable
NAT_PROMISE_JOB:      int32 = 93    # microtask body: run one settled reaction
NAT_PROMISE_RESOLVE:  int32 = 94    # Promise.resolve(v)
NAT_PROMISE_REJECT:   int32 = 95    # Promise.reject(r)
NAT_PROMISE_ALL:      int32 = 96    # Promise.all([...])
NAT_PROMISE_RACE:     int32 = 97    # Promise.race([...])
NAT_PROMISE_ALLSETTLED: int32 = 98  # Promise.allSettled([...])
NAT_PROMISE_ANY:      int32 = 324   # Promise.any([...])
# ---- Round-8 TypedArray/DataView/Intl completion (see r7.ad, r8_ta.ad) -------
# The TypedArray prototypes carried only set/subarray/slice/fill/values, so the
# ITERATION half of the Array surface (join/map/forEach/reduce/indexOf/…) threw
# "is not a function" — the failure mode probe_js_hard.sh saw as `typedarray_map`
# / `typedarray_set` / `uint8_subarray` (all three actually died on `.join()`).
# One native serves them all; the method is selected by a TAM_* code stashed in
# obj_p_a of the bound function object.
NAT_TA_ARRMETH:       int32 = 325   # TypedArray.prototype.<array method> (TAM_* in obj_p_a)
NAT_DATAVIEW:         int32 = 326   # new DataView(buffer[, byteOffset[, byteLength]])
NAT_DV_GET:           int32 = 327   # DataView.prototype.get<Type>(byteOffset[, littleEndian])
NAT_DV_SET:           int32 = 328   # DataView.prototype.set<Type>(byteOffset, value[, littleEndian])
NAT_INTL_COLLATOR:    int32 = 330   # new Intl.Collator([locales[, options]])
NAT_COLLATOR_COMPARE: int32 = 331   # Intl.Collator.prototype.compare(a, b)
# URI handling globals. Four distinct escape SETS share two implementations;
# the set is selected by the URI_SET_* code in obj_p_a of the function object.
NAT_URI_ENCODE:       int32 = 332   # encodeURI / encodeURIComponent
NAT_URI_DECODE:       int32 = 333   # decodeURI / decodeURIComponent
NAT_URI_ESCAPE:       int32 = 334   # escape()   — legacy, UTF-16 code units
NAT_URI_UNESCAPE:     int32 = 335   # unescape() — legacy
NAT_URIERROR:         int32 = 336   # the URIError constructor

# encode/decode SET selector (obj_p_a on the native function object).
URI_SET_FULL: int32 = 0   # encodeURI / decodeURI  — the reserved set survives
URI_SET_COMP: int32 = 1   # encodeURIComponent / decodeURIComponent

# TypedArray array-method selector codes (obj_p_a of the method function).
TAM_JOIN:      int32 = 1
TAM_MAP:       int32 = 2
TAM_FILTER:    int32 = 3
TAM_FOREACH:   int32 = 4
TAM_REDUCE:    int32 = 5
TAM_INDEXOF:   int32 = 6
TAM_INCLUDES:  int32 = 7
TAM_FIND:      int32 = 8
TAM_FINDINDEX: int32 = 9
TAM_SOME:      int32 = 10
TAM_EVERY:     int32 = 11
TAM_AT:        int32 = 12
TAM_REVERSE:   int32 = 13
TAM_SORT:      int32 = 14
TAM_LASTINDEX: int32 = 15
TAM_REDUCERIGHT: int32 = 16
TAM_TOSTRING:  int32 = 17
TAM_KEYS:      int32 = 18
TAM_ENTRIES:   int32 = 19
NAT_PROMISE_THEN:     int32 = 99    # Promise.prototype.then
NAT_PROMISE_CATCH:    int32 = 100   # Promise.prototype.catch
NAT_PROMISE_FINALLY:  int32 = 101   # Promise.prototype.finally
NAT_COMB_FUL:         int32 = 102   # combinator per-element fulfilled hook
NAT_COMB_REJ:         int32 = 103   # combinator per-element rejected hook
NAT_FINALLY_FUL:      int32 = 104   # finally() fulfilled wrapper (pass value through)
NAT_FINALLY_REJ:      int32 = 105   # finally() rejected wrapper (re-throw reason)
# fetch() + Response. With an embedder-registered TRANSPORT (js_set_fetch_transport
# — the browser's kernel /net http9 client), fetch(url) performs a REAL over-the-
# wire HTTP request; with NO transport (host / dual-target) it resolves from a
# JS-settable FIXTURE table. The engine stays extern-free either way (it only
# calls back into the embedder's transport; it opens no sockets). fetch(url)
# returns a Promise<Response>; the Response body methods each return a Promise
# that settles on the SAME #178 microtask queue, so await/.then compose.
NAT_FETCH:            int32 = 110   # fetch(url[, options]) -> Promise<Response>
NAT_SET_FETCH_FIXTURE: int32 = 111 # __setFetchFixture(url, {status,body,...}) test hook
NAT_RESPONSE_TEXT:    int32 = 112   # Response.prototype.text() -> Promise<string>
NAT_RESPONSE_JSON:    int32 = 113   # Response.prototype.json() -> Promise<parsed>
NAT_RESPONSE_ARRAYBUF: int32 = 114  # Response.prototype.arrayBuffer() -> Promise<array>
NAT_HEADERS_GET:      int32 = 115   # Headers.prototype.get(name) -> string | null
NAT_HEADERS_HAS:      int32 = 116   # Headers.prototype.has(name) -> bool
NAT_HEADERS_ENTRIES:  int32 = 117   # Headers.prototype.entries() -> [[name,value],...]
NAT_STORAGE_GET:      int32 = 120   # Storage.prototype.getItem(key) -> string | null
NAT_STORAGE_SET:      int32 = 121   # Storage.prototype.setItem(key, value)
NAT_STORAGE_REMOVE:   int32 = 122   # Storage.prototype.removeItem(key)
NAT_STORAGE_CLEAR:    int32 = 123   # Storage.prototype.clear()
NAT_STORAGE_KEY:      int32 = 124   # Storage.prototype.key(n) -> string | null
# Map: constructor + prototype methods (ES2015 collections).
NAT_MAP:          int32 = 130   # Map constructor (optional iterable of [k,v])
NAT_MAP_GET:      int32 = 131
NAT_MAP_SET:      int32 = 132
NAT_MAP_HAS:      int32 = 133
NAT_MAP_DELETE:   int32 = 134
NAT_MAP_CLEAR:    int32 = 135
NAT_MAP_FOREACH:  int32 = 136
NAT_MAP_KEYS:     int32 = 137
NAT_MAP_VALUES:   int32 = 138
NAT_MAP_ENTRIES:  int32 = 139
# Set: constructor + prototype methods.
NAT_SET:          int32 = 140   # Set constructor (optional iterable of values)
NAT_SET_ADD:      int32 = 141
NAT_SET_HAS:      int32 = 142
NAT_SET_DELETE:   int32 = 143
NAT_SET_CLEAR:    int32 = 144
NAT_SET_FOREACH:  int32 = 145
NAT_SET_VALUES:   int32 = 146
# Object statics (ES5/ES2015/ES2017).
NAT_OBJECT_CREATE:      int32 = 150
NAT_OBJECT_GETPROTO:    int32 = 151
NAT_OBJECT_SETPROTO:    int32 = 152
NAT_OBJECT_GETOWNNAMES: int32 = 153
NAT_OBJECT_DEFINEPROP:  int32 = 154
NAT_OBJECT_ISFROZEN:    int32 = 155
NAT_OBJECT_GETOWNDESC:  int32 = 156
NAT_OBJECT_GETOWNDESCS: int32 = 157
NAT_OBJECT_DEFINEPROPS: int32 = 158
NAT_OBJECT_HASOWN:      int32 = 216   # Object.hasOwn(obj, key) — ES2022
NAT_OBJECT_IS:          int32 = 217   # Object.is(a, b) — SameValue (NaN eq, +0/-0 distinct)
# Number static + Array.from.
NAT_NUMBER_ISSAFEINT: int32 = 160
NAT_ARRAY_FROM:       int32 = 161
# Round-2 conformance batch: String static helpers.
NAT_STRING_RAW:         int32 = 162   # String.raw(strings, ...subs) — cooked-template raw assembly
NAT_STRING_FROMCHARCODE: int32 = 163  # String.fromCharCode(...codes)
NAT_STRING_FROMCODEPOINT: int32 = 164 # String.fromCodePoint(...cps)
NAT_SYMBOL:             int32 = 168   # Symbol([description]) — unique primitive
# Generator object prototype methods (bounded/eager generator model — see interp.ad).
NAT_GEN_NEXT:           int32 = 169   # gen.next([v]) -> {value, done}
NAT_GEN_RETURN:         int32 = 170   # gen.return([v]) -> {value:v, done:true}
NAT_GEN_THROW:          int32 = 171   # gen.throw(e) -> mark done, throw e
NAT_GEN_ITER:           int32 = 172   # gen[Symbol.iterator]() -> the generator itself
# Reflect — thin wrappers over the engine's existing object operations (round 7).
NAT_REFLECT_GET:        int32 = 180   # Reflect.get(target, key[, receiver])
NAT_REFLECT_SET:        int32 = 181   # Reflect.set(target, key, value[, receiver]) -> bool
NAT_REFLECT_HAS:        int32 = 182   # Reflect.has(target, key) -> bool  (the `in` op)
NAT_REFLECT_OWNKEYS:    int32 = 183   # Reflect.ownKeys(target) -> array of own keys
NAT_REFLECT_GETPROTO:   int32 = 184   # Reflect.getPrototypeOf(target)
NAT_REFLECT_SETPROTO:   int32 = 185   # Reflect.setPrototypeOf(target, proto) -> bool
NAT_REFLECT_DEFINEPROP: int32 = 186   # Reflect.defineProperty(target, key, desc) -> bool
NAT_REFLECT_DELETEPROP: int32 = 187   # Reflect.deleteProperty(target, key) -> bool
NAT_REFLECT_APPLY:      int32 = 188   # Reflect.apply(target, thisArg, argsList)
NAT_REFLECT_CONSTRUCT:  int32 = 189   # Reflect.construct(target, argsList[, newTarget])
# Proxy — new Proxy(target, handler) with common traps (round 7).
NAT_PROXY:              int32 = 190   # the Proxy constructor
# Typed arrays + ArrayBuffer (byte-backed; little-endian; round 7).
NAT_ARRAYBUFFER:        int32 = 200   # new ArrayBuffer(byteLength)
NAT_TYPEDARRAY:         int32 = 201   # every TypedArray ctor (elem type via obj_p_a on the ctor fn)
NAT_TA_SET:             int32 = 202   # TypedArray.prototype.set(src[, offset])
NAT_TA_SUBARRAY:        int32 = 203   # TypedArray.prototype.subarray([begin[, end]])
NAT_TA_SLICE:           int32 = 204   # TypedArray.prototype.slice([begin[, end]])
NAT_TA_FILL:            int32 = 205   # TypedArray.prototype.fill(value[, start[, end]])
NAT_TA_ITER:            int32 = 206   # TypedArray.prototype[Symbol.iterator] / values
NAT_TA_OF:              int32 = 207   # TypedArray.of(...items)
NAT_TA_FROM:            int32 = 208   # TypedArray.from(arrayLike[, mapFn])
NAT_STRUCTURED_CLONE:   int32 = 210   # structuredClone(value) — deep clone (round 7)
NAT_SYMBOL_FOR:         int32 = 211   # Symbol.for(key) — global symbol registry
NAT_SYMBOL_KEYFOR:      int32 = 212   # Symbol.keyFor(sym) — reverse registry lookup
# BigInt (arbitrary precision) — round 8.
NAT_BIGINT:            int32 = 213    # the BigInt(value) conversion function
NAT_BIGINT_ASINTN:     int32 = 214   # BigInt.asIntN(bits, x)  — sign-extend into [-2^(n-1), 2^(n-1))
NAT_BIGINT_ASUINTN:    int32 = 215   # BigInt.asUintN(bits, x) — wrap into [0, 2^n)
# ---- URL / URLSearchParams / base64 (self-contained Web-platform APIs) -------
NAT_URL:               int32 = 220   # new URL(url[, base]) — WHATWG URL parser
NAT_URL_TOSTRING:      int32 = 221   # URL.prototype.toString() / .href getter mirror
NAT_URL_TOJSON:        int32 = 222   # URL.prototype.toJSON()
NAT_URLSP:             int32 = 223   # new URLSearchParams(init) — string | object
NAT_URLSP_GET:         int32 = 224   # get(name) -> first value | null
NAT_URLSP_GETALL:      int32 = 225   # getAll(name) -> array of values
NAT_URLSP_HAS:         int32 = 226   # has(name) -> bool
NAT_URLSP_SET:         int32 = 227   # set(name, value)
NAT_URLSP_APPEND:      int32 = 228   # append(name, value)
NAT_URLSP_DELETE:      int32 = 229   # delete(name)
NAT_URLSP_TOSTRING:    int32 = 230   # toString() -> form-urlencoded serialization
NAT_URLSP_FOREACH:     int32 = 231   # forEach(cb[, thisArg])
NAT_URLSP_ENTRIES:     int32 = 232   # entries() -> array of [k,v]
NAT_URLSP_KEYS:        int32 = 233   # keys() -> array of names
NAT_URLSP_VALUES:      int32 = 234   # values() -> array of values
NAT_URLSP_SORT:        int32 = 235   # sort() — stable sort by name
NAT_BTOA:              int32 = 236   # btoa(str) -> base64 (RFC 4648)
NAT_ATOB:              int32 = 237   # atob(b64) -> binary string
NAT_URLSP_ITER:        int32 = 238   # [Symbol.iterator]() -> entries iterator
NAT_URLSP_ITNEXT:      int32 = 239   # the iterator's next() -> {value,done}
# ---- Intl (formatting/i18n) + Date-locale/parse/UTC extensions (round 9) -----
# Minimal, en-US-oriented Intl. Locale is accepted but only en-US grouping/
# separators / month+weekday names are implemented (documented simplification).
NAT_INTL_NUMBERFORMAT:  int32 = 240   # new Intl.NumberFormat(locales, options)
NAT_INTL_NF_FORMAT:     int32 = 241   # NumberFormat.prototype.format(n)
NAT_INTL_DATETIMEFORMAT: int32 = 242  # new Intl.DateTimeFormat(locales, options)
NAT_INTL_DTF_FORMAT:    int32 = 243   # DateTimeFormat.prototype.format(date)
NAT_DATE_PARSE:         int32 = 244   # Date.parse(isoString) -> epoch-ms | NaN
NAT_DATE_UTC:           int32 = 245   # Date.UTC(y, mo, d, h, mi, s, ms) -> epoch-ms
NAT_DATE_TZOFFSET:      int32 = 246   # getTimezoneOffset() -> 0 (engine is UTC)
NAT_DATE_SETTIME:       int32 = 247   # setTime(ms)
NAT_DATE_SETFULLYEAR:   int32 = 248   # setUTCFullYear/setFullYear(y)
NAT_DATE_SETMONTH:      int32 = 249   # setUTCMonth/setMonth(m0)
NAT_DATE_SETDATE:       int32 = 250   # setUTCDate/setDate(d)
NAT_DATE_SETHOURS:      int32 = 251   # setUTCHours/setHours(h)
NAT_DATE_SETMINUTES:    int32 = 252   # setUTCMinutes/setMinutes(mi)
NAT_DATE_SETSECONDS:    int32 = 253   # setUTCSeconds/setSeconds(s)
NAT_DATE_SETMS:         int32 = 254   # setUTCMilliseconds/setMilliseconds(ms)
NAT_DATE_TOLOCALEDATESTRING: int32 = 255  # toLocaleDateString([locale[, opts]])
NAT_DATE_TOLOCALETIMESTRING: int32 = 256  # toLocaleTimeString([locale[, opts]])
NAT_DATE_TOLOCALESTRING: int32 = 257  # toLocaleString([locale[, opts]])
# ---- FormData (multipart/urlencoded form multimap) ---------------------------
# Storage + methods are shared with URLSearchParams (parallel @@k/@@v arrays),
# so only the constructor needs its own id; the prototype binds the NAT_URLSP_*
# method ids, which operate on the same @@k/@@v layout.
NAT_FORMDATA:          int32 = 258   # new FormData([obj]) — empty | object init
NAT_FUNCTION_CTOR:     int32 = 259   # the Function global (identity for `instanceof Function`)
NAT_GC:                int32 = 260   # gc() — test-only: force a value-arena collection
NAT_OBJECT_ISEXT:      int32 = 261   # Object.isExtensible(o)
NAT_OBJECT_PREVENTEXT: int32 = 262   # Object.preventExtensions(o) -> o
NAT_OBJECT_SEAL:       int32 = 263   # Object.seal(o) -> o
NAT_OBJECT_ISSEALED:   int32 = 264   # Object.isSealed(o)
NAT_NAV_SENDBEACON:    int32 = 265   # navigator.sendBeacon(url[, data]) -> bool (best-effort stub)
NAT_ARR_VALUES:        int32 = 266   # Array/String.prototype.values / [Symbol.iterator] -> iterator
NAT_ARR_KEYS:          int32 = 267   # Array.prototype.keys() -> index iterator
NAT_ARR_ENTRIES:       int32 = 268   # Array.prototype.entries() -> [i,v] iterator
NAT_ARRITER_NEXT:      int32 = 269   # the array/string iterator's next() -> {value,done}
# ---- performance (High Resolution Time / User Timing) ------------------------
# Real sites' framework bootstraps read `performance.now()` and call the User
# Timing no-ops (`performance.mark`/`measure`/`clearMarks`) unconditionally; a
# missing `performance` global throws ReferenceError and aborts the whole
# bootstrap script (on google this is exactly what strands `google.c.maft`).
NAT_PERF_NOW:          int32 = 310   # performance.now()  -> ms since navigation start (monotonic)
NAT_PERF_MARK:         int32 = 311   # mark/measure/clearMarks/clearMeasures/setResourceTimingBufferSize (no-op)
NAT_PERF_GETENTRIES:   int32 = 312   # getEntries/getEntriesByType/getEntriesByName -> [] (empty)
# ---- XMLHttpRequest (legacy async transport still referenced by real bundles) -
# A missing `XMLHttpRequest` global throws ReferenceError and aborts the whole
# referencing script (google + jQuery-era bundles probe it unconditionally).
# send() routes through the SAME fetch transport / __setFetchFixture table as
# fetch(); readyState advances 0->4 firing readystatechange + load/loadend.
NAT_XHR_CTOR:          int32 = 313   # new XMLHttpRequest()
NAT_XHR_OPEN:          int32 = 314   # open(method, url[, async])
NAT_XHR_SEND:          int32 = 315   # send([body])
NAT_XHR_SETREQHDR:     int32 = 316   # setRequestHeader(name, value)
NAT_XHR_GETRESPHDR:    int32 = 317   # getResponseHeader(name) -> value | null
NAT_XHR_GETALLRESPHDRS: int32 = 318  # getAllResponseHeaders() -> raw header block
NAT_XHR_ABORT:         int32 = 319   # abort()
NAT_XHR_ADDLISTENER:   int32 = 320   # addEventListener(type, cb)
NAT_XHR_REMOVELISTENER: int32 = 321  # removeEventListener(type, cb)
NAT_XHR_DELIVER:       int32 = 322   # internal: deferred delivery task (async send)
NAT_XHR_OVERRIDEMIME:  int32 = 323   # overrideMimeType(mime) — no-op (headless)
# ---- Real builtin-prototype method VALUES (round 10) --------------------------
# One dispatch id per prototype; the specific method NAME is carried on the
# native function OBJECT via obj_p_a (recovered through native_self), so a single
# id backs every method of that prototype. `this` is the receiver passed by
# normal dispatch or by .call/.apply/.bind. These make `X.prototype.m` a real
# callable value (the dominant transpiled/minified idiom).
NAT_OP_METHOD:         int32 = 300   # Object.prototype.*   (toString/hasOwnProperty/...)
NAT_FP_METHOD:         int32 = 301   # Function.prototype.* (call/apply/bind/toString)
NAT_AP_METHOD:         int32 = 302   # Array.prototype.*    (slice/push/map/forEach/...)
NAT_SP_METHOD:         int32 = 303   # String.prototype.*   (delegates to string_method)
NAT_NP_METHOD:         int32 = 304   # Number.prototype.*   (toString/toFixed/valueOf)
NAT_BP_METHOD:         int32 = 305   # Boolean.prototype.*  (toString/valueOf)
HOST_NATIVE_BASE: int32 = 1000

# ====================================================================
# Regex instruction opcodes (backtracking bytecode VM).
# ====================================================================
RI_CHAR:    int32 = 0          # re_x = char byte
RI_ANY:     int32 = 1          # . (any char except newline)
RI_CLASS:   int32 = 2          # re_x = class index
RI_MATCH:   int32 = 3          # accept
RI_JMP:     int32 = 4          # re_x = target pc
RI_SPLIT:   int32 = 5          # try re_x first, then re_y
RI_SAVE:    int32 = 6          # re_x = capture slot; record sp
RI_BOL:     int32 = 7          # ^
RI_EOL:     int32 = 8          # $
RI_WORDB:   int32 = 9          # \b
RI_NWORDB:  int32 = 10         # \B
RI_BACKREF: int32 = 11         # re_x = group index
RI_LAHEAD:  int32 = 12         # lookahead:  re_x = pc after child block, re_y = neg
RI_LADONE:  int32 = 13         # lookahead child terminator (accept the assertion)
RI_LBEHIND: int32 = 14         # lookbehind: re_x = pc after child block, re_y = neg
RI_LBDONE:  int32 = 15         # lookbehind child terminator (accept iff sp == lb_target)

# Regex AST node ops (scratch AST between parse and codegen).
RN_CHAR:   int32 = 0           # rn_a = char
RN_ANY:    int32 = 1
RN_CLASS:  int32 = 2           # rn_a = class index
RN_BOL:    int32 = 3
RN_EOL:    int32 = 4
RN_WORDB:  int32 = 5
RN_NWORDB: int32 = 6
RN_BACKREF: int32 = 7          # rn_a = group index
RN_GROUP:  int32 = 8           # rn_d = child (alt), rn_a = capture index (-1 = non-capturing)
RN_STAR:   int32 = 9           # rn_d = child, rn_b = greedy
RN_PLUS:   int32 = 10          # rn_d = child, rn_b = greedy
RN_QUEST:  int32 = 11          # rn_d = child, rn_b = greedy
RN_REPEAT: int32 = 12          # rn_d = child, rn_b = min, rn_c = max(-1=inf), rn_a = greedy
RN_SEQ:    int32 = 13          # rn_a = head of item list (rn_next chained)
RN_ALT:    int32 = 14          # rn_a = head of branch list (rn_next chained)
RN_LOOK:   int32 = 15          # lookahead:  rn_d = child, rn_a = neg (0 =, 1 !)
RN_LOOKB:  int32 = 16          # lookbehind: rn_d = child, rn_a = neg (0 <=, 1 <!)

