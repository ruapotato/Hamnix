# Native Browser (hambrowse) — W3C / ECMAScript Conformance Roadmap

From a 14-agent parallel audit (2026-07-16). Dual-target, host-iterated (~40 gates). ~30-50% conformant/area; full conformance needs real spec ALGORITHMS per subsystem — enabled by the modular lib/web/ tree refactor.

## ECMAScript rounds landed (lib/web/js/*, host gates in scripts/test_jsengine_*_host.sh)

- R1-6: templates/tagged templates, classes + fields + accessors, Symbol + well-known symbols, change-array-by-copy, regex named groups, generators (`function*`/`yield`/`yield*`, bounded-eager model) + the iteration protocol, Map/Set, optional chaining/nullish, for-of, Object/Reflect-ish statics.
- **R7** (this round):
  - **Generator methods** — `*name(){}` in object literals and class bodies (incl. `static *m(){}`). `test_jsengine_generators_host.sh`.
  - **Reflect** — get/set/has/ownKeys/getPrototypeOf/setPrototypeOf/defineProperty/deleteProperty/apply/construct (thin wrappers over core ops). Also fixed the `in` operator to walk the prototype chain. `test_jsengine_reflect_host.sh`.
  - **Proxy** — `new Proxy(target, handler)` with the get/set/has/deleteProperty/ownKeys traps routed through member-get/set, `in`, `delete`, Object.keys/Reflect.ownKeys; absent trap forwards to target. Deferred: apply/construct/defineProperty/getOwnPropertyDescriptor/get(set)PrototypeOf traps, revocable. `test_jsengine_proxy_host.sh`.
  - **Typed arrays + ArrayBuffer** — byte-backed (shared pool, little-endian pointer load/store): Int8/Uint8/Uint8Clamped/Int16/Uint16/Int32/Uint32/Float32/Float64. Construct from length / array / iterable / ArrayBuffer (shared view); indexed get/set with ToInt32+mask (clamp for Uint8Clamped); length/byteLength/byteOffset/buffer/BYTES_PER_ELEMENT; set()/subarray()/slice()/fill(); for-of/spread/Array.from; static of()/from(). Deferred: full Array method suite on views, DataView, BigInt64 arrays, endianness control. `test_jsengine_typedarray_host.sh`.
  - **structuredClone(value)** — deep clone preserving shared refs + cycles; plain objects/arrays/Map/Set/Date/ArrayBuffer/typed arrays; functions/symbols throw DataCloneError. Deferred: accessor props, transfer list. `test_jsengine_structuredclone_host.sh`.
- **R8** (numeric literals + JSON reviver):
  - **Numeric literal forms** — ES2021 numeric separators `_` in decimal/hex/binary/octal integers + fraction + exponent (`1_000_000`, `0xFF_FF`, `1e1_0`); binary `0b`/`0B` and octal `0o`/`0O` integer literals; BigInt `n` suffix (originally kept as a float64 Number — **now a true arbitrary-precision BigInt as of R11**, see below). Fixed in `lib/web/js/lexer.ad` (lex_number) + `lib/web/js/value.ad` (parse_num_bytes). Previously the lexer split `1_000` into `1` + identifier `_000` (ReferenceError) and read `0b101` as `0` + identifier — a hard abort of any modern minified/transpiled bundle using these forms. `test_jsengine_numliteral_host.sh`.
  - **JSON.parse(text, reviver)** — the optional second argument is now honored: a bottom-up InternalizeJSONProperty walk calls `reviver.call(holder, key, value)` at every node (children first), deletes the property on an `undefined` result, handles arrays by index, threads the holder object as `this`, and invokes the root with key `""`. Previously the reviver was silently ignored (parsed data returned untransformed — a correctness bug). Fixed in `lib/web/js/builtins/collections.ad` (json_parse_val + json_internalize/json_intern_props/json_reviver_call). `test_jsengine_numliteral_host.sh`. (JSON.stringify's `replacer` argument was subsequently landed in R10 — see above.)
- **R9** (class private members + static blocks + global symbol registry):
  - **Class private `#fields` / `#methods` / accessors** — the lexer now tokenizes `#name` as `TK_PRIVATE`, retaining the leading `#` so a private slot's key can never collide with a normal string property. Private instance fields (`#x = v`), private methods (`#m(){}`), private getters/setters (`get #x(){}`), and private static members (`static #n = v`) all parse and evaluate; `this.#x` / `Ctor.#n` access works through the ordinary member get/set path (stored under the `#`-prefixed key). The `#x in obj` **brand check** is a dedicated `ND_PRIVNAME` LHS handled by the `in` binop (never evaluated as a value; probes the private slot through the prototype chain). Crucially, private slots are HIDDEN: `Object.keys`, `for-in`, and `JSON.stringify` skip any key beginning with `#` (via a new `is_hidden_key` in `lib/web/js/value.ad`). Fixed across `lib/web/js/{lexer,parser,interp,value}.ad` + `builtins/collections.ad`. `test_jsengine_es2022priv_host.sh`.
  - **`Symbol.for(key)` / `Symbol.keyFor(sym)`** — a process-wide symbol registry interns by string key so `Symbol.for("a") === Symbol.for("a")` (and stays distinct from a plain `Symbol("a")`); the registration key is stored back on the symbol as a hidden `@@forkey` own property for `Symbol.keyFor` reverse lookup (returns `undefined` for an unregistered symbol; throws `TypeError` on a non-symbol). Added in `lib/web/js/builtins/r7.ad` (symbol_for/symbol_key_for) + registered in `setup.ad`. `test_jsengine_es2022priv_host.sh`.
  - **`static { }` initializer blocks** — the class-body parser now recognizes a bare `{ ... }` member after `static` (`ND_METHOD` with `n_c=3`); `eval_class` runs the block bodies interleaved with `static` fields in SOURCE ORDER, with `this` = the class object and the class's own name bound in a class-scope env (so `static { C.x = ... }`, `static y = C.z`, and `this.#priv` inside a block all resolve). Added in `lib/web/js/parser.ad` (parse_class) + `interp.ad` (eval_class static pass). `test_jsengine_es2022priv_host.sh`.
  - **Bonus fix (symbol-key leak)** — the same `is_hidden_key` filter also stops symbol-keyed properties (`@@sym:` keys) from leaking into `Object.keys` / `JSON.stringify`, a pre-existing correctness bug (`o[sym]=v` previously showed `@@sym:NNN` in enumeration output).
- **R10** (`for await...of` + JSON.stringify replacer):
  - **`for await (x of asyncIterable)`** async iteration — the parser recognizes a contextual `await` after `for` (inside an async body) and marks `ND_FOROF` (`n_d=1`); `exec_forof` dispatches to a new `exec_forawait` (`lib/web/js/interp.ad`). It first tries the source's `@@asyncIterator` (a fresh global `g_sym_async` symbol, also installed on the generator prototype so an `async function*` is its own async iterator): it calls `next()`, **awaits** the result via the engine's existing deterministic `do_await` (microtask-drain) to get `{value, done}`, and binds/executes the loop body per element (honoring `break`/`continue`/labels through the shared `forof_bind`/`forof_after`). If there is no `@@asyncIterator`, it falls back to the **async-from-sync wrapper**: it materializes the sync iterable via `collect_iterable_into` and **awaits each produced value** — so `for await (x of [Promise.resolve(1), 2, ...])` yields the resolved values in order. Because `await` drains the deterministic queue, the whole loop completes synchronously (no call-stack suspension), matching the engine's eager async/generator model. `test_jsengine_asynciter_host.sh`.
  - **`JSON.stringify(value, replacer[, space])`** — the 2nd argument is now honored. A **function replacer** `fn(key, value)` is called (with the holder as `this`) per property, its result substituted; an **array replacer** is an allowlist of object keys to include (applied at every object level, not to array indices). `value.toJSON(key)` is also honored on this path. Because a replacer function does its own string-building (which cannot run while the shared build cursor is held by `json_emit` — the same two-phase hazard as `String.replace`), the value tree is **pre-transformed** into a fresh tree (`json_transform`/`json_rep_value`/`json_key_allowed`, root holder `{"": value}`) BEFORE the emitter runs; the no-replacer path stays on the fast direct emitter (byte-identical to before). Added in `lib/web/js/builtins/collections.ad` (`json_stringify_full` + helpers). `test_jsengine_asynciter_host.sh`.
- **R11** (true arbitrary-precision BigInt — closes the largest ES gap):
  - **BigInt value type** — a new `TAG_BIGINT` value tag (`lib/web/js/consts.ad`) whose `v_ref` indexes a new immutable bignum record: a **sign + base-2^32 little-endian limb magnitude** (`lib/web/js/bigint.ad`, pools `bi_sign`/`bi_off`/`bi_len` over a shared `bi_limbs` uint32 bump pool). Records are immutable, so a sign flip shares limbs with no copy. `123n` / `0xffn` / `0b1010n` / `0o17n` literals now LEX to a distinct `TK_BIGINT` token carrying the source digits (lexer keeps the text sans `n`; the regex/divide heuristic treats a BigInt literal as a value so `7n / 2n` is division, not a regex) and PARSE to `ND_BIGINT`, evaluated by exact `bi_parse` — no more float64 precision loss past 2^53.
  - **Arithmetic** — exact `+ - * / % **` on two BigInts: schoolbook magnitude add/sub/mul, **bit-by-bit long division** (÷ **truncates toward zero**, `%` takes the dividend's sign), and exponentiation-by-squaring. `++`/`--` and compound `+= -= *= …` stay BigInt. Mixing a BigInt with a Number in an arithmetic/bitwise op throws **TypeError** (per spec); unary `+` on a BigInt throws; `/ 0n` and a negative `**` exponent throw **RangeError**.
  - **Comparisons** — `< > <= >=` and `== ===` including exact **BigInt-vs-Number numeric compare** (`2n < 3`, `5n == 5` true, `5n === 5` false — `===` is false across types); loose `==` also handles BigInt-vs-String/Boolean by mathematical value.
  - **Bitwise** — `& | ^` implemented with **infinite-width two's-complement** semantics (correct for negative operands via a to/from two's-complement conversion); `<<` is a magnitude shift keeping sign; `>>` is an **arithmetic (floor) shift** (`-8n >> 1n === -4n`, `-7n >> 1n === -4n`); `~x === -(x+1)`. `>>>` throws TypeError (BigInts have no unsigned shift).
  - **Conversions / methods** — `typeof 1n === "bigint"`; `String(1n)` / `` `${1n}` `` / `1n.toString(radix)` (2–36); truthiness (`0n` falsy); `BigInt(number|string|boolean|bigint)` (non-integer Number → RangeError); `BigInt.asIntN(bits,x)` / `BigInt.asUintN(bits,x)`. `JSON.stringify` on a BigInt (or an object containing one) throws **TypeError**, per spec. Console/display prints the digits with no trailing `n` (String() semantics). Touched: `lib/web/js/{bigint,consts,state,lexer,parser,interp,setup}.ad` + `builtins/{collections,strings}.ad`. `test_jsengine_bigint_host.sh` (40 asserts vs known results: `2n**100n`, `50!`, div/mod signs, two's-complement bitwise, mixing/JSON TypeErrors).
  - Coverage is honest and broad: bitwise AND/OR/XOR/shifts (incl. negatives), `asIntN`/`asUintN`, and all six arithmetic ops are DONE. NOT done: `BigInt64Array`/`BigUint64Array` typed arrays, `DataView` get/setBigInt64, and a `Number(bigint)` that round-trips huge magnitudes exactly (uses a float64 approximation for out-of-int64 comparison fallbacks only).
  - Next JS gap: async iterator `return()`/`throw()` propagation and `Symbol.asyncIterator` on user objects returning promises from `next()` (partially exercised; async generators covered); then `BigInt64Array`/`DataView` BigInt accessors.
- **R12** (destructuring ASSIGNMENT + `window`-shaped globals):
  - **Destructuring as an ASSIGNMENT statement** — `[a,b]=[b,a]` (swap), `[x,,y]=arr` (elision/skip), `[a,...rest]=arr` (rest), `({p,q}=obj)`, `({name:nm}=obj)` (rename), object rest `({a,...others}=obj)`, element/property **defaults** (`[m=5]`, `{v=5}`), nested patterns (`[p,[q,r]]=…`, `({user:{id,tags:[t0,t1]}}=…)`), and member/index leaf targets (`[o.x,a[0]]=…`) all work. Previously this did NOTHING: `assign_to` handled only ident/member/index LHS, so an array/object **literal** LHS was silently ignored — only the `let/const/var` DECLARATION path (`bind_pattern`) understood patterns. Root cause + fix: added `assign_array_pattern`/`assign_object_pattern` in `lib/web/js/interp.ad`, dispatched from `assign_to` on an `ND_ARRAY`/`ND_OBJECT` target; they walk the literal's element list and assign each leaf via `assign_to` (recursing for nested patterns). Because `eval_assign` already fully evaluates the RHS before calling `assign_to`, the swap is correct. Two supporting parser fixes (`lib/web/js/parser.ad`): array-literal **elisions are now preserved** as a new `ND_HOLE` node (`parse_array_lit`) so `[x,,y]=arr` maps `y←arr[2]` (a hole also now reads as `undefined` with correct `.length` in a value context, via `eval_array_lit`); and object shorthand **CoverInitializedName** `{x = default}` parses (wrapping the shorthand in an `ND_ASSIGN`) so `({v=5}=obj)` applies its default. Declaration destructuring is unchanged and re-asserted green. `test_jsengine_destructure_host.sh`.
  - **`window`-shaped globals** — the existing `window`/`globalThis`/`self` alias now also carries the browser viewport/URL data properties pages read ungated: `innerWidth`/`innerHeight`/`outerWidth`/`outerHeight` (default 1024×768), `scrollX`/`scrollY`/`pageXOffset`/`pageYOffset` (0), `devicePixelRatio`/`screenX`/`screenY`, a minimal `location` object (`href`/`protocol`/`host`/`hostname`/`port`/`pathname`/`search`/`hash`/`origin`), and a `screen` object. Defined on the global scope (like `navigator`) so both `window.innerWidth` and bare `innerWidth` resolve. Added in `lib/web/js/setup.ad`. `window.addEventListener('load'|'DOMContentLoaded', …)` / `window.onload` firing is now DONE in the DOM layer (round 9, `lib/web/dom/canvas.ad`: `window` gets the listener methods in `_js_build_document`, window listeners share the `DOC_EL` lifecycle, and `_fire_window_onload` runs `window.onload` after the load listeners). `test_jsengine_destructure_host.sh`.
- **R13** (`Object.hasOwn`/`Object.is` + RegExp `s`/`y` flags + signed-zero unary minus):
  - **`Object.hasOwn(obj, key)`** — the ES2022 static replacement for `hasOwnProperty` that dodges a shadowed/absent prototype method. Coerces the key to a property name and reports an OWN (not inherited) property, including array indices and the array `length`; a non-object first arg returns `false`. `object_has_own` in `lib/web/js/builtins/collections.ad`, registered in `setup.ad` (NAT_OBJECT_HASOWN).
  - **`Object.is(a, b)`** — the SameValue algorithm: identical to `===` except **NaN is equal to itself** and **+0 is distinct from -0** (via a `1/x` sign probe), with reference identity for objects and value equality for primitives/BigInt (falls through to `strict_eq`). `same_value` in `lib/web/js/builtins/collections.ad`, registered in `setup.ad` (NAT_OBJECT_IS).
  - **Unary minus preserves signed zero** — `eval_unary` (`lib/web/js/interp.ad`) now computes `U_NEG` as an IEEE negation (`-1.0 * x`) instead of `0.0 - x`, so `-(+0) === -0` (`1/-0 === -Infinity`). The old `0.0 - x` collapsed `-(+0)` back to `+0`, which broke `Object.is(0, -0)` and any `Math.sign`-of-negative-zero style logic. `String(-0)` stays `"0"` per spec.
  - **RegExp `s` (dotAll) flag** — `.` now also matches U+000A when the `s` flag is set (`re_da` global threaded from `obj_re_flags` bit3 through `regex_search` into the `RI_ANY` opcode); `/x/s.dotAll` is reflected, and dotAll flows through `exec`/`test`/`match`/`matchAll`/`replace`/`split`/`search` and the `new RegExp(src, "s")` constructor. `lib/web/js/{state,setup}.ad`.
  - **RegExp `y` (sticky) flag** — a sticky match must begin **exactly at `.lastIndex`** with no forward scan: `regex_search` returns `-1` after a single anchored attempt when sticky (bit4). `.sticky` is reflected; `exec`/`test` read+advance `.lastIndex` for `y` (not only `g`); and the `matchAll`/`replace`/`match` global loops honor the anchor so a gap stops iteration (correct tokenizer semantics). `lib/web/js/setup.ad`. `test_jsengine_es2022b_host.sh` (34 asserts vs node: hasOwn/is edge cases, `1/-0`, `/a.b/s`, sticky `.lastIndex` progression + non-contiguous stop + named-group×dotAll combo).
  - Regex flags: **`d` (indices) flag — now DONE (2026-07-18, gate `reindices`)**: `/re/d` reflects `hasIndices`, and a match result carries a parallel `.indices` array of `[start,end)` pairs per capture group (undefined for a non-participating group) plus `.indices.groups` (named-group pairs; undefined when there are none) — only present when `d` is set, threaded through both `exec()` and `matchAll()`. Exposure of the already-tracked `re_cap[]` start/end (`lib/web/js/setup.ad` `js_make_regex` bit5 + `regex_result_array`). Regex flags: **`u`/`v` (Unicode) mode — now DONE as an honest byte-model subset (2026-07-18, gate `reunicode`)**; see the round-8 section below.
- **R14** (HTML event-loop APIs — `requestAnimationFrame`/`cancelAnimationFrame` + the ordering guarantees frameworks depend on):
  - **Already present (verified, NOT rebuilt)** — `setTimeout`/`setInterval`/`clearTimeout`/`clearInterval`/`queueMicrotask` and the whole **microtask queue** landed with the Promise/async rounds (`lib/web/js/builtins/timers.ad` + `promise.ad`). Timers and promise reactions share ONE bounded deferred-callback table (`lib/web/js/state.ad`, `TIMER_CAP` 4096, `TIMER_DRAIN_CAP` 10000) that `js_eval` DRAINS at end-of-script in **(microtask-first, then delay, then insertion)** order — a `setTimeout(...,0)` scheduled before a `queueMicrotask`/`Promise.resolve().then` still runs AFTER it. NOT wall-clock async; the drain is deterministic and capped so a self-re-enqueuing `setInterval` cannot spin forever.
  - **`requestAnimationFrame(cb)` / `cancelAnimationFrame(id)`** (this round) — rAF callbacks register on the SAME timer table (`raf_schedule`, `timer_raf=1` flag) as one-shot **frame macrotasks**. Each enqueues into a frame indexed by `raf_sched_gen`; its ordering "delay" is the frame's **virtual timestamp** `(gen+1)*FRAME_MS_VC` (`FRAME_MS_VC=16.0`, ~60fps), which is also the `DOMHighResTimeStamp` handed to the callback at fire time. Because the timestamp is derived purely from a frame counter — **no `Date`, no random, no sleeping** — drains stay fully deterministic. Sharing the timer table means timers and frames live on ONE virtual timeline: a `setTimeout(...,0)` (delay 0) fires before frame 0 (t=16), and a microtask still beats both. A rAF that re-schedules a rAF from inside its own callback lands in the **NEXT** frame, not an infinite same-frame loop: `raf_sched_gen` is bumped the moment the first callback of the current frame fires (in `run_one_task`), so an animation loop advances 16→32→48… frame by frame and terminates at the drain cap. `cancelAnimationFrame` reuses `timer_clear` (shared id space). Added in `lib/web/js/builtins/timers.ad` (`raf_schedule` + `run_one_task` rAF branch), `state.ad` (`timer_raf`, `FRAME_MS_VC`, `raf_sched_gen`), `consts.ad` (NAT_REQUEST_ANIM_FRAME/NAT_CANCEL_ANIM_FRAME), `builtins/collections.ad` (dispatch), `setup.ad` (globals), reset in `api.ad` (`js_init`). `test_hambrowse_timers_host.sh` (15 asserts vs node/browser order: microtask-before-macrotask from BOTH `queueMicrotask` and `Promise.resolve().then`; timers by delay-then-insertion; clear cancels; interval-N-then-clear; args forwarded; nested timer→microtask; rAF runs with timestamp; cancelAnimationFrame; rAF-loop virtual-clock 16/32/48; and the shared timer+frame timeline).
  - Honest remaining gaps: rAF/timer ids share one namespace (spec keeps them separate — harmless in practice; real code doesn't cross-cancel); the model is eager end-of-script drain (no true suspension / no wall-clock scheduling / no real fps pacing — a `setTimeout(f, 5000)` runs immediately at drain, ordered by delay); no `setImmediate`/`process.nextTick`; `queueMicrotask` shares the macrotask table (flagged, not a separate FIFO structure) but drains with correct microtask-first priority.
- **R15** (self-contained Web-platform APIs — `URL` / `URLSearchParams` + `btoa` / `atob`):
  - **`new URL(url[, base])`** — a WHATWG-URL parser that splits into `href`/`protocol`/`username`/`password`/`host`/`hostname`/`port`/`origin`/`pathname`/`search`/`hash` own properties plus a live `searchParams`. The component splitter mirrors the one `lib/web/dom/canvas.ad` (`_url_parse_into`) already uses for `location`, EXTENDED here with userinfo (`user:pass@`) and a `searchParams` object. **Relative-to-base resolution** covers the cases real routers use: an already-absolute url (used as-is); scheme-relative `//host/x` (inherits base scheme); path-absolute `/x` (base origin + path); fragment-only `#h` and query-only `?q` (rebuild off the base minus its own fragment/query); and a relative segment `x` / `../x` / `./x` (replace the base's last path segment, then RFC-3986 `remove_dot_segments` fold — `new URL('../d','https://ex.com/a/b/c').pathname === '/a/d'`). A relative url with no absolute base throws **TypeError**. `toString()`/`toJSON()` return `href`. Added in `lib/web/js/builtins/url.ad` (`_url_split`/`_url_resolve`/`_url_remove_dots`/`_url_construct`), constructors + prototypes in `install_url_apis` (`setup.ad`). Honest limit: **`searchParams` is one-way** — it is populated from the initial query, but mutating it does NOT rewrite `url.search`/`url.href` (documented; avoids the accessor-backing machinery the object model lacks).
  - **`URLSearchParams(init)`** — from a `?a=1&b=2` query string OR a plain object of key→value pairs. Full method set: `get`/`getAll` (repeated keys preserved in order), `has`, `set` (overwrite the first match + drop the rest, or append when absent), `append`, `delete`, `toString`, `forEach(cb, thisArg)`, `entries`/`keys`/`values`, `sort` (stable by name), and a default `[Symbol.iterator]` === `entries()` so `for (const [k,v] of params)` works. Correct **application/x-www-form-urlencoded** codec: `toString` percent-encodes and maps space→`+`; parsing decodes `%XX` and `+`→space (`'a b'`→`'a+b'`, round-trips). Pairs are stored under hidden (`@@k`/`@@v`) parallel arrays so `Object.keys`/`JSON.stringify` never surface engine state.
  - **`btoa(str)` / `atob(b64)`** — standard RFC 4648 base64 with `=` padding. `btoa('Hello') === 'SGVsbG8='`; padding edges `btoa('M')==='TQ=='`, `btoa('Ma')==='TWE='`; `atob` tolerates ASCII whitespace (space/tab/newline/CR/FF), stops at `=`, and throws **InvalidCharacterError** on an invalid base64 char; full round-trip. Honest limit: this engine stores strings as BYTES (latin1 code units), so a code unit is always ≤ 0xFF — the spec's `btoa` >0xFF `InvalidCharacterError` cannot trigger for byte inputs (multi-byte UTF-8 is encoded byte-wise). Added in `lib/web/js/builtins/url.ad` (`_btoa`/`_atob`), globals via `def_global_native`.
  - Gate: `test_hambrowse_url_host.sh` (37 asserts — URL split + origin, all six relative-resolution shapes + dot-folding + no-base TypeError, URLSearchParams get/getAll/set/append/delete/toString encoding + repeated keys + iteration order + for-of + sort, base64 round-trip + padding + whitespace + validation). Zero regressions across the existing JS gates (`jsengine_host`, `es2022`, `es2022b`, `timers`, `structuredclone`, `typedarray`, `proxy`, `webstorage`).
- **R16** (REAL over-the-wire `fetch()` — the marquee capability separating a toy browser from a real one):
  - **What `fetch()` did before** — it was fixture-only: the engine (extern-free, dual-target) resolved `fetch(url)` synchronously against a JS-settable `__setFetchFixture` table, never touching the network. `lib/web/js/module.ad`'s comment noted "real http9 traffic is wired in a front-end", but nothing wired it — a page's `fetch()` could only see canned data.
  - **The kernel HTTP/DNS path used (NO sockets)** — Hamnix is Plan-9-shaped: networking is the `/net` FILE TREE, not `socket()`. The reusable client is `user/http9.ad`'s `http_get(url, dst, dst_cap, *out_status, *out_body_off, *out_body_len)`: it parses the URL, resolves the host via the in-kernel DNS resolver (`sys_resolve`, syscall 269 → `drivers/net/dns.ad`) or an IPv4 literal, connects through `user/net9.ad`'s `net_dial` / `net_dial_tls` (TLS for `https`), sends an `HTTP/1.0` GET, drains the response honoring `Content-Length` / chunked (decoded in place), and follows up to 6 redirects. This is the SAME client a page navigation and `<img>`/`<link>` use — proven on-device.
  - **The engine seam (stays extern-free)** — added `js_set_fetch_transport(fn)` (mirrors the existing `js_set_module_resolver` seam; `lib/web/js/api.ad` + `state.ad`). The engine holds only a raw address and, in `do_fetch` (`lib/web/js/builtins/fetch.ad`), calls the embedder's transport for an absolute `http(s)://` URL, then PARSES the returned raw HTTP bytes itself (`make_response_net`: status line → `status`/`statusText`, header block → a full **Headers** object, body slice → `__body`). A non-absolute URL (bare fixture key) still routes to the fixture table, so both coexist and every existing gate stays green. Transport returns NULL on a DNS/connect failure → the Promise **rejects** (TypeError); a non-2xx HTTP status **fulfills** with `ok=false` (Fetch-spec correct). `init.method`/`init.body` are forwarded.
  - **Richer Response/Headers** — Headers now stores EVERY parsed response header (ordered lowercased-name/value arrays on hidden slots) with case-insensitive **`get`**, new **`has`**, and **`entries()`** (`[[name,value],…]`); `text()`/`json()`/`arrayBuffer()` read the real body bytes. `NAT_HEADERS_HAS`/`NAT_HEADERS_ENTRIES` added (`consts.ad`, dispatch in `builtins/collections.ad`).
  - **On-device wiring (LANDED)** — `user/hambrowse.ad` registers an `http_get`-backed transport (`_js_fetch_transport`, into a dedicated `jsfetch_buf` kept separate from the page's `resp_buf`) via `js_set_fetch_transport` before the first `he_layout()`. Because the whole browser is ONE Adder compilation unit, this is the same engine `htmlengine` drives — one shared `fetch_transport_addr`, no `htmlengine`/`canvas.ad` passthrough needed. So a page `<script>`'s `fetch(url)` makes an ACTUAL request through `/net`. Native compile verified (`x86_64-adder-user`).
  - **Verification** — QEMU-free host gate `test_hambrowse_fetch_host.sh` drives the EXACT on-device parse path with a deterministic canned transport (`js_host --fetch-net`): asserts `status`/`ok`/`statusText`/`url` read off raw wire bytes, full Headers `get`/`has`/`entries`, a 404 fulfilling with `ok=false`, a transport failure rejecting, and `init.method`/`body` (POST) forwarding. Same parse whether bytes come from a socket or the buffer. Zero regressions (`jsengine_host`, `fetch_host`, `promise`, `async`, `es2022`, `modules`, `timers`, `url`).
  - **Remaining link (honest)** — the host gate proves the wire-response HANDLING; a live end-to-end on-device QEMU-net run (page `fetch` → `10.0.2.2:<port>` fixture server → asserted body via serial markers) is not yet captured this round (heavy/contended QEMU slot). And http9 is **GET-only**: the transport forwards `init.method`/`body`, but the http9 bridge always issues GET — a real over-the-wire POST awaits an http9 `http_post` entry point. Both are the follow-ups; the plumbing and parse are complete and tested.
- **R17** (formatting / i18n — `Intl.NumberFormat` + `Intl.DateTimeFormat`, `Date.toLocale*String`, and the last `Date` / `JSON` gaps). New module `lib/web/js/builtins/intl.ad`; NAT ids 240–257 (`consts.ad`); `Intl` namespace + Date prototype/statics registered in `setup.ad`; native dispatch tail chained from `call_native_url` → `call_native_intl`. Gate `test_hambrowse_intl_host.sh` (42 asserts vs node semantics; every date case uses a FIXED epoch-ms — no `Date.now`/random — so output is deterministic).
  - **`Intl.NumberFormat(locales, options).format(n)`** — thousands **grouping** (`1234567.891` → `1,234,567.891`), `minimumFractionDigits`/`maximumFractionDigits` **rounding** (ties away from zero, spec default `halfExpand`: `1234.5678`→`1,234.57`, `2.5`→`3`), `minimumFractionDigits` padding (`5`→`5.00`), `style:'currency'`+`currency` (`$1,234.50`, `-$1,234.50`, `€9.90`, `£3.00`, `¥`; 2 fraction digits by default), `style:'percent'` (×100 + `%`, 0 default fraction digits: `0.1234`→`12%`), and `useGrouping:false`. Rounding is done in **int64** (scale by 10^maxFrac, round, split) to avoid float drift.
  - **`Intl.DateTimeFormat(locales, options).format(date)`** + **`Date.prototype.toLocaleDateString`/`toLocaleTimeString`/`toLocaleString`** — common option bags: `year`/`month`/`day` (numeric `M/D/YYYY`; `month:'long'`→`January 1, 2021`; `month:'short'`→`Jan 1, 2021`; `month:'2-digit'`), `weekday:'long'/'short'` prefix, `hour`/`minute`/`second` in 12h (`1:01:01 AM`, midnight→`12 AM`, noon→`12 PM`) or 24h via `hour12:false` (`01:01`). An empty option bag defaults to the numeric date (matches the Intl default). Renders UTC fields directly (`date_decompose`), build-safe.
  - **`Date` gaps** — `Date.parse(iso)` (ISO 8601: `YYYY-MM-DD`, optional `T|space HH:MM[:SS[.sss]]`, optional `Z`; NaN on malformed), the **string `new Date("2021-01-01T…")` constructor** now routes through the same ISO parser (was `NaN`), `Date.UTC(y,mo,d,h,mi,s,ms)` (month-overflow normalized like the component ctor), `getTimezoneOffset()`→`0` (engine is UTC — documented), `setTime(ms)`, and the `setUTC*`/`set*` setters (`setUTCFullYear`/`setUTCMonth` (0-based, overflow-normalized)/`setUTCDate`/`setUTCHours`/`setUTCMinutes`/`setUTCSeconds`/`setUTCMilliseconds`; the non-UTC aliases map to the same natives since the clock is UTC). Setters decompose→mutate one field→recompose via Hinnant `days_from_civil`.
  - **`JSON` gaps** — the `reviver` arg to `parse`, the function/array `replacer` + `space` indentation to `stringify`, and **general user `toJSON`** (any object's `toJSON` method, via the pre-transform) were already landed (R8/R10) and are exercised here for regression. The one genuine remaining bug fixed this round: **Date `toJSON` on the fast (no-replacer) path** — `JSON.stringify({d:new Date(0)})` previously serialized a Date as `{}` (the fast direct emitter never consulted `toJSON`); now `Date.prototype.toJSON` (== `toISOString`) is honored directly in `json_emit` by emitting the quoted ISO text into the current build (no nested string-build, no `invoke` — avoids the two-phase cursor hazard that forces the general case onto the pre-transform path). Covers compact, pretty (`space`), root, and array-element positions. Honest scope: **arbitrary user-defined `toJSON` is honored whenever a function or array replacer is present** (that path pre-transforms the tree, invoke-safely); with no replacer (including the plain `JSON.stringify(x)` and the `JSON.stringify(x, null, 2)` space-only form) `toJSON` is honored for Date specifically (the dominant real case) but not yet for other user objects — those still need the invoke-safe transform, deferred to avoid allocating a shadow tree on the hot path.
  - **Honest locale-simplification scope (documented in `intl.ad`)** — the `locales` argument is ACCEPTED but ignored beyond **en-US**: grouping is `','`, the decimal point is `'.'`, and month/weekday names are the English long/short forms. Non-en digit shaping, separators, calendars, and `resolvedOptions()`/`formatToParts`/`formatRange` are out of scope. Currency symbols cover the common set (USD/CAD/AUD/NZD→`$`, GBP→`£`, EUR→`€`, JPY/CNY→`¥`); an unrecognized code renders as `CODE ` (code + space). Because the engine has no wall-clock timezone, all dates are UTC, so `getTimezoneOffset()` is `0` and `toLocale*String` render UTC fields.
  - Zero regressions across the existing JS gates (`jsengine_host`, `json`, `date`, `es2020`, `es2022`, `es2022b`, `timers`, `stdlib`, `structuredclone`, `url`).

## Coverage by area

| Area | Coverage | Gaps | High |
|---|---:|---:|---:|
| css-backgrounds-borders | 55% | 6 | 1 |
| css-grid | 38% | 11 | 4 |
| html-forms | 48% | 9 | 2 |
| html5-parsing | 32% | 10 | 3 |
| dom-events | 72% | 7 | 1 |
| css-values-units | 33% | 12 | 5 |
| css-box-model | 35% | 10 | 4 |
| css-selectors | 40% | 1 | 1 |
| ecmascript-language | 40% | 12 | 7 |
| css-flexbox | 56% | 12 | 3 |
| css-text-fonts | 45% | 1 | 1 |
| css-positioning | 60% | 10 | 4 |
| ecmascript-builtins | 55% | 15 | 6 |
| dom-core | 68% | 13 | 2 |

## FRESH AUDIT 2026-07-17 (verified against the modular lib/web/ tree)

The 2026-07-16 "High-impact worklist (56)" below was captured on the PRE-refactor monoliths
— its `lib/htmlengine.ad`/`lib/jsengine.ad` line numbers are dead, and many items have since
landed. A read-only re-audit against the current `lib/web/{js,html,css,dom,layout}/` tree +
the passing host gates found the following.

**RESPONSIVE + WEB-API ROUND 2026-07-18 (three genuinely-missing high-value W3C features, host-verified):**
- **[css-conditional] CSS `@media` queries — responsive CSS (NEW, gate `media`).** The engine had NO
  `@media` support: a `@media (...) { ... }` block was mis-parsed by the ruleset scanner (prelude read
  as a selector, inner rules as garbage decls) and silently dropped, so every responsive breakpoint was
  ignored. Now `lib/web/css/cascade.ad`'s sheet scanner (`_parse_sheet`) recognises `@`-at-rules
  (`_parse_at_rule`) and evaluates a **Media Queries Level 4 subset** against the LIVE viewport (`bw`×`bh`)
  via `_media_matches`/`_mq_query`/`_mq_feature`: media types (`screen`/`all` match; `print`/`speech` do
  not), `and`, comma (**OR** list), `not`, `only`, and the range/discrete features **`min-width`/
  `max-width`/`width`, `min-height`/`max-height`/`height`, `orientation` (landscape/portrait from bw vs bh),
  and `prefers-color-scheme`** (the engine renders LIGHT, so `dark` never matches). A matching block's inner
  rulesets cascade EXACTLY like top-level rules (same `_parse_ruleset` path, correct source-order
  specificity); a non-matching block is skipped; other block at-rules (`@keyframes`/`@font-face`/`@supports`/
  `@page`) are brace-matched and skipped, and statement at-rules (`@import`/`@charset`/`@namespace`) skip to
  `;`. Length tokens parse px/em/rem (em/rem ×16). Verified HOST-ONLY render-to-PNG (`test_hambrowse_media_host.sh`,
  fixture `hambrowse_media.html`, 16 pixel asserts): the SAME page rendered at widths **400/700/1000** flips
  each element's background per the breakpoint it should match (`max-width:500`, `min-width:800`,
  `min-width:600 and max-width:900` range, `orientation:landscape`), and a `prefers-color-scheme:dark` block
  never applies. **HONEST SCOPE / LIMITATIONS:** viewport width = `bw` (window content width — no separate
  device-width / scrollbar accounting); em/rem length units use a fixed 16px root (no cascade-resolved root
  font-size); truly-unknown features (`hover`/`pointer`/`resolution`/`aspect-ratio`/…) evaluate PERMISSIVELY
  (match) so a block gated on an unmodelled feature still contributes its rules (a deliberate don't-drop-content
  choice); the custom-property pre-sweep (`_collect_cvars`) still harvests `--vars` inside a non-matching
  `@media` block; and this is a STATIC evaluation at layout time (a resize re-lays-out and re-evaluates, but
  there is no live matchMedia/change-listener surface).
- **[ecmascript] RegExp `d` (indices) flag — DONE (gate `reindices`).** See the R13 regex-flags note below —
  `/re/d` now reflects `hasIndices` and exposes per-capture `[start,end)` `.indices` (+ `.indices.groups`)
  through `exec()`/`matchAll()`.
- **[ecmascript] RegExp `u`/`v` (Unicode) mode — SUBSET DONE (gate `reunicode`, round-8).** `\u{CP}` code-point
  escapes + surrogate-pair combining (compiled to UTF-8 byte atoms), ASCII-range `\p{...}`/`\P{...}` property classes,
  `.unicode`/`.unicodeSets` reflection, and string/template `\u{CP}` decoding. Full code-point-granular semantics
  (byte-level `.`, astral `\p{}`) deferred — see the round-8 section + round-9 map.
- **[html-forms] `FormData` — DONE (gate `formdata`).** The AJAX form multimap primitive; see live-worklist
  item #10 below. Reuses the URLSearchParams `@@k/@@v` storage + methods; only the constructor is distinct.

**CSS-ANIMATIONS + matchMedia ROUND 2026-07-18b (two genuinely-missing high-value W3C features, host-verified):**
- **[css] CSS animations — `@keyframes` + `animation`/`animation-name` STATIC END-STATE (NEW, gate `keyframes`).**
  The engine had NO animation support: `@keyframes` blocks were brace-skipped by `_parse_at_rule` and
  `animation`/`animation-name` declarations silently ignored, so an element that settles to a keyframed end
  colour (fade-in, colour-cycle, reveal) rendered its BASE (pre-animation) state. Now `lib/web/css/cascade.ad`
  records each `@keyframes NAME { … }` definition's SETTLED frame — the `100%`/`to` keyframe, else the last
  frame (`_parse_keyframes_body`) — as an INERT end-state pseudo-rule (`_store_keyframe`, tagged `r_kfname` so
  `_rule_matches` skips it in selector matching) keyed by the interned animation name (`_kf_intern`, a
  per-`_collect_css` name registry). A rule/element declaring `animation-name: X` or the `animation` shorthand
  (the name token found by skipping durations/timing-functions/keywords via `_anim_kw_skip`) resolves `X` to a
  winner (`m_animname`) and `_cascade_match_current` OVERLAYS that keyframe's **background / text-colour /
  transform / border-colour** onto the cascade winners, regardless of source order (keyframes may be defined
  after the rule that references them — both are parsed in one `_collect_css` pass, so the id lookup pairs them).
  Verified HOST-ONLY render-to-PNG (`test_hambrowse_keyframes_host.sh`, fixture `hambrowse_keyframes.html`):
  a `.fade` box with `animation:fadein …` settles to the `100%` frame `#00aa00` (over its base `#333`), a
  `.slide` box with `animation-name:reveal` settles to the `to` frame `#cc0000`, a no-animation box keeps its
  base `#112233`, and a box whose `animation` names a NON-EXISTENT keyframes keeps its base `#445566`.
  **HONEST SCOPE / LIMITATIONS:** this is the **settled END-STATE** only — the model renders the visually-final
  frame (equivalent to `animation-fill-mode: forwards` / the end of a finite animation), with **no timeline, no
  mid-frame interpolation, no live ticking** (a headless single render has no clock — consistent with the whole
  engine's static-render model). Overlay is limited to the four visually-dominant settle properties
  (background, colour, transform, border-colour); other keyframed properties (opacity, width/position offsets,
  per-side margins) are NOT yet overlaid. An `infinite` animation has no true final state — we render its end
  keyframe anyway (a reasonable static snapshot). `animation-fill-mode`/`-delay`/`-direction` are parsed-and-
  ignored beyond name resolution. **`transition` is likewise graceful:** a static render already shows the
  resting/final state a transition animates TO (there is no interactive state change to animate FROM), so
  `transition` needs no special handling — it is a no-op decl the parser skips, which is the correct rendered
  result.
- **[dom] `window.matchMedia(query)` — the JS surface of media queries (NEW, gate `matchmedia`).** SPAs branch
  on `window.matchMedia('(min-width:…)').matches` for responsive JS (lazy-loading, layout mode,
  prefers-color-scheme); the engine exposed `@media` in CSS but had NO `matchMedia`, so such a script threw
  (`matchMedia is not a function`). Now `lib/web/dom/canvas.ad` `_match_media` (NID_MATCHMEDIA, on `window` +
  the bare global like `getComputedStyle`) returns a **MediaQueryList** whose `.matches` is the LIVE evaluation
  of the query against the viewport via a new public `he_media_match(p,n)` in `lib/web/css/cascade.ad` — the
  SAME Media Queries L4 evaluator (`_media_matches`) the stylesheet's `@media` blocks use — plus `.media`
  (the query text), `.onchange` (null), and `addListener`/`removeListener`/`addEventListener`/
  `removeEventListener`/`dispatchEvent` (NID_MQL_NOOP). Verified HOST-ONLY (`test_hambrowse_matchmedia_host.sh`,
  fixture `hambrowse_matchmedia.html`, one page at TWO widths): `(min-width:600px)` is `true` at 880 / `false`
  at 400, `(max-width:500px)` flips the other way, the `(min-width:600) and (max-width:900)` range matches only
  at 880, `screen and (min-width:400)` matches, `print`/`prefers-color-scheme:dark` never match (screen/light
  render), `orientation:landscape` matches at 880×600 but not 400 (portrait), and the listener methods accept
  args without throwing. **HONEST SCOPE:** the listener registration is a **best-effort no-op** — the headless
  engine does ONE layout, so a `change` event never fires (there is no live resize/scheme-change source); the
  MediaQueryList's `.matches` is a correct point-in-time snapshot for the render's viewport.

**CSS `filter` ROUND 2026-07-18c (the #1 remaining CSS gap, host-verified):**
- **[css] CSS `filter` — `grayscale`/`brightness`/`invert`/`sepia`/`contrast`/`saturate`/`opacity`/`blur`
  (NEW, gate `filter`).** Filter Effects Module L1. Previously ABSENT (the round-2 audit's #1 CSS hole:
  hero-image treatments, glassmorphism, disabled/reader states). Followed the documented plumbing path exactly.
  `lib/web/css/cascade.ad` parses `filter:` in `_box_decl` via a new `_filter_value(vs,ve)` that packs the first
  recognised filter function into a single int `(type << 20) | amount` (amount = an integer percent —
  `grayscale(0.5)==grayscale(50%)==50` — for the colour filters, or a pixel radius for `blur`). It rides the
  cascade/rule machinery as a full parallel property (`d_filter`/`m_filter`/`r_filter[]`, reset + specificity
  tracked like `border-radius`) with **zero reflow cost** — the value flows through the untouched
  `layout/box.ad` record set. `box.ad` resolves the winner (inline `d_filter` over stylesheet `m_filter`) into
  a `box_filter_stack`, and stamps `bfill_filter[]` (parallel to `bfill_rgb[]`, set at ALL FOUR bfill emit
  sites — the block-box close, the inline-block chip, the blockquote bar, and the page-bg fill — so a stale
  index can never leak a filter). `he_bfill_filter(i)` (`dom/canvas.ad`) exposes it, and `htmlpage_render`
  (`lib/htmlpage.ad`) runs a per-pixel **POST-PASS** `htmlpaint_filter_rect(x,y,w,h,packed)` over each filtered
  box's rect AFTER all fills/glyphs/borders are painted — so the element AND its subtree content are filtered
  together (true CSS filter semantics, not just the background). `lib/htmlpaint.ad` implements the transforms on
  the internal framebuffer: grayscale/sepia/invert as Rec.601-luma / colour-matrix mixes, brightness/contrast as
  channel scales, saturate as luma-extrapolation, and `blur` as a **separable box blur** (two 1-D passes via a
  4096-wide scratch). Verified HOST-ONLY render-to-PNG (`test_hambrowse_filter_host.sh`, fixture
  `hambrowse_filter.html`, probe `scripts/hb_filter_probe.py`): eight solid boxes (one filter each) are
  pixel-scanned across the whole image — each box's ORIGINAL colour is GONE and the EXPECTED filtered colour is
  PRESENT (grayscale `#cc4422`→`#696969`, brightness `#4c4c4c`→`#727272`, invert `#204060`→`#dfbf9f`, sepia
  `#6688aa`→`#b09d7a`, contrast `#909090`→`#a0a0a0`, saturate `#a06040`→`#d15111`, opacity `#402010`→`#9f8f87`
  (approx), blur preserves the uniform interior + visibly smears the box's text), and an unfiltered control box
  is unchanged. Zero regressions (borderradius/boxshadow/website re-run green). **HONEST SCOPE / LIMITATIONS:**
  ONE filter function per element (a space-separated filter LIST collapses to the FIRST recognised function — no
  chaining); `drop-shadow()` is recognised (parsed, no crash) but painted as a **no-op** (its offset/blur/colour
  args are not modelled); `opacity()` is APPROXIMATED by lightening toward white (there is no real backdrop to
  composite against in the static single-render model); `hue-rotate()` is not implemented; the effect covers the
  element's BORDER BOX (out-of-flow descendants painted outside the box are not filtered); `backdrop-filter`
  (filtering what is BEHIND the element) is a distinct feature, still absent. The filter runs only on the
  host render-to-PNG framebuffer path (a no-op when draws are redirected to an external RGBA target).

**TRUE PER-SIDE BORDERS ROUND 2026-07-18d (the #1 remaining CSS gap above, host-verified):**
- **[css-backgrounds-borders §4] true per-side border WIDTH + STYLE (NEW, gate `borderside`).** Previously a CSS
  border was a single UNIFORM 1px stroke — one width, one style — so a `border-bottom:2px solid` tab underline, a
  thick coloured accent side, or a dashed frame ALL collapsed to the same thin solid rectangle (the round-3
  audit's #1 CSS hole). Now `lib/web/css/cascade.ad` parses **`border-top`/`-right`/`-bottom`/`-left`** (and each
  `-width`/`-style`/`-color` longhand) plus the **`border-width`/`border-style` TRBL shorthands** (1..4 values,
  CSS top/right/bottom/left expansion) into two PACKED ints — `d_bordps` (8 bits/side width, `top | right<<8 |
  bottom<<16 | left<<24`, 0..120px) and `d_bordss` (4 bits/side style, `code+1` where 0 solid / 1 dashed / 2
  dotted / 3 double / 4 none). They ride the cascade/rule machinery as ONE parallel property pair (`r_bordps`/
  `r_bordss` + a shared `sbps` specificity slot → `m_bordps`/`m_bordss`, inline `d_` wins over sheet `m_` exactly
  like `d_bordc`), flow through the layout record set (`bbox_ps`/`bbox_ss` in `layout/box.ad`, emitted at block/
  float close alongside `bbox_rgb`), and `lib/htmlpage.ad` (`_hpg_paint_border_sides`/`_hpg_edge_h`/`_hpg_edge_v`)
  paints **FOUR INDEPENDENT EDGES** — each its own thickness and solid/dashed/dotted/double pattern (dashed/dotted
  = repeated `fill_rect` segments with an equal gap; double = two thin rules + a gap; none = skipped side).
  Cross-rule/decl composition works within a block: a uniform `border:2px solid` seeds all four sides so a
  following `border-bottom:5px dashed` overrides only the bottom (`_seed_sides_from_uniform`). Verified HOST-ONLY
  render-to-PNG (`test_hambrowse_borderside_host.sh`, fixture `hambrowse_borderside.html`, 18 pixel asserts via the
  gfx driver's arbitrary-pixel sampler): a box with a **12px left edge vs a 2px top edge** (ink 9px into the left,
  paper 2px past the top — distinct widths), a box with a **6px DASHED bottom vs a SOLID top** (bottom alternates
  ink/paper along its length, top stays continuous ink — dashed != solid), a uniform **`border:3px dashed`**
  showing an ink dash AND a paper gap on one edge, and a **`border-width:2px 10px 2px 10px`** TRBL shorthand
  giving a 10px left / 2px top. **ZERO REGRESSION proof:** a plain uniform `border:Npx solid` keeps `d_bordps==0`
  and stays on the legacy 1px `stroke_round_rect` path — **all 163 existing hambrowse fixtures render BYTE-IDENTICAL
  (ppm + display-list dump) between base and this change**; the pre-existing `border` gate stays green.
  **HONEST SCOPE / LIMITATIONS:** (1) per-side COLOURS are NOT yet modelled — the box carries ONE (uniform) border
  colour (`bbox_rgb`); the last `border-*-color` token wins for all four sides (the common single-accent-side
  pattern e.g. `border-bottom:2px solid #ccc` is correct because only one side is present, but a red-top/blue-bottom
  box paints one colour). (2) A uniform thick SOLID border (`border:5px solid`) still renders as the legacy 1px
  stroke (kept on the legacy path deliberately, for byte-identical regression); only a NON-solid uniform style
  (dashed/dotted/double) or an actual per-side longhand routes to the multi-px per-side painter. (3) Border widths
  are clamped to 0..120px (keeps the packed int positive, dodging the signed-`>>` quirk). (4) `border-radius` is
  honoured only on the legacy uniform stroke — a per-side-bordered box paints square corners (the four edges are
  axis-aligned fills; a rounded per-side border needs arc-segment stroking, deferred). (5) `groove`/`ridge`/
  `inset`/`outset` are accepted but rendered as `solid` (no 3-D bevel model). (6) Dashed/dotted dash lengths are a
  fixed heuristic (dash = 2×width, dot = 1×width, equal gaps), not the browser's exact dash algorithm.

**PER-SIDE BORDER COLOURS + FILTER FUNCTION-LISTS ROUND 2026-07-18e (round-5 — the map's #2 and #5, host-verified):**
- **[css-backgrounds-borders §4] true per-side border COLOURS (NEW, gate `bordercolor`).** Round-4 landed per-side
  WIDTH + STYLE but carried ONE uniform border colour (the last `border-*-color` token won for all four sides — so a
  red-top / blue-bottom box painted one colour). Now `lib/web/css/cascade.ad` parses **`border-top/-right/-bottom/
  -left-color`** and the **`border-color` TRBL shorthand** (1..4 colours, paren-aware so `rgb(...)` stays one token)
  into four cascade colour slots (`d_bc_t/r/b/l` scratch → `r_bctop/rt/bot/lft` rule arrays → `m_bctop/…` winners on
  one shared specificity slot `sbcs`, like the width/style pair). They flow through `layout/box.ad` (`box_bordcr/cb/
  cl_stack` → `bbox_rgb_r/b/l`, resolved at emit to each side's declared colour or the uniform fallback) and
  `lib/htmlpage.ad`'s `_hpg_paint_border_sides` now takes FOUR colours and strokes each edge in its own. A per-side
  colour longhand seeds the uniform fallback `d_bordc` ONLY when it is still unset, so an accent colour never clobbers
  an earlier uniform `border` colour (the other sides keep it); a full `border` shorthand resets the per-side colours.
  Verified HOST-ONLY render-to-PNG (`test_hambrowse_bordercolor_host.sh`, fixture `hambrowse_bordercolor.html`, 12
  pixel asserts): a box with four distinct `border-<side>-color` longhands samples **red/green/blue/yellow** per edge,
  a `border-color: #dd1111 #11bb22 #1133dd #ddbb22` TRBL shorthand expands T/R/B/L, and a single overriding
  `border-bottom-color` leaves the three unset sides at the uniform grey fallback. **ZERO REGRESSION:** a box with no
  per-side colour keeps every side at the uniform colour (byte-identical); the `borderside`/`border`/`keyframes`/
  `realarticle`/`wordwrap`/`borderradius`/`boxshadow`/`website` gates stay green. **HONEST SCOPE / LIMITATIONS:**
  (1) per-side colours take VISUAL effect only on the per-side painter path (per-side width/style declared, or a
  `border-width`/`border-style` shorthand) — a purely-uniform `border:Npx solid` with per-side colours added still
  paints the uniform colour on the legacy 1px stroke (a documented edge case; the accent-border cases that use a
  per-side width/style shorthand all route correctly). (2) The four per-side colours ride ONE specificity slot
  (`sbcs`): the highest-specificity rule that declares ANY side colour wins all four (an unset side falls back to the
  uniform colour) — matches real usage where side colours are declared together. (3) Rounded per-side corners are
  still square (per-side edges are axis-aligned fills; arc-segment stroking deferred). (4) No `!important` per-side
  colour path (uniform `!important` border colour still honoured).
- **[css-filter] filter FUNCTION-LISTS (chaining) + `hue-rotate()` (NEW, gate `filterlist`).** Round-3 landed single
  CSS filters but a space-separated LIST collapsed to the FIRST recognised function (`filter: grayscale(1)
  brightness(1.2)` silently dropped the brightness) and `hue-rotate()` was absent. Now `lib/web/css/cascade.ad`
  parses the whole function LIST: a **single** function still returns its bare packed int (byte-identical legacy
  path), but **two or more** intern into a small registry (`filt_reg_items`/`filt_reg_len`, `FILT_REG_CAP` 64,
  `FILT_MAX` 8 functions) and the value carries `FILT_LIST_MARK` (bit 28, above the 4-bit type + 20-bit amount so it
  never collides) `| regindex`. `lib/htmlpage.ad` reads the list back (`he_filter_is_list`/`_len`/`_at`) and applies
  each function IN ORDER over the box rect — true CSS chaining, since each per-pixel pass reads the previous
  function's framebuffer result. **`hue-rotate(Ndeg)`** is a real luma-preserving colour-matrix rotation in
  `lib/htmlpaint.ad` (the CSS Filter Effects matrix, integer `_hp_sin1000`/`_hp_cos1000`). Verified HOST-ONLY
  render-to-PNG whole-image scan (`test_hambrowse_filterlist_host.sh`, fixture `hambrowse_filterlist.html`, probe
  `hb_filterlist_probe.py`): `grayscale(100%) brightness(150%)` settles to a **brightened grey `#9d9d9d`** (proving
  BOTH ran — single grayscale is `#696969`), `sepia(100%) invert(100%)` does NOT stop at sepia `#b09d7a` (settles to
  the inverted-sepia `#4f6285`), `hue-rotate(120deg)` rotates red `#cc0000` toward green `#005a00`, and a
  single-function `grayscale(100%)` stays byte-identical. Zero regressions (existing `filter` gate green).
  **HONEST SCOPE / LIMITATIONS:** `drop-shadow()` is still a recognised NO-OP (its offset/blur/colour args are not
  modelled — a real drop-shadow draws a blurred silhouette OUTSIDE the box, which the in-place post-pass model does
  not support); `backdrop-filter` (filtering what is BEHIND the element) is still absent; a chained list is capped at
  8 functions and 64 distinct chained values per page (degrades to the first function past the cap); the chain is
  applied over the element's border box only (out-of-flow descendants painted outside are not filtered).

**GRID NAMED AREAS + CHECKBOX/RADIO SHAPES ROUND 2026-07-18f (round-6 — the map's #4 and #6, host-verified):**
- **[css-grid §7.3/§8.5] `grid-template-areas` / `grid-area: NAME` named placement (NEW, gate `gridareas`).** The
  #1 untouched CSS-grid gap and the highest-value real-site layout primitive: a page shell declared as a VISUAL map
  (`grid-template-areas: "head head" "side main" "foot foot"`) with each item claiming a named rectangle via
  `grid-area: NAME`. Previously `grid-template-areas` was brace/quote-skipped (the parser's own comment said
  "deferred") and `grid-area` was unrecognised, so such a container fell back to raw source-order auto-flow —
  header/sidebar/main/footer stacked in the wrong cells. Now `lib/web/css/cascade.ad` parses the quoted-string
  template (`_grid_parse_areas`/`_ga_add_cell`) into each named area's **bounding rectangle** (0-based start row/col +
  row/col span), interning names through the shared `_kf_intern` string registry; the rectangles ride the cascade as
  a full parallel property (`r_gan`/`r_ga_*` rule arrays → `m_ga_*` winners on a dedicated specificity slot `bga`,
  reset + tracked like the track templates) and the inline-`style=` path (`d_ga_*`). `grid-area` parses either a
  custom-ident NAME (`_grid_area_parse` → `d_gareaname`/`m_gareaname`) or the line-based `row-start/col-start/
  row-end/col-end` shorthand (desugared into the existing `grid-row`/`grid-column` start+span). At layout, the
  container's area map is captured per grid frame in `lib/web/layout/box.ad` (`_flex_container_open` → `flex_gan`/
  `flex_ga_*`), and `lib/web/dom/forms.ad`'s grid item-open path resolves the item's `grid-area` name to that frame's
  rectangle, driving the EXISTING explicit-placement machinery (start row/col + spans, occupancy map). When
  `grid-template-columns` is ABSENT, the column count is DERIVED from the areas template (equal 1fr tracks) so a
  `grid-template-areas`-only grid still has real track x-positions. Verified HOST-ONLY (`test_hambrowse_gridareas_
  host.sh`, fixture `hambrowse_gridareas.html`): a `150px 1fr` / `60px 200px 40px` page shell places `head`/`foot`
  spanning the full width (SEG col 0, PNG width 640) with `side`/`main` side by side on the middle row (side a 150px
  rail at x0, main starting at x150), and a SECOND grid with NO `grid-template-columns` derives three equal columns
  from `"aa aa bb" "cc cc bb"` — area `bb` landing in the right (third) column, `cc` a row below `aa`. Pixel-verified
  via `hb_grid_probe.py`. **HONEST SCOPE / LIMITATIONS:** up to `GAREA_MAX`=12 distinct named areas per container and
  8 tracks (`GRID_MAXTRACK`); a name's cells are reduced to their **bounding box** (a non-rectangular / disjoint area
  is tolerated as its bounding rect rather than rejected — a browser drops the whole template, but the useful
  static-render behaviour is to place the box); multi-ROW areas render at distinct y ONLY when `grid-template-rows`
  gives fixed heights (auto/content-sized rows compute a 0 track-top, so an explicit-row item without fixed row
  heights can overlap — real area layouts specify `grid-template-rows`, exercised in the fixture); `grid-auto-flow:
  dense`/column and `subgrid` remain out of scope.
- **[html-forms] `<input type=checkbox|radio>` PIXEL SHAPES (NEW, gate `checkradio`).** Checkbox/radio previously
  painted only their bracket ASCII (`[x]`/`(*)`); now the REAL pixel painter draws widget SHAPES. A new field-seg
  kind (`lib/web/layout/box.ad` `_mark_check_seg`: 3 = checkbox, 4 = radio; the checked flag stashed in
  `seg_fieldlen`) is set on the control's segment by `lib/web/dom/forms.ad` (the `[x]`/`( )` text is KEPT for the
  text/gate view but the pixel painter skips it), and `lib/htmlpage.ad` draws: a **checkbox** as a rounded SQUARE —
  unchecked a white box + grey border, checked an accent-blue (rgb 26,115,232) fill with a two-stroke white tick; a
  **radio** as a CIRCLE (a rounded rect with radius = half the side) — unchecked a white circle + grey border,
  checked the same with a filled accent centre dot. Verified HOST-ONLY through the gfx PPM backend
  (`test_hambrowse_checkradio_host.sh`, fixture `hambrowse_checkradio.html`, probe `hb_checkradio_probe.py`): the
  four controls classify top-to-bottom as checkbox SQUARE checked, checkbox SQUARE unchecked, radio CIRCLE checked,
  radio CIRCLE unchecked — SHAPE from the box's top-edge ink width (a square's top edge is a ~9px horizontal line, a
  circle's apex is ~1px), CHECKED state from the accent fill's presence, and the checked checkbox's 15×15 fill is
  distinctly larger than the checked radio's ~9×9 centre dot. Zero regressions (`button`/`inputtypes`/`formvalid`
  gates green — the text-field/button painter path and the ASCII text view are untouched). **HONEST SCOPE:** the
  shape is a fixed ~15px widget (does not honour CSS `width`/`height`/`accent-color`/`appearance` on the control);
  no indeterminate (`:indeterminate`) or focus-ring state; interactive toggle on click is the DOM/event layer's
  separate concern (the rendered state follows the `checked` attribute, which a JS `.checked=` rewrite updates
  through the normal re-render path).

**ANIMATION-FILL-MODE + FORM-CONTROL POLISH ROUND 2026-07-18g (round-7 — the map's #7 and #6, host-verified):**
- **[css-anim] `animation-fill-mode` + WIDENED @keyframes end-state overlay (NEW, gate `animfill`).** Round-2's
  keyframe overlay ALWAYS painted the settled END frame (equivalent to `animation-fill-mode: forwards`) and overlaid
  only background / colour / transform / border-colour. Now `lib/web/css/cascade.ad` `_parse_keyframes_body` captures
  BOTH edge frames — the settled END frame (`100%`/`to`, else the last frame) AND the FROM frame (`0%`/`from`, else
  the first frame) — as pseudo-rules tagged `r_kf_from` (0 = end, 1 = from). A new `_kf_sel_is_from` detector matches
  `0%`/`from` (guarded so the trailing `0` of `100%` never false-matches). `animation-fill-mode` is parsed (the
  `animation-fill-mode` longhand AND the unambiguous `forwards`/`backwards`/`both` keywords inside the `animation`
  shorthand) into `d_animfill`/`r_animfill`/`m_animfill` (-1 unset / 0 none / 1 forwards / 2 backwards / 3 both), riding
  the cascade on the same specificity slot as `animation-name`. `_cascade_match_current` now selects WHICH frame to
  overlay: forwards/both **and the unspecified LEGACY default** overlay the END frame; an explicit **none/backwards**
  overlay the FROM (0%) frame. The overlay also **widens** from four properties to also copy **width / height /
  margins** (`r_wd`/`r_ht`/`r_ml`/`r_mr`/`r_mt`/`r_mb` → `m_*`, applied BEFORE the box-sizing + figure-margin passes so
  they compose). Verified HOST-ONLY render-to-PNG (`test_hambrowse_animfill_host.sh`, fixture `hambrowse_animfill.html`,
  6 boxes): a `forwards` and a `both` box settle to the end-frame green `#00aa00`, a `backwards` and an explicit-`none`
  box show the 0%-frame red `#cc0000`, an unspecified box keeps the legacy end-frame green, and a box whose keyframe
  animates `width` 80→300px settles (with `forwards`) to a painted width ~300px (read off the POSFILL x0/x1). Zero
  regressions (the `keyframes` gate stays green — its `.fade` uses explicit `forwards`, `.slide` is unspecified → both
  keep the end frame). **HONEST SCOPE / LIMITATIONS:** still the static settled-frame model (no timeline / no mid-frame
  interpolation / no ticking). To preserve the round-2 gate the UNSPECIFIED fill-mode is treated as the legacy settled
  END frame (real CSS default is `none`) — only an EXPLICIT `none`/`backwards` paints the FROM frame; this is the honest
  zero-regression choice for a headless single render. Opacity is not overlaid as an independent channel (it rides the
  packed background alpha via `_pack_fill_rgb` — a keyframe that animates ONLY opacity with no background does not shift
  a bare box's alpha); position offsets (`top`/`left`) are not yet in the widened set. The doubled pseudo-rule count
  (from + end per name) fits the 256-rule table for typical pages.
- **[html-forms / css-ui] `accent-color` + `:indeterminate` on checkbox/radio (NEW, gate `accentind`).** Round-6 landed
  the checkbox/radio pixel SHAPES but with a FIXED accent blue `rgb(26,115,232)` and only 2 states. Now **`accent-color`**
  is a real cascade property: parsed in a SHARED `_box_decl` branch (so BOTH the stylesheet and inline `style=""` paths
  honour it — `auto`/`currentcolor` fall back to the UA accent), riding `d_accent`/`r_accent`/`m_accent` on a dedicated
  specificity slot `sacc` (`lib/web/css/cascade.ad`; a rule that declares ONLY `accent-color` is no longer dropped by the
  "nothing to store" short-circuit). Because `<input>` is a VOID element (not pushed/cascaded on the normal flow),
  `lib/web/dom/forms.ad`'s `_emit_input` resolves the control's accent via a transient **`_img_cascade`** (the same
  push→cascade→inline→pop+restore-nth-child helper `<img>` uses), then merges the inline `style=""` `d_accent` over the
  cascade `m_accent`, and stashes it per-segment in a new `seg_accent` (`lib/web/layout/box.ad`, exposed via
  `he_seg_accent` in `lib/web/dom/canvas.ad`). `lib/htmlpage.ad` fills the checked box / radio dot / indeterminate dash in
  that colour (UA blue when unset). **`:indeterminate`** is honoured via an `indeterminate` boolean attribute on the
  control (the honest static trigger — `:indeterminate` has no HTML content attribute, only a JS property): it becomes
  state 2 (`_mark_check_seg` now takes state 0/1/2 + accent), rendered as an accent-filled box with a centred white DASH
  (distinct from the checked diagonal tick). Verified HOST-ONLY render-to-PNG (`test_hambrowse_accentind_host.sh`, fixture
  `hambrowse_accentind.html`, probe `hb_accentind_probe.py`): a stylesheet `accent-color:#dd2222` checked checkbox fills
  RED, an `indeterminate` checkbox is a UA-blue box with a centre dash, a plain checked checkbox is UA blue with no dash,
  a stylesheet `accent-color:#22aa33` radio dot is GREEN, and an inline `style="accent-color:#ee8800"` checkbox fills
  ORANGE. Zero regressions (`checkradio`/`inputtypes`/`formvalid`/`button` green). **HONEST SCOPE / LIMITATIONS:** the
  control is still a fixed ~15px widget (CSS `width`/`height`/`appearance` on the control are NOT honoured — only
  `accent-color`); `:indeterminate` is driven by the boolean attribute, not a live JS `.indeterminate=` property write
  (a JS write would need the DOM re-render path to re-emit the segment); no focus-ring / `:checked`-pseudo styling;
  interactive click-to-toggle remains a DOM/event-layer concern; the accent applies to checkbox/radio only (not range/
  progress/other form controls).

**REGEXP `u`/`v` UNICODE MODE ROUND 2026-07-18h (round-8 — the map's #1, the single biggest remaining ES gap, host-verified):**
- The byte-oriented backtracking regex VM (`lib/web/js/setup.ad`) + the string lexer (`lib/web/js/lexer.ad`) now support
  an **honest byte-model subset** of the `u`/`v` (Unicode / unicodeSets) flags. Because JS strings are stored as **UTF-8
  byte buffers** in this engine, the design choice throughout is: decode code points to their UTF-8 byte sequence at
  COMPILE time and match those bytes — which is *exactly correct* for a UTF-8 subject, for the astral cases below.
  - **Flag + reflection** — `u` (117) / `v` (118) set the `re_uni` compile global (bit6/bit7 of `obj_re_flags`); `/re/u`
    reflects `.unicode`, `/re/v` reflects `.unicodeSets`, and both appear in `.flags`. `v` is treated as a `u` superset
    for this subset. `js_make_regex`.
  - **`\u{CP}` code-point escapes + surrogate pairs** — in u-mode `\u{1F600}` and a `😀` high/low surrogate
    PAIR combine to one code point and compile to an **`RN_SEQ` of the 2/3/4 UTF-8 bytes** (`re_utf8_atom` /
    `re_parse_u_cp`). A quantifier binds the whole code point (`/\u{1F600}+/u` matches per-emoji, not per-byte). Works
    top-level; astral members inside a `[...]` class are dropped (a byte bitmap can't hold them) — code points ≤ 0xFF are
    added. Verified: `/\u{1F600}/u.test("😀")`, `.match(...).index`, `/😀/u` literal, `+` quantifier, non-match of a
    different emoji.
  - **`\p{Name}` / `\P{Name}` property classes** — general categories over the **ASCII range**: `L`/`Letter`/`Alphabetic`,
    `N`/`Nd`/`Number`, `Lu`/`Uppercase[_Letter]`, `Ll`/`Lowercase[_Letter]`, `White_Space`/`space`, `ASCII`, `Any`
    (`re_prop_which` + `cls_add_prop`). `\P` negates over 0..255. Supported top-level AND inside `[...]` classes
    (`[\p{L}\p{N}]+`).
  - **String/template `\u{CP}` decoding (lexer)** — `"\u{1F600}"`, `` `\u{41}` ``, and `😀` surrogate pairs in
    string/template literals now decode to their UTF-8 bytes (`lex_emit_uesc` → `emit_utf8`); previously `\u{...}` was
    silently dropped (the subject string never contained the character, so even a correct regex couldn't match).
  - **Gate** `scripts/test_jsengine_reunicode_host.sh` (32 inline asserts vs node semantics via the x86_64-linux JS host
    driver), registered in `ci_battery_manifest.txt`. Legacy non-u behaviour preserved (`\uHHHH` low-byte, `\p`→literal
    `p`, `\u{3}`→quantified `u`) — verified, and zero regressions across the JS host gate suite + browser render gates.
  - **DEFERRED (documented, not faked):** `.` and case-folding operate on BYTES, not code points, in u-mode (a
    non-ASCII `.` matches one UTF-8 byte); `\p{...}` covers only the ASCII range (astral/Latin-1 property matching needs
    code-point awareness the byte bitmap lacks); astral members inside `[...]` classes; `String.length` counts UTF-8
    bytes (not UTF-16 code units) — pre-existing to the byte-string model, unchanged. A full code-point VM (the "hard"
    rewrite the prior map flagged) remains the only way to close these; the subset above covers the dominant real cases
    (emoji/astral literals + escapes, ASCII property classes) correctly.

**GRID-AUTO-FLOW + FORM-CONTROL WIDTH ROUND 2026-07-18i (round-9 — the map's #2 grid-auto-flow and the tractable half
of #4 form-control geometry, host-verified):**
- **`grid-auto-flow: dense` / `column` (W3C css-grid §7.7) — DONE (gate `gridflow`).** The grid placement machinery
  gained real auto-flow control, GATED so a grid without `grid-auto-flow` is byte-identical (all six other grid gates
  verified green). Plumbing mirrors `flex-wrap` exactly: a new single-int cascade property `grid-auto-flow` parses to a
  packed winner `m_gaflow`/`d_gaflow` (bit2 `=set` marker, bit0 `=column` axis, bit1 `=dense` packing; parsed by
  `_grid_auto_flow_value` in `lib/web/css/cascade.ad` — tokens accepted in either order, `row dense` / `dense column`
  / `column` / `dense`), with a full `r_gaflow[]` rule array + specificity-tracked winner loop. The winner threads through
  `lib/web/dom/forms.ad` (grid-arming block) into two new globals `g_flex_dense`/`g_flex_colflow`, stamped per grid frame
  as `flex_dense[]`/`flex_colflow[]` at `_flex_container_open` (`lib/web/layout/box.ad`).
  - **dense** — an AUTO-placed item (no explicit `grid-column`/`grid-row`) now backfills the FIRST free rectangle scanned
    row-major from the grid ORIGIN (`_grid_find_dense`), instead of only advancing forward from the flow cursor. So when a
    `span 2` item can't fit the single free cell left on a row (leaving a hole) and wraps to the next row, a later
    single-cell item BACKFILLS the hole rather than following the cursor — the classic masonry/gallery pack.
  - **column** — an auto item fills DOWN a column (rows `0..grid-template-rows`) before moving to the next column
    (`_grid_find_colmajor`), the transpose of the default row flow. Requires a known `grid-template-rows` count (real
    column grids always specify it); with none it degrades to row flow (documented).
  - Explicit placement (`grid-column`/`grid-row`/`grid-area`) is UNAFFECTED — dense/column only re-route auto items.
  - Verified HOST render-to-PNG on real engine coordinates (`scripts/test_hambrowse_gridflow_host.sh`, fixture
    `hambrowse_gridflow.html`): a sparse grid leaves a hole (trailing single lands on the span item's wrapped row) while
    an identical `grid-auto-flow:dense` grid backfills it (single lands on row 0); a column-flow grid places its 2nd item
    BELOW the 1st in the same column (same x, lower row) vs the row-flow control (to the right, same row).
  - LIMITATIONS (honest): dense/column position auto items at the FIXED-row-pitch model (`_grid_row_top_rows`, one text
    row per content-height grid row) exactly as the existing 2-D row-span path does, so a MULTI-LINE cell backfilled into
    an earlier row can under-estimate that row's true height (real dense galleries use `grid-template-rows`/uniform cells,
    exercised in the fixture); `subgrid`, `grid-auto-columns`/`grid-auto-rows` sizing, and >8 tracks (`GRID_MAXTRACK`)
    still deferred; column flow needs an explicit `grid-template-rows` (else row flow).
- **Form-control CSS `width` geometry — DONE (gate `ctrlwidth`).** A text/password `<input>` now sizes its field box to
  the cascaded CSS `width` (class OR inline `style=""`) instead of the fixed `>=8`-cell UA default. `_control_width_cells`
  (`lib/web/dom/forms.ad`) resolves the control's width via the same transient `_img_cascade` match `accent-color` uses
  (inline `d_wd` overrides stylesheet `m_wd`; both already px-resolved incl. `%`), converts to monospace cells
  (`width/CELL_W − 2` bracket cells, floored at 2), and grows the field's underscore padding run to span that width. So
  `<input style="width:120px">` → a 13-inner-cell field, `.wide{width:240px}` → 28 cells, and a `width:160px` password →
  18 masked cells; with no CSS width the field is byte-identical to the prior `>=8`-cell default (all form gates green).
  Verified HOST render-to-PNG (`scripts/test_hambrowse_ctrlwidth_host.sh`, fixture `hambrowse_ctrlwidth.html`) counting
  the laid-out field-run cell span. LIMITATIONS (honest): `width` only this round — `height` (multi-row field boxes /
  textarea rows) and `appearance` are NOT yet honoured (the field is still one text-row tall); a field can't shrink below
  its own value text (the monospace cell model has no clip); non-text controls (button/select/range) keep their
  content-sized width. `height`/`appearance` + range/progress `accent-color` are the remaining half of map-item #4.

**ROUND 11 2026-07-18 (accent-color on range/progress + grid-auto-rows implicit-track sizing):**
- **[html-forms / css-ui] `accent-color` on `<input type=range>` + `<progress>` (NEW, gate `rangeprog`).** Round-10 left
  a range as a static `[--O--]` glyph run (no accent, no value fraction) and `<progress>` as an UNKNOWN tag whose
  fallback text (`70%`) leaked into the flow. Now a range resolves its cascaded `accent-color` (the same
  `m_accent`/`d_accent` cascade the checkbox/radio share, via a transient `_img_cascade` for the void `<input>`) plus
  `min`/`max`/`value` → a fill percentage, committed as a **seg_field kind-5** segment (`_mark_check_seg(5, pct, accent)`
  in `lib/web/dom/forms.ad`). `<progress value max>` is now a real element handler: it resolves `accent-color` (its own
  cascade winner, non-void) + `value`/`max` → percentage, commits a **seg_field kind-6** segment, and SKIPS its fallback
  body (`_el_pop`+`_enter_skip`, like `<button>`/`<select>`). The pixel painter (`lib/htmlpage.ad`) draws a rounded light
  track with the value fraction filled in the accent colour (a range also gets a round accent thumb at the fill position),
  UA blue `rgb(26,115,232)` when `accent-color` is unset. GATED: the `[--O--]`/`[####----]` glyphs are kept for the
  text/gate view and the painter skips them (like the existing field boxes). Gate `test_hambrowse_rangeprog_host.sh`
  (fixture `hambrowse_rangeprog.html`, probe `hb_rangeprog_probe.py`): a stylesheet `accent-color:#dd2222` value=100 range
  fills RED full-width, a `#22aa33` value=50 progress fills GREEN half, a default range fills UA-blue half, an inline
  `style="accent-color:#ee8800"` value=80 progress fills ORANGE — and the full range's coloured run is wider than the
  half range's (proving the value fraction). STILL absent: `<meter>`, interactive drag, focus ring.
- **[css-grid] `grid-auto-rows` implicit-track sizing (NEW, gate `gridautorows`).** Round-10 always content-sized the
  IMPLICIT grid rows (those past `grid-template-rows`), so the ubiquitous `grid-template-columns: repeat(N, 1fr)` +
  `grid-auto-rows: <px>` dashboard collapsed every row to one text line. Now `grid-auto-rows` rides the cascade
  (`r_gar`/`m_gar`/`d_gar` + a `bgar` specificity slot in `lib/web/css/cascade.ad`, parsed by the shared
  `_grid_one_track`), is carried to the grid frame (`g_grid_gar` → `flex_gar[frame]` in `lib/web/layout/box.ad`), and a
  FIXED-px auto-row gives implicit rows that height in `_grid_row_h_rows`. GATED: an `fr`/`auto` auto-row (enc < 0) or no
  `grid-auto-rows` leaves `flex_gar=0` → implicit rows stay content-sized, byte-identical to before (all 8 other grid
  gates verified green). Gate `test_hambrowse_gridautorows_host.sh` (fixture `hambrowse_gridautorows.html`): a 2-col grid
  with NO `grid-template-rows` and `grid-auto-rows:72px` places its second row's items ~125px (5 text rows @16px) below
  the first — vs ~25px (one content row) at baseline. `grid-auto-columns` is left ignored (implicit COLUMNS never form in
  the row-major track model — items wrap to new rows). STILL absent: `subgrid`, `>8` explicit tracks (`GRID_MAXTRACK`),
  `minmax()` auto-rows, a row-spanning box filling its fixed tracks' height.

**TOP REMAINING W3C GAPS after the 2026-07-18 round-11 accent-range/progress + grid-auto-rows round (a map for the
round-12 agent, roughly by real-world value — the browser is now broad but NOT "fully W3C-implemented"; concrete holes):**
1. **[ecmascript] RegExp `u`/`v` FULL code-point semantics — SUBSET LANDED (gate `reunicode`); remainder is the hard
   part.** `\u{...}` escapes, surrogate-pair combining, ASCII `\p{...}` property classes, and string-literal `\u{CP}`
   decoding are DONE (byte-model, correct for UTF-8 subjects). STILL absent: code-point-granular `.`/case-fold, astral
   `\p{...}` and astral `[...]` class members, UTF-16 `String.length`/index units. These need a code-point VM rewrite
   (hard). Lower priority now — the common cases work.
2. **[css-grid] `grid-auto-flow: dense` / column-flow — DONE round-9 (gate `gridflow`).** Both `dense` (back-fill the
   first free hole from the grid origin) and `column` (column-major flow, needs a known `grid-template-rows`) land, gated
   so grid-auto-flow-free grids stay byte-identical. `grid-auto-rows` px sizing — DONE round-11 (gate `gridautorows`).
   REMAINING grid holes: `subgrid`, `grid-auto-columns` (no-op in row-major flow) + `minmax()` auto-rows, >8 tracks
   (`GRID_MAXTRACK`), >12 named areas per container, a row-spanning box filling its FIXED tracks'
   height, and the multi-line-cell-into-earlier-row height under-estimate in dense/2-D placement (fixed-row-pitch model).
3. **[css-filter] real `drop-shadow()` + `backdrop-filter` — ABSENT/no-op.** `drop-shadow()` is a recognised NO-OP
   (needs a blurred silhouette painted OUTSIDE the box); `backdrop-filter` filters what is BEHIND the element. Both need
   a compositing pass the static single-render model lacks — lower tractability.
4. **[html-forms] form-control CSS geometry — `width` (r9), `height` + `appearance:none` DONE round-10 (gate
   `ctrlgeom`).** A text/password `<input>`, `<button>` and `<select>`-style control now honours cascaded CSS `width`
   (r9), `height` (48px→3 rows; the box paints that many rows tall and the layout reserves them — mirrors the tall
   inline-block chip floor), and `appearance:none` (stylesheet + inline + `-webkit-`/`-moz-`; suppresses the default
   field surface/border and button-grey face so author CSS styles the control). Gated: a control WITHOUT these
   declarations is byte-identical (appear=0, rows=0). `accent-color` on `<input type=range>`/`<progress>` — DONE round-11
   (gate `rangeprog`; range = seg kind 5, progress = seg kind 6, both accent-tinted with a value fraction). STILL absent:
   `<meter>`, applying the author BACKGROUND to an appearance:none void `<input>` (the transient `_img_cascade` m_bg/d_bg
   winners are unreliable across void controls — chrome is suppressed but the author bg is not yet re-painted),
   focus-ring / `:checked` pseudo-styling, and interactive click-to-toggle (a DOM/event-layer concern).
5. **[html5-parsing] adoption-agency / active-formatting-list reconstruction — box tree AND DOM tree spec-correct
   (VISUAL locked round-10 gate `adoption`; DOM tree locked gate `adoptdom`). DONE.** Two layers:
   (a) VISUAL: the engine models inline emphasis as INDEPENDENT ORTHOGONAL style counters (`g_bold_n`/`g_italic_n`) and
   does NOT reset a formatting counter on a block boundary — reproducing the WHATWG adoption-agency +
   reconstruct-the-active-formatting-elements VISUAL result exactly. `<b>A<i>B</b>C</i>` paints A=bold, B=bold+italic,
   C=italic (the reconstructed `<i>` keeps C italic, NOT bold); a `<b>` left open across `</p><p>` is reconstructed so
   the next block stays bold. Gate `adoption` asserts the per-run `SEGFLAGS`.
   (b) DOM TREE: `lib/web/dom/domtree.ad` `_adoption_fixup()` (run at the end of `_dom_build_tree_index`) now makes the
   span-overlay tree match the spec node tree. It detects the adoption signature — a FORMATTING element
   (`_tx_is_formatting`: a/b/big/code/em/font/i/nobr/s/small/strike/strong/tt/u + mark/sub/sup) whose content span
   extends PAST its recorded parent's close (`tx_ce[child] > tx_ce[parent]`) — CLAMPS the inner element to the parent's
   close and SPLICES a RECONSTRUCTED same-tag element (holding the stuck-out remainder) in as a SIBLING of the parent,
   in document order (`_tx_insert`). So `<b>A<i>B</b>C</i>` yields the spec DOM `<b>A<i>B</i></b><i>C</i>`: DOM
   traversal sees "C" in a reconstructed `<i>` that is `<b>`.nextElementSibling (same `parentNode`), NOT a child. The
   rescan lifts multi-level nests one ancestor per pass and terminates (each fix clamps the child's ce). Gate
   `adoptdom` asserts the node tree (childNodes/nextElementSibling/parentNode) for the b/i and em/strong overlaps plus
   a well-nested regression. Behavior-preserving: well-nested pages never trigger the fixup, so the tree is byte-
   identical for them. DEFERRED (first-cut, low value): full Noah's-Ark dedupe of the active-formatting list;
   reparenting NON-formatting content nested inside the stuck-out region; unterminated-formatting reconstruction
   (`<p><b>x</p>` with no `</b>`); and `querySelectorAll` matching the synthetic node (it re-scans source text, so
   reconstructed nodes are reachable via traversal/combinators but not by a source-text tag scan).
6. **[dom] live `MediaQueryList` change events + `window.dispatchEvent` of a synthetic event + `scroll`/`resize`
   sourcing** — all no-ops in the headless single-layout model. Low value without a live event loop.
7. **[css-backgrounds-borders] rounded per-side borders** — a per-side-bordered box paints SQUARE corners (arc-segment
   stroking per corner deferred). Low value; cosmetic.

**HISTORICAL map (pre-round-7; retained for provenance — items renumbered/closed above):**
**TOP REMAINING W3C GAPS after the 2026-07-18f grid-areas + checkbox/radio round (a map for the next/round-7 agent,
roughly by real-world value — the browser is now broad but NOT "fully W3C-implemented"; these are the concrete holes):**
1. **[html-forms] `new FormData(formElement)` DOM-scrape — PARKED (out of the browser-agent file scope).** The
   reusable scraper ALREADY EXISTS (`lib/web/dom/canvas.ad` `_serialize_form`/`_serialize_field`), but wiring the
   extern-free JS-engine FormData ctor (`builtins/url.ad` `NAT_FORMDATA`) to the DOM scrape needs a JS↔DOM SEAM
   (mirror `js_set_fetch_transport`: a `js_set_formdata_scraper(fn)` the engine calls when the ctor arg is a DOM
   element). That seam must be REGISTERED in a `user/hambrowse*.ad` driver (as the fetch transport is) AND
   host-verified through a DOM-capable driver — both `user/hambrowse*.ad`, outside the browser-agent's owned files.
   Round-6+ path once the driver can register the seam. (The input-type PIXEL SHAPES half moved to item 6 — it is
   fully within the agent's owned files.)
2. **[css-backgrounds-borders §4] per-side border COLOURS — DONE round-5 (gate `bordercolor`).** Rounded per-side
   corners are the remaining half: a per-side-bordered box still paints SQUARE corners (the four edges are
   axis-aligned fills; honouring `border-radius` needs arc-segment stroking per corner — deferred).
3. **[ecmascript] RegExp `u`/`v` Unicode mode — ABSENT (byte-oriented VM).** `\u{...}` code-point classes,
   surrogate handling, `\p{...}` property escapes. Hard (VM rewrite); moderate value.
4. **[css-grid] named `grid-template-areas`/`grid-area` — DONE round-6 (gate `gridareas`).** Remaining grid holes:
   `grid-auto-flow:dense`/column, `subgrid`, >8 tracks (`GRID_MAXTRACK`), >12 named areas per container, and
   distinct-y multi-row areas without an explicit `grid-template-rows` (content-sized rows compute a 0 track-top).
   Lower-value / harder — the high-value named-area layout primitive is now covered.
5. **[css-filter] `filter` FUNCTION LISTS + `hue-rotate()` — DONE round-5 (gate `filterlist`).** Remaining filter
   holes: real `drop-shadow()` (still a recognised NO-OP — needs a blurred silhouette painted OUTSIDE the box,
   which the in-place post-pass model does not support) and `backdrop-filter` (filter what is BEHIND the element).
   Both need a compositing pass the static single-render model lacks — lower tractability.
6. **[html-forms] `<input type=checkbox|radio>` PIXEL SHAPES — DONE round-6 (gate `checkradio`).** Checkbox draws a
   rounded SQUARE (checked = accent fill + white tick), radio a CIRCLE (checked = filled accent dot). Remaining
   form-control polish: honour CSS `width`/`height`/`accent-color`/`appearance` on the control, `:indeterminate` /
   focus-ring states, and the interactive click-to-toggle (a DOM/event-layer concern). (The FormData(formElement)
   DOM-scrape half still needs a JS↔DOM seam registered in `user/hambrowse*.ad`, outside the browser-agent's file
   scope — park until the driver can register a `formdata_scraper` seam.)
7. **[css-anim] extend the animation END-STATE overlay** to opacity + width/height + position offsets + margins
   (currently background/colour/transform/border-colour), and honour `animation-fill-mode` (only `forwards`/
   `both` should paint the end state; `none`/`backwards` should paint the FROM/`0%` frame). Low risk (the
   `@keyframes` registry + overlay hook already exist — just widen the copied property set).
7. **[dom] live `MediaQueryList` change events** — `matchMedia().matches` is now exposed; a `change` event on
   viewport resize would need the (absent) live resize loop. Low value in the headless model.
8. **[html5-parsing] adoption-agency / active-formatting-list reconstruction** — misnested inline recovery
   (`<b><i>x</b>y</i>`) is still depth-counted, not spec-correct.

**REAL-ARTICLE FIDELITY FIXES 2026-07-18 (QA-by-using on a Wikipedia-article / docs-page fixture, gate `realarticle`):**
- **[css-tables] float:right infobox `background-color` fills the WHOLE box** — a floated `<table>` reached `_float_box_open` (`lib/web/layout/box.ad`) with `bgc = -1` (its caller cannot fold the table's own background into a float-box arg), so the declared shade survived only as patchy per-cell text fills with white gutters between cells and rows — nothing like a real browser's uniform infobox panel. Fix: when `bgc < 0`, fall back to the table's OWN pushed background (top of the bg stack) so the whole box gets one uniform fill behind every cell. Pixel-verified: the `#f0f4f8` infobox samples uniform at centre / under-header / lower-right.
- **[css-tables] bordered data-table grid draws SINGLE column rules (no doubled/gappy lines)** — the border-stroke pass maps a box's left edge to `lx+2` but its right edge to `rx+6` (the `+6` reaches a general box's right box-art bar cell). For a table cell `rx` is ALREADY the true column boundary, so an INTERNAL cell's right rule landed at `col_x[b]+5` while the neighbour's left rule landed at `col_x[b]+2` — two parallel lines 3px apart, the classic "doubled grid" a browser never draws. Fix (`lib/web/layout/tables.ad` `_cell_close`): pull an internal cell's registered right edge in by 3 so its right rule coincides with the neighbour's left rule (one clean line per boundary); the RIGHTMOST cell keeps its true `rx` so its rule still merges with the table frame. Pixel-verified: adjacent cell edges coincide (`x1 == next x0`) across all 8 shared column edges in the 3×4 fixture table.
- **Gate `border` now GREEN** (was RED on base): the floated-infobox assertion hard-coded an expected `edge=#000000` while the fixture declares `border:1px solid #a2a9b1` (the real Wikipedia rule colour, which the stroke correctly honours) — the assertion was stale, now checks the declared colour.
**RENDERING-FIDELITY FIXES 2026-07-18 (clearing the three real-article deferrals above, gate `wordwrap`):**
- **[css-text] word-wrap breaks at WHITESPACE, not mid-word (default `overflow-wrap:normal`)** — `lib/web/layout/box.ad::_emit_word` unconditionally char-broke any word wider than the content measure, so a narrow column (an infobox) split `Platform` into `Platf|orm` / `Falconiform|es` — a break real browsers only make under an opt-in property. Fix: an over-wide word now stays intact and OVERFLOWS its box on its own line (word-boundary wrap) UNLESS a breaking mode is set. Added the inheriting `overflow-wrap` / `word-wrap` / `word-break` cascade to `lib/web/css/cascade.ad` (`_ovw_value`, `d_ovw`/`m_ovw`/`r_ovw`, `ovw_stack` + `g_break_word`, parsed in the rule + inline decl paths, pushed/popped in `_open_style`/`_close_style`); `_emit_word`'s mid-run char-break (`dobreak`) now fires only when `g_break_word` (overflow-wrap:break-word / word-wrap:break-word / word-break:break-all) is set — OR under `white-space:pre-wrap` (`g_ws==3`), where the flow tokenizer emits the whole preserved-whitespace run as one token and char-breaking it at the measure IS how pre-wrap soft-wraps (regression-checked: gate `whitespace` stays green). Pixel/SEG-verified: `Platform` stays one segment and overflows a 44px box, while `overflow-wrap:break-word` and `word-break:break-all` boxes still split their over-wide word.
- **[css-tables] internal HORIZONTAL inter-row rules coincide into ONE clean line** — a bordered table cell reused the frame's reserved-row → rule-Y mapping (`row_top[btop]+row_h[btop]-2` .. `row_top[bbot]+2`), which centres rules in *reserved* rows; for a cell whose `btop`/`bbot` are CONTENT rows this drew a thin band straddling each row boundary, so an upper cell's bottom rule and the lower cell's top rule landed ~a row-band apart (the "slightly doubled" divider). Fix: a new `bbox_kind` flag (`lib/web/layout/box.ad`, set 1 for cells in `tables.ad::_cell_close`, reset in `flow.ad::_reset_layout`, default 0 = frame/generic) switches the `lib/htmlpage.ad` paint to a FULL-row span for cells (`row_top[btop]` .. `row_top[bbot]`), so a cell's bottom rule sits at exactly the next cell's top rule. Pixel-verified: in the 3-row fixture the left-column cells share edges exactly (r1.bot==r2.top==62, r2.bot==r3.top==87 — zero-width inter-row band); the frame keeps the reserved-row centring so its outer rule is unchanged.
- **[css-box] `<figure>` UA-default ~40px side margins** — a bare `<figure>` sat at the body left margin; real browsers apply `figure { margin: 1em 40px }`. Fix (`lib/web/css/cascade.ad::_cascade_match_current`): when the current element is `<figure>` and the author cascade left `m_ml`/`m_mr` unset, default them to 40 (any stylesheet/inline margin still wins). Pixel/SEG-verified: figure content lays out at x=48 (8 + 40) while body paragraphs stay at x=8.

**Confirmed ALREADY IMPLEMENTED (do NOT re-dispatch — the old worklist is stale on these):**
- css-values-units: decimal/fractional lengths, `rem`, `vh/vw/vmin/vmax`, `calc()`+`min/max/clamp`,
  `var()`/custom-properties+fallback, `box-sizing` — `css/cascade.ad` `_len_apply_unit`/`_style_len`
  (~1436-1666) + `var()` in `_apply_decl` (~4974) + custom-property harvest `_collect_css` (~6175).
  Gates: `cssvalues`, `cssnl`, `varfallback`, `viewport`. The `viewport` gate (2026-07-20,
  `scripts/test_hambrowse_viewport_host.sh`, fixture `hambrowse_viewport.html`) pins each viewport unit
  to concrete resolved pixels at a known 800x600 viewport: `50vw`->400px, `25vw`->200px, `50vh`->300px
  (distinct from `50vw`, proving the HEIGHT axis), `50vmin`->300px (min axis = height), `50vmax`->400px
  (max axis = width) — so a unit resolved against the wrong axis/scale fails on host.
- css-flexbox: flex-grow / flex-shrink / flex shorthand / flex-basis now DISTRIBUTE (not just parse) —
  POSITIVE free space is split proportional to inline flex-grow, and NEGATIVE free space (overflow) is
  distributed as SHRINKAGE weighted by each item's scaled flex-shrink factor (flex-shrink × flex-basis),
  both over each item's flex-basis/content size (`layout/box.ad`
  `_flex_grow_scan`/`_flx_layout_grow`, wired into `_flex_container_open`). Gates:
  `flexgrow` (flex:1 even split, flex:2/flex:1 → 2:1, flex:0 0 200px fixed+grow),
  `flexshrink` (NEW: 3×flex-basis:300 shrink equally to fit, flex-shrink:0 holds while siblings absorb,
  mixed shrink:1/shrink:3 proportional), `flexclass` (NEW: CLASS/stylesheet-resolved flex — `.col{flex:1}`
  even split + `.wide{flex:2}` → 2:1 with NO inline flex), `flexmin/flexalign/flexjustify/flexwrap/flexgap`.
  grow/shrink/basis now read from the CASCADE winner (class/stylesheet rules) AND inline style (inline
  overrides per-property) — `box.ad _flex_grow_scan` calls `cascade.ad _flx_child_cascade` per item.
  Boundary: min-content clamp floors at 0 (frozen-item iteration deferred); single-level only (nested-flex deferred).
- css-flexbox / css-text / css-box (on-device QA fixes, DONE 2026-07-18, gate `flexwrap_qa` — render-to-PNG):
  three REAL defects found by QA of a realistic article/landing page. (1) **Flex-item inline text now WRAPS
  to the item's OWN resolved content-box width.** The flex nowrap guard (`g_flex_nowrap_depth`) was armed for
  EVERY natural-path item, so a `flex:1` card shrunk below its body sentence laid one line that bled across
  and PAST the card's right border (multi-column card grids). It is now armed ONLY for a CONTENT-SIZED pill
  (resolved track ≥ its measured max-content) — recorded per box in `box_flexnowrap_stack` (`layout/box.ad`),
  set/cleared at the flex-item open/close (`dom/forms.ad`); a shrunk card is left unarmed so its text reflows
  inside the card, while the nav-pill nowrap (proportional "Log in" not splitting) is preserved (`flexmin`
  stays green). (2) **`display:flex; gap:Npx` with NO justify-content now PACKS at flex-start with the gap**
  instead of falling through to the equal-column approximation (which ignored the gap AND spread the items
  edge-to-edge) — `_flex_container_open` takes the natural flex-start packing path when a `gap`/`column-gap`
  (or an explicit justify) is set and the items fit (`layout/box.ad`). (3) **A margin-less structural block
  (`<div>`/`<section>`/… ) no longer emits a phantom paragraph gap on close** — only `<p>`/`<figure>` do, so
  whitespace-only inline content between blocks generates no box and sibling containers stack flush per CSS
  (`dom/forms.ad _hx_block_close(paragap)`); real CSS margins still apply via `ftop`/`botg`. Verified
  HOST-ONLY render-to-PNG (`hb_flexwrap_qa_probe.py`): each card's text wraps to multiple rows with zero ink
  past its border, a `gap:20px` nav packs 4 links with the ~20px gutter, and two whitespace-separated divs are
  pixel-adjacent. Zero regressions across the flex/grid/box gates (`overflow`/`boxsizing`/`gridspan` row
  assertions updated to the corrected tighter block stacking).
- css-grid: `minmax()`, `repeat(auto-fill/auto-fit)`, PERCENTAGE tracks (`25% 75%`, resolved against the grid CONTAINER inline size) — `cascade.ad _grid_simple_track/_grid_one_track/_grid_parse_template`, tracks sized in `box.ad _grid_resolve_tracks`. Gates: `grid/gridr2/gridjustify/gridpng`.
- css-selectors: attribute selectors (gate `attrsel`). css-positioning: `position:relative`/`absolute`/`z-index` (gate `position`), with `absolute` `right:`/`bottom:` offsets now anchoring the box's border-box edge to the containing block's padding-box right/bottom taken from the ancestor's LAID-OUT GEOMETRY (works for gradient/transparent/image/solid backgrounds alike, gate `abspos`); `position:sticky` static-pin + `position:fixed` viewport-pinning now RESOLVE, not just parse (gate `sticky`). An `absolute`/`fixed` box is now truly OUT OF FLOW (CSS 9.7): it BLOCKIFIES an inline element (a `<span>` badge) and generates no box in normal flow, so it no longer lays out at its static top-left inline position — it neither paints its label at the wrong corner nor consumes inline advance that pushed a following `<h1>` a badge-width right and clipped it (gate `absinline`; fixed the heading-clip + top-left-badge symptoms). CSS `float:left`/`right` (single + simultaneous left+right pair; gates `float`/`twofloat`) and now **`clear:left`/`right`/`both`** — a cleared block drops to the bottom edge of the float it clears instead of flowing beside it (gate `clear`).
- dom: `Element.closest`/`matches` (`query.ad:841`, gate `matches`); arbitrary event types +
  `Event`/`CustomEvent`/`MouseEvent`/`KeyboardEvent` ctors + listener options (`once` +
  functional three-phase `capture`, see below) +
  `removeEventListener` + `stopImmediatePropagation` (gates `events/evttypes/domr6/evtcapture`). dom-events
  machinery lives ENTIRELY in `lib/web/dom/{canvas,bindings}.ad` — any string event type is
  interned into the `GEN_KIND` listener table (`bindings.ad:113`) and bubbles the source tree like
  the 4 legacy kinds; there is NO 4-kind cap (the old worklist claim below is dead).
- dom-core: **implied `<body>` for head-only documents** (gate `implicitbody`) — `document.body`/
  `head`/`documentElement` already resolved for pages with explicit or content-forced tags, but a
  document whose ONLY content is head-only (bare text and/or a lone `<script>`, nothing that forces
  the head closed) came back with `document.body === undefined`: the tag inserter
  (`bindings.ad _insert_implied_tags`) placed the implied `<body>` open-tag splice point AT
  end-of-source, which its `while k < src_len` pass-2 loop never reached, so the rewritten source read
  `<head>…</body></html>` with a `</body>` but no `<body>` open. Now the implied `</head>` and `<body>`
  are flushed at the tail (spec order, ahead of `</body>`/`</html>`) so `document.body.appendChild(…)`
  no longer throws. NOTE: rendering a node grafted onto a LITERALLY-empty implied body (zero-length span)
  is scoped out — the DOM is correct but that body has no layout region; a content-bearing implied body
  (the realistic case) both threads the DOM and renders (both asserted by the `implicitbody` gate).
- dom-selectors: **FULL common CSS selector grammar in `querySelector`/`querySelectorAll` (+ `matches`/
  `closest`)** (2026-07-18, gate `qsa`) — the DOM-side query matcher (`lib/web/dom/{domtree,query,
  bindings,canvas}.ad`, INDEPENDENT of the `cascade.ad` cascade matcher) now supports the grammar
  frameworks depend on, over BOTH the source tree and the created-node overlay:
  - **combinators** descendant (space), child `>`, adjacent `+`, general `~` (right-to-left
    evaluation from the candidate; `_match_chain`/`_cre_match_chain`);
  - **attribute operators** `[a]`, `[a=v]`, `[a^=v]` prefix, `[a$=v]` suffix, `[a*=v]` substring,
    `[a~=v]` whitespace-word, `[a|=v]` dash-match — one shared `_attr_op_match` (case-insensitive,
    matching the prior `=` behaviour) called from both `_sel_match_at` (source) and `_cre_sel_match`
    (created);
  - **structural pseudo-classes** `:first-child`/`:last-child`/`:only-child`, `:first/last/only-of-type`,
    `:nth-child`/`:nth-last-child`/`:nth-of-type`/`:nth-last-of-type` with `An+B` math (incl.
    `odd`/`even`), `:not(simple)`, `:empty` (these already existed for both trees);
  - **compound** selectors (`a.btn[data-x="1"]`) and **SELECTOR LISTS** `a, b, c` — a new top-level
    comma splitter (`_sel_split`, nesting-aware for `[...]`/`(...)`) makes the scan test every
    sub-selector per candidate, so `querySelector` returns the first match in DOCUMENT order across
    the whole list and `querySelectorAll` returns the de-duplicated union in document order.
  `matches()`/`closest()` were upgraded from the old rightmost-compound approximation to the full
  combinator/pseudo chain + selector-list union (`_el_matches_sel`/`_tx_matches_sel`). Gate `qsa`
  (32 asserts: each combinator, all seven attribute operators, `2n`/`odd`/`3n+1` nth math, `:not`,
  first/last-child, compound, list union/order/uniqueness, matches/closest). Remaining gaps:
  `:not()` argument is a single simple selector (not a full compound list); `:disabled`/`:checked`/
  `:hover`/`:nth-*(of S)` and case-sensitivity flags (`[a=v i]`) are not yet honoured; `matches()`
  on a purely created (never-in-source) node still returns false (the dispatch resolves only source
  elements).
- dom-events: **document lifecycle** — `document.addEventListener('DOMContentLoaded'|'load', fn)`
  now REGISTERS (document records under the `DOC_EL` sentinel, `bindings.ad:113`) and FIRES once,
  in spec order, after every page `<script>` runs (`canvas.ad` `_fire_document`, called at the tail
  of `_run_scripts`). `new Event('keydown')` on an element + `new CustomEvent('foo',{detail:42})`
  round-trip verified. Gate: `domlifecycle`. (Before this, document listeners were silently dropped
  because the document object is not a registered DOM element.)
- dom-events: **REAL three-phase propagation (round 9, 2026-07-17)** — capture was a stored-but-
  ignored flag: every listener fired in ONE bubble-only pass and `event.eventPhase` was never set.
  `_dispatch_event`/`_dispatch_generic` (`canvas.ad`) now materialize the propagation path
  (`_build_disp_path`: target→root via the tx tree, into `bindings.ad` `disp_path`) and walk the
  three DOM phases: **CAPTURE** (root→target's parent, firing only `capture:true` listeners),
  **TARGET** (every listener on the target in registration order, capture & bubble alike), then —
  when the event bubbles — **BUBBLE** (target's parent→root, `capture:false` listeners). Listener
  eligibility per phase is `_phase_wants`; an `on<evt>` content/IDL attribute is a bubble-side
  handler (never fires in capture). `event.eventPhase` reflects `NONE=0/CAPTURING=1/AT_TARGET=2/
  BUBBLING=3` per hop (`EPHASE_*` in `bindings.ad`), `currentTarget` is re-pointed each hop while
  `target` stays the origin, and `stop(Immediate)Propagation` halts between/within hops in every
  phase. `event.bubbles` is now HONORED — a `new Event(type)` (bubbles=false default) runs
  capture+target but no bubble hop; a plain `{type:'click'}` (no `bubbles` field) still bubbles
  (legacy default-true). `removeEventListener(fn, true)` drops the capture registration. Gate:
  `evtcapture` (exact-order oracle `cO cM tA tB bM bO`; per-hop `eventPhase`; capture-phase
  `stopPropagation`; `bubbles:false` suppression; capture-flag removal).
- dom-events: **`window.onload` + `window.addEventListener` (round 9)** — the dominant real-page
  init hook now fires. `window` (the JS global) carries `addEventListener`/`removeEventListener`
  (`_js_build_document`, `canvas.ad`); window listeners record under the `DOC_EL` sentinel so
  `window.addEventListener('load'|'DOMContentLoaded', fn)` fire with the document lifecycle. After
  the `load` listeners, `_fire_window_onload` invokes `window.onload = fn` once (a fresh `load`
  event, `this === window`). Gate: `domlifecycle` section (D). REMAINING: `window.dispatchEvent`
  of a synthetic event is a no-op (no window target in `NID_DISPATCH`); `scroll`/`resize` are never
  sourced in the headless render.
- **webstorage + html/browsers (2026-07-17): Web Storage + History API + Location parsing** — two
  self-contained browser globals real SPAs/routers depend on. **Web Storage was already present**
  (`localStorage`/`sessionStorage` as independent `Storage` objects in `lib/web/js/builtins/native.ad`
  `make_storage` + dispatch in `collections.ad`: `setItem`/`getItem`/`removeItem`/`clear`/`key`/`length`,
  string coercion of values, `null` for a missing key, insertion-ordered `key(i)`); this round adds the
  gate + doc. **History API is NEW** (`lib/web/dom/canvas.ad`): a `history` object with
  `pushState(state,title,url)` / `replaceState` / `state` / `length` / `back()` / `forward()` / `go(delta)`
  over a session-history model (`jshist_*` index + state + append-only URL ring). `pushState` advances the
  index, **truncates the forward tail**, and grows `length`; `replaceState` overwrites the current entry
  **without growing**; `back`/`forward`/`go` walk the index, restore the entry's URL into `location`, and
  fire a **`popstate` window event carrying `event.state`** (dispatched to `window.addEventListener('popstate')`
  DOC_EL/GEN_KIND listeners + `window.onpopstate`). **Location parsing** — a full WHATWG-URL component
  split (`_url_parse_into`): `protocol`/`host`/`hostname`/`port`/`pathname`/`search`/`hash`/`origin`
  (userinfo stripped, `origin = protocol//host`, bare authority → `pathname "/"`). `location.href = …`
  re-parses; `location.hash = …` rebuilds `href` and fires **`hashchange`**; `location.assign/replace`
  parse; `location.toString()` → `href`. A `__setLocation(url)` host test hook drives deterministic
  parsing (the headless engine has no live navigation). Gate: `test_hambrowse_webstorage_host.sh` (25
  asserts: Storage coercion+null+independence, pushState/replaceState length + `history.state`,
  back/forward/go `popstate` state, location component parsing + hashchange). **Scope deferred:**
  cross-page-load PERSISTENCE (headless single-document engine — the API SEMANTICS are what matter);
  `StorageEvent`; state is stored by reference (not structured-clone-copied).
- **observer APIs (2026-07-18): MutationObserver + IntersectionObserver + ResizeObserver** — the three
  observer surfaces modern frameworks (React reconcilers, Vue, lazy-load libs, auto-sizing widgets) depend
  on. All live in `lib/web/dom/canvas.ad` (state + `_obs_dispatch` + delivery) with NID ids 87–99 in
  `lib/web/dom/bindings.ad`; constructors registered as native globals in `_js_build_document`; NO
  `lib/web/js/` engine edits (delivery rides the EXISTING microtask queue via the global `queueMicrotask`).
  **MutationObserver** — `new MutationObserver(cb)`, `observe(target, options)`, `disconnect()`,
  `takeRecords()`. Every DOM mutation made during a script is RECORDED at the existing mutation points
  (`appendChild`/`insertBefore`/`removeChild`/`replaceChild`/`remove` → childList; `setAttribute`/
  `removeAttribute` → attributes; `textContent`/`nodeValue`/`data` member-set → characterData) into a
  batched record queue, and the observer callback is delivered **ONCE as a MICROTASK after the current
  script finishes** — the spec's microtask-checkpoint "notify mutation observers" step (verified: the
  synchronous post-mutation log line precedes the callback's log line). Options honoured:
  `childList`/`attributes`/`characterData`/`subtree` (subtree matches via a bounded `parentNode`-chain
  ancestor walk), `attributeOldValue` (captured BEFORE the write clobbers it),
  `characterDataOldValue`, and `attributeFilter` (case-insensitive name allowlist). `MutationRecord`
  carries `type`/`target`/`addedNodes`/`removedNodes`/`attributeName`/`oldValue` (+ null
  `previousSibling`/`nextSibling`/`attributeNamespace`). `disconnect()` deactivates the observer's
  registrations AND empties its record queue; `takeRecords()` drains the queue SYNCHRONOUSLY (and the
  already-scheduled delivery microtask then finds nothing to deliver). The record queue is gated by a
  live-registry check (`mreg_n != 0`) so script-free pages and internal node-building pay nothing and
  emit no spurious records → zero regression across the DOM/JS gates.
  **IntersectionObserver** — `observe(el)`/`unobserve(el)`/`disconnect()`; each entry
  (`isIntersecting`/`intersectionRatio`/`boundingClientRect`/`intersectionRect`/`target`/`time`) is computed
  by intersecting the element's laid-out box (`_el_bbox` → viewport-relative `_new_rect_obj`) against the
  viewport root (`innerWidth`×`innerHeight`). **ResizeObserver** — `observe(el)`/`unobserve`/`disconnect`;
  the entry's `contentRect` (+ `contentBoxSize`/`borderBoxSize` inline/block) reports the element's
  laid-out content box. **HEADLESS delivery model (honest):** this is a SINGLE-LAYOUT render — there is NO
  live scroll or resize loop, so IntersectionObserver and ResizeObserver deliver their entries **ONCE**,
  as a microtask after `observe()`, against the one layout the engine produced (an on-screen element reports
  `isIntersecting=true`; an element placed far off-screen via `position:absolute;top:6000px` reports
  `false`). They do NOT re-fire on scroll/resize/mutation because the headless engine has no continuous
  frame loop. Delivery is coalesced: the first queued observer work schedules ONE `NID_OBS_DELIVER`
  microtask that flushes MutationObserver batches, then IntersectionObserver entries, then ResizeObserver
  entries (the queued flag is cleared before callbacks fire, so a callback that mutates/observes schedules
  a fresh microtask — chained observers compose). Gate `test_hambrowse_observers_host.sh` (14 asserts:
  batched-as-one-microtask timing + added/removed counts + observer identity; attributeName/oldValue;
  attributeFilter exclusion; disconnect silence; synchronous takeRecords drain; IO on/off-screen; RO
  contentRect; zero JSERR).
- html5-parsing: implied end tags (gate `impliedtags`); quote-aware tag tokenization (gate `tagquote`).
- dom-core / CSSOM-view: **layout geometry** — `getBoundingClientRect()` (DOMRect: x/y/width/height/
  top/left/right/bottom), `getClientRects()` (single-fragment DOMRectList), `offsetWidth/Height/Left/Top`,
  `clientWidth/Height` (+`scrollWidth/Height`), `offsetParent`, and `getComputedStyle(el)`
  returning **RESOLVED / used values** (CSSOM): colours as `rgb()`/`rgba()` (named + `#hex` + `rgb()`),
  lengths as used `px` (`width:50%` of a 400px parent → `200px`; `margin`/`padding`/`font-size` px), and
  computed `display`/`position`/`font-weight`(400/700)/`visibility`/`opacity`/`z-index`/`font-family`, plus
  a `getPropertyValue(prop)` method (+ kebab-case index access) returning `''` for an unset/unknown property.
  An element's
  box is READ back from the laid-out SEG display list (`layout/box.ad` `seg_x`/`seg_line`/`seg_len`): the
  bounding rect of the segments whose text reconstructs the element's text content, converted to pixels
  (`seg_line*LINE_H`, `seg_len*CELL_W`). Geometry is exposed entirely from `dom/query.ad` (`_el_bbox`/
  `_new_rect_obj`/`_new_client_rects`/`_new_computed_style`/`_set_el_geom_props`/`_page_scroll_x/y`) +
  dispatch in `dom/canvas.ad` (NID_GETRECT/NID_GETCLIENTRECTS/NID_GETCOMPSTYLE) — NO layout
  re-implementation. Rects are **VIEWPORT-RELATIVE**: `_new_rect_obj` subtracts the live page scroll offset
  (`document.documentElement`/`body` `scrollTop/scrollLeft`, or the `window.scrollX/Y` / `pageX/YOffset`
  mirrors — largest non-zero wins), so scrolling the viewport shifts the reported rect by exactly the
  scroll amount (CSSOM-view). A **pre-layout pass** runs before a page's `<script>`s
  (`canvas.ad:_run_scripts`) so scripts observe box metrics; the final relayout is byte-identical, so the
  rendered output is unchanged. Gate `geom` proves the reported box is byte-identical to the SEG-dump
  coordinates (coordinate cross-check); gate `domgeom2` proves the scroll compensation, `getClientRects()`,
  and the expanded `getComputedStyle` props; gate `domgeom` (INTEGRATION, 2026-07-20) proves geometry +
  traversal cooperate on one page — `Element.matches()`/`closest()` (class/compound/id/negative + an
  event-delegation ancestor walk), `document.body`/`documentElement`/`head` roots, and
  `getBoundingClientRect()`/`offsetWidth/Height`/`clientWidth/Height` pinned to the laid-out box (a 9-char
  block → 72×16, with x/y cross-checked vs the SEG dump). ALL of these APIs were ALREADY implemented in the
  modular `lib/web/{dom,layout}/` tree from prior rounds; this round added only the integration gate. **Boundary (honest):** a text-less element (pure box /
  image-only) reports an empty rect (and `getClientRects()` an empty list); a run merged across inline
  siblings over-approximates width; scroll is a static offset a script sets (no live scroll source /
  interactive scroll-following); `getClientRects()` always collapses to one fragment (no multi-line inline
  box split); `getComputedStyle` resolves the common properties from the laid-out box (used width/height/text
  colour) + the script-set inline declaration (a framework's read-back) with UA defaults, resolving colours to
  `rgb()`/`rgba()` and lengths to used `px` (percentages against the containing block's used width). **Computed
  vs used (honest):** `width`/`height`/`color` (text) are USED values from layout; `margin`/`padding`/`font-
  size` are the resolved inline value (or the `0px`/`16px` UA default) — NOT read back from per-element box
  spacing (unmodelled); `position`/`visibility`/`opacity`/`z-index`/`text-align`/`font-family` are COMPUTED
  keywords (inline or UA default) that layout does not fully consume back (e.g. `text-align` is baked into
  `seg_x`, not re-read); percentage width resolves against script-set inline ancestor widths, and a source
  `style=""` attribute is NOT mirrored into `.style` (only script-set inline props are read). Not the full
  cascade / UA-sheet inheritance. `offsetParent` approximates as the parent element (nearest-positioned-
  ancestor rule not modelled). Gate `getcomputedstyle` proves the resolved-value forms end-to-end.
- dom-core: **document roots** — `document.body` / `document.documentElement` / `document.head` resolve
  to the `<body>`/`<html>`/`<head>` element nodes (`dom/canvas.ad` `_doc_set_el` in `_js_build_document`);
  each is the SAME object `querySelector` returns and carries a live `className`/`style`/`classList`, so
  `document.body.appendChild(document.createElement('div'))`, `document.documentElement.classList.add(...)`
  and `document.body.scrollHeight` all work. Element `scrollTop`/`scrollLeft` are writable static offsets
  (default 0). `_build_rewrite` is now recursive (`_rewrite_range`) so an appended-to/restyled element's
  descendant overrides still bake into the render. Gate `domcore`.
- ecmascript: for-of, optional chaining/nullish, Map/Set/WeakMap/WeakSet, accessor get/set, Symbol +
  Symbol.iterator, generators/yield, Object.create/defineProperty, Reflect, Proxy, typed arrays,
  structuredClone (R1-7 above), **ES modules import/export (R9, gate `modules`)**. Only class
  private `#fields` remains open.

**Genuinely OPEN, ranked (keyed to current files) — the live worklist:**
1. DOM geometry: `getBoundingClientRect`/`offsetWidth/Height/Top/Left`/`clientWidth/Height`/`offsetParent`/
   `getComputedStyle` — **DONE (gate `geom`).** Boxes read back from the laid-out SEG display list; a
   pre-layout pass makes metrics available to page scripts; coordinate cross-check vs the SEG dump.
   `dom/query.ad` (`_el_bbox`/`_new_rect_obj`/`_new_computed_style`) + `dom/canvas.ad` (NID_GETRECT/
   NID_GETCOMPSTYLE + pre-layout in `_run_scripts`) + `dom/bindings.ad` (NID constants). See the
   ALREADY-IMPLEMENTED list above for the honest coverage boundary. REMAINING dom-geometry gaps:
   `scrollTop/scrollLeft` **static** value DONE (writable data props default 0, `_set_el_geom_props`
   in `dom/query.ad`; gate `domcore`); **viewport-relative rects** (scroll compensation via `_page_scroll_x/y`),
   `getClientRects()` (single-fragment DOMRectList), and an **expanded `getComputedStyle`**
   (padding/margin/font-size/background-color) now DONE (gate `domgeom2`, `dom/query.ad` +
   `NID_GETCLIENTRECTS` in `dom/bindings.ad`/`dom/canvas.ad`). **RESOLVED-VALUE `getComputedStyle`** now DONE
   (gate `getcomputedstyle`): colours → `rgb()`/`rgba()` (via `css_color_rgb` in `css/cascade.ad`), lengths →
   used `px` (percentages resolved against the containing block in `_cs_used_width`), computed `position`/
   `font-weight`/`visibility`/`opacity`/`z-index`/`font-family`, and a `getPropertyValue(prop)` method (kebab
   access, `''` for unset) — all in `dom/query.ad` `_new_computed_style`. Still OPEN: INTERACTIVE scroll-
   following (no live scroll/viewport source), multi-fragment `getClientRects()` (inline box split across
   lines), source-`style=""`-attribute → `.style` mirroring, and full-cascade/UA-sheet-inherited resolution
   (per-element box spacing read-back, stylesheet-selector-matched widths for percentage bases).
2. `document.body`/`documentElement`/`head` — **DONE (gate `domcore`).** Each resolves to the
   `<body>`/`<html>`/`<head>` element node — the SAME registered object `getElementById`/`querySelector`
   returns, with live `className`/`style`/`classList` — via `_doc_set_el` in `dom/canvas.ad`
   `_js_build_document` (scans the source for the first matching start tag, registers it through the
   shared `_dom_register_el` dedup path, and stores it on `document`). `document.body.appendChild(...)`
   renders AND coexists with a mutation on a node NESTED under body: `_build_rewrite` was refactored into
   a recursive `_rewrite_range(lo,hi)` so an overridden element (e.g. body with an appended child) whose
   inner content is not wholly replaced re-processes its own inner range instead of copying it raw —
   previously a descendant's textContent/style override was silently dropped once its ancestor was
   appended-to. `document.body.scrollHeight` reads the laid-out body box. **Boundary (honest):** the
   source-anchored parser does NOT synthesize `html`/`head`/`body` when the tag is literally absent, so
   a `<head>`-less or `<body>`-less document leaves that accessor unset (browsers synthesize it); nearly
   all real pages carry the tags.
3. opacity + rgba()/hsla() alpha compositing — **DONE (gate `boxshadow`).** rgba()/hsla()/`#rrggbbaa` alpha is captured (`css/values.ad` `g_cv_alpha`, `_parse_alpha_frac`) and `opacity:X` parsed (`css/cascade.ad`); both are folded into the packed background/border colour at rule-store time (`_pack_fill_rgb`/`_pack_bordc`: bits 24-28 = 5-bit alpha level, bit 31 stays 0 so the value survives the `>=0` guards and never collides with the -1 unset sentinel) and pass UNCHANGED through the layout record set (`bfill_rgb`/`bbox_rgb`); the paint pass composites the fill/border at that alpha over the background (`htmlpage.ad` `_hpg_alpha` + `htmlpaint_fill_round_rect_a`/`stroke_round_rect_a`/`_blend_px`). Verified by blended-pixel asserts: `opacity:.5` on `#3060c0`→(155,178,225), `rgba(255,0,0,.5)`→pink (255,132,132). LIMITATIONS: opacity dims the element's own fill+border only, NOT its text/descendants (no group compositing); alpha is quantised to 32 levels; opacity must share a rule with the background it dims; inline `style=` bg/border alpha not folded.
4. linear/radial-gradient backgrounds — **DONE (gate `gradient`).** `background`/`background-image: linear-gradient(<angle|to side>, c0, c1[, cN])` and `radial-gradient(<shape>, c0, cN)` are parsed in `css/cascade.ad` (`_parse_gradient`: direction/angle via `_grad_dir` — `to top/right/bottom/left`, the four diagonals, and `<N>deg`; 2..8 colour stops via `_grad_stop`, each `<color> [<pos>%]`, reusing `_color_value`) into a small SIDE REGISTRY (`grad_type/angle/nstop/scol/spos`, reset per page in `_collect_css`). Because the packed fill int has no room for a gradient's state, the registry ID is smuggled through `bfill_rgb` under a marker (`_pack_gradient`: bits 24-28 == 31, an alpha level `_pack_fill_rgb` never emits, so it can't collide with a solid fill and stays `>=0` through `layout/box.ad`'s guard — that file is NOT edited). At paint time `htmlpage.ad` detects the marker (`grad_is`), gathers the stops via the `grad_*` accessors, and rasterises via `htmlpaint_fill_gradient`: a per-pixel linear projection onto the gradient axis (`sin/cos` via a Bhaskara approximation, corner-to-corner axis length) or radial centre→farthest-corner distance, interpolating the stops and respecting `border-radius` (same corner-arc inset as `fill_round_rect`); a vertical (axis-aligned) gradient takes a fast one-colour-per-row span path. Verified by interpolated-pixel asserts: to-right red→blue (left red / mid purple / right blue, constant down each column), to-bottom green→black, a 135deg diagonal (green rises L→R AND T→B), a 3-stop red/lime/blue (lime midpoint), and a radial white→black (white centre, dark corners). LIMITATIONS: radial is always centre→farthest-corner (no `at <pos>`, explicit size, or true ellipse-vs-circle distinction — it fits the box so a wide box reads elliptical); a gradient only fires when it is the FIRST token of the value (`background: <grad> no-repeat` with the gradient not leading falls back to no bg); inline `style=` gradients go through the box path, not this rule-store path; stop colour interpolation is straight sRGB (no premultiplied/linear-light); auto stop positions are spaced evenly (mixed explicit/auto approximated). `conic-gradient` and `repeating-*-gradient` are now handled — see item 4b.
4b. conic-gradient / repeating gradients / background-image:url() — **DONE (gate `gradient2`).** `conic-gradient([from <angle>] [at <pos>], stops)` paints a per-pixel angular ramp around the box centre (`htmlpaint.ad` `_hp_conic_t`: a trig-free "diamond angle", 0 at 12 o'clock, clockwise, exact at the four cardinals; `from <angle>` offsets the start, `at <pos>` recognised but the offset not yet applied). `repeating-linear/radial/conic-gradient(...)` tile the stop pattern (`_hp_gwrap` wraps the gradient parameter into `[pos0, posLast)`). Both extend `_parse_gradient` (new `gtype 2` + a `grad_repeat` flag) and add a `repeat` param to `htmlpaint_fill_gradient`. `background-image: url(<path>)` (also as the leading token of the `background` shorthand) is parsed by `_parse_bg_url` into a `gtype 3` url registry entry (quote-stripped name), rides the SAME `GRAD_MARK` packing through the untouched `layout/box.ad`, and at paint time `htmlpage.ad` looks the name up in the decoded-image store (`htmlimg_find`) and blits it via `htmlpaint_blit_image_bg` (scaled to fill, border-radius clipped, alpha-blended). Verified by pixel asserts: a conic red/lime/blue wheel (red top / green bottom / yellow right / teal left — four distinct angular hues), a 4-tile `repeating-linear-gradient(to right, red 0%, blue 25%)` (the ramp recurs one 25% period later), and a 2×2 quadrant PNG shown as a background (decoded red/green/blue/yellow pixels). LIMITATIONS: the url() image must be DECODED into the shared store to appear — the decode driver (`layout/box.ad` `_eimg_record` + the host/native image pass) only records `<img src>`, NOT CSS `url()` backgrounds (that file is off this track's ownership), so a CSS-only url renders empty until it is also referenced by an `<img>`; conic `at <pos>` centre offset and non-`%` (`<angle>`/`<length>`) stop positions are not applied. `background-size`/`position`/`repeat` are now handled — see item 4c.

4c. background-size / -position / -repeat — **DONE (gate `bgsize`).** `css/cascade.ad` parses the three longhands onto per-gid placement arrays folded onto the rule's url entry at rule-store time: `_parse_bg_size` (`cover` / `contain` / explicit `<w> [<h>]` in px or `%` / `auto` per axis), `_parse_bg_pos` (`left/center/right` × `top/center/bottom` keywords, `<x>% <y>%`, `<x>px <y>px`, single-value → other axis centred), `_parse_bg_repeat` (`repeat` / `no-repeat` / `repeat-x` / `repeat-y`). `htmlpaint.ad` `htmlpaint_blit_image_bg` gains ten placement params and computes tile size (cover = max-scale crop, contain = min-scale fit, explicit with aspect-preserving `auto`), then the first-tile origin (percent-of-free-space or px), then tiles/clips per axis inside the padding box via `_bg_map` (respecting border-radius). Verified by pixel asserts on a wide solid-red image: `cover` fully covers the box (no page bg), `contain`+`no-repeat` leaves a white letterbox on the trailing edge, `no-repeat`+`center` draws one centred block with white corners, `repeat-x` tiles a red band across the top with white below. DEFAULT NOTE: a `background-image` with NO `background-size`/`-position`/`-repeat` keeps the legacy scale-to-fill (szmode 0), preserving `gradient2`; specifying any of position/repeat (without size) switches that image to the CSS-correct `auto`/natural size so tiling/placement is visible. LIMITATIONS: no `background-size` percentages resolved against a non-content box quirk; no multi-layer (comma-separated) backgrounds; position is single-layer, two-value (no 3/4-value `left 10px top 20px` offsets).
5. border-radius — **DONE (gate `borderradius`).** See the DONE entry below — parsed in the MODULAR tree (`lib/web/css/cascade.ad` `_box_decl`/`_border_side_dispatch` → `d_bordr`/`m_bordr`/`r_bordr[]`), threaded through `lib/web/layout/box.ad` (`box_bordr_stack` → `bbox_rad`/`bfill_rad`) and `lib/web/dom/canvas.ad` (`he_bbox_radius`/`he_bfill_radius`), and painted by `lib/htmlpaint.ad` (`htmlpaint_fill_round_rect`/`htmlpaint_stroke_round_rect`, clamped to half the smaller box dim). NOT the stale monolith `lib/htmlengine.ad`.
6. box-shadow — **DONE v1 (gate `boxshadow`).** Parsed in `css/cascade.ad` (`_box_shadow_class`: offset/blur/spread lengths + `none`) into a coarse 2-bit size class packed into the fill colour (bits 29-30), and painted BEHIND the border-box before the element's own fill (`htmlpage.ad` fill loop → `htmlpaint_shadow_round_rect`: an offset, spread-expanded, radius-aware rounded rect feathered by stacking `blur`+1 alpha-blended concentric rects). Verified: a dark 30% grey drop shadow, offset down/right, fading to the paper, respecting the fill geometry. LIMITATIONS: single shadow only (no comma list); NO inset; offset/blur/colour are approximated by a default dark-30% shadow whose SIZE scales with the declared class (per-element geometry/colour can't be plumbed through the `layout/box.ad`+`dom/canvas.ad` record set, which this track does not own); only elements that also have a background (a `bfill` record) get a shadow.
7. background-image:url() (+position/size/repeat) — **DONE (blit + placement, gates `gradient2` and `bgsize`; see items 4b and 4c).** `css/cascade.ad` `_parse_bg_url` + `htmlpage.ad` `htmlimg_find`→`htmlpaint_blit_image_bg`. A `url()` whose image is decoded into the store paints as the element background; `background-size` (cover/contain/`<len>`/`%`/auto), `background-position` (keywords/`%`/px) and `background-repeat` (repeat/no-repeat/repeat-x/repeat-y) now size, place and tile it (radius-clipped). REMAINING: CSS-only urls aren't auto-decoded (the `layout/box.ad` image-record pass, off this track, only scans `<img src>`).
8. HTTP method=POST submission + enctype — **DONE (gate `formvalid`).** A form's `method` attribute is
   now honoured: `method=POST` serializes the named controls into a `application/x-www-form-urlencoded`
   REQUEST BODY ("a=1&b=2") in `dom_body_buf` and leaves the action target query-free, with
   `dom_nav_method`=1; `method=GET` keeps the existing `action?a=1&b=2` query string. Exposed to the
   front-end via `he_nav_method()`/`he_nav_body_ptr()`/`he_nav_body_len()` beside `he_nav_ptr`.
   `dom/canvas.ad` (`_serialize_form`/`_serialize_field`/`_form_is_post` + `_sfld_putc`/`_sfld_encoded`
   target-dispatch; body accessors). LIMITATIONS: only urlencoded enctype (multipart/FormData deferred, #10);
   the NATIVE front-end's http9 client is GET-only so a real over-the-wire POST awaits an http9 `http_post`
   (engine-side serialization + API is complete and host-verified; no new sockets).
9. overflow/overflow-x/y (hidden|scroll|auto|clip) — **DONE (gate `overflow`).** A non-visible overflow box establishes a padding-edge clip rect; descendant segments/fills/borders outside it are removed (tall child clamped to the box, nowrap line truncated at the right edge). `css/cascade.ad` (`_box_decl` `_overflow_value` + `m_ovfx/m_ovfy`) + `layout/box.ad` (`_apply_overflow_clip` at `_block_box_close`). REMAINING: interactive scroll offset/scrollbars for auto|scroll (static first paint only). `layout/scroll.ad` still holds only the legacy viewport-scroll helpers.
9b. text-overflow (clip|ellipsis) — **DONE (gate `ellipsis`).** The single-line `overflow:hidden;white-space:nowrap;text-overflow:ellipsis` idiom renders a `…` (emitted as "..." — the monospace glyph set has no U+2026) at the overflow-x clip's truncation point, pulling the visible text back up to 3 cells so text+ellipsis fits the content box; `clip` (the default) hard-cuts. `css/cascade.ad` (`text-overflow` decl + `m_txtovf/r_txtovf`) + `layout/box.ad` (`box_txtovf_stack`, ellipsis branch in `_apply_overflow_clip`). REMAINING (deferred): multi-line `-webkit-line-clamp`, the `<string>`/left/`fade` text-overflow variants, and the case where the truncation boundary falls between segments rather than through a text run.
9c. CSS 2D `transform` (+ transform-origin) — **DONE (gate `transform`).** `css/cascade.ad` `_box_decl` now parses `transform`: `translate(x[,y])`/`translateX`/`translateY` (px, `%`), `scale(sx[,sy])`/`scaleX`/`scaleY`, `rotate(<deg>|<rad>|<turn>)`, `matrix(a,b,c,d,e,f)`, and whitespace/comma-separated composition applied LEFT-TO-RIGHT, composing into a 2×3 affine (`_xf_parse_transform`→`_xm_mul`; coefficients in 12-bit fixed point `XF_FX`, `sin/cos` via a Bhaskara approximation — EXACT at 0/90/180/270, ~0.16% worst-case elsewhere). `transform-origin` (`_xf_parse_origin`; `left/center/right`/`top/bottom` keywords + `%`/px, default `50% 50%` = centre) rides the same template. Because the packed fill int is full, the affine is stored in a side TEMPLATE registry (`xt_*`) and, at cascade-resolve time (`_cascade_match_current`), BOUND with the fill the box paints (`xb_*` pool) and re-packed through `m_bg`/`bfill_rgb` under `XFORM_MARK` (bits 24-28 == 31 **and** shadow-bits == 1 — a pattern `_pack_fill_rgb` never emits and distinct from a gradient's 24-28==31/shadow==0; `grad_is` refined to require shadow==0). The reference flows UNCHANGED through the untouched `layout/box.ad`/`dom/canvas.ad` record set (they treat it as an opaque colour), so a plain block `<div style/class transform:…>` transforms without editing those files. At paint time `htmlpage.ad` detects it (`xform_is`), resolves `%` translate + `%` origin against the box's actual size, and repaints the border-box as a transformed QUAD via `htmlpaint.ad htmlpaint_fill_box_xform` (four mapped corners → `htmlpaint_fill_poly`). Verified by transformed-pixel asserts (gate `transform`, fixture `hambrowse_transform.html`): `translate(50px,30px)` shifts the coloured region exactly +50/+30 (origin vacated to page bg), `scale(2)` doubles it about its centre, `rotate(90deg)` turns a WIDE box visibly TALL (grows vertically, loses width), and `matrix(1,0,0,1,50,30)` renders identically to the translate. FIDELITY / LIMITATIONS (honest): translate + scale are pixel-exact; rotation renders a real filled affine quad (scanline-rasterised — not a bounding box), with corners placed via the Bhaskara `sin/cos` (cardinal angles exact). ONLY the element's own background box is transformed — its DESCENDANTS' painted text/segments are NOT, because their per-subtree paint ranges are not exposed to `htmlpage` through the `layout/box.ad`/`dom/canvas.ad` record set (off this track's ownership); so a transformed `<div>`'s text stays at its laid-out position while its coloured box moves/scales/rotates. A gradient/`url()` inner fill under a transform is not rasterised in the transformed quad (solid inner only); `%` translate composed AFTER a scale/rotate is applied post-matrix (exact for a leading `translate(%)`, approximated otherwise); an inline `style=` transform on a BLOCK element (vs a class/id rule) is not yet folded (block bg reads `m_bg` directly in `dom/canvas.ad`; inline-block picks it up via `_open_style`); no 3D transforms / `perspective` / `transform-box` / individual `translate`/`rotate`/`scale` properties.
9d. Inline `<svg>` + SVG shapes/paths/presentation (pre-existing) — **DONE (gates `svg`, `inlinesvg`).** `lib/svg.ad` is a from-scratch, integer-only SVG rasterizer feeding RGBA into `lib/htmlimg`; `lib/web/layout/box.ad _handle_inline_svg` (wired at `lib/web/dom/forms.ad`) rasterises a whole inline `<svg>…</svg>` subtree and emits a replaced image box blitted like `<img>`, while `<img src=*.svg>` dispatches by content (`svg_sniff`). Shapes `<rect>` (rx/ry rounded), `<circle>`/`<ellipse>`, `<line>`, `<polyline>`/`<polygon>`, `<path>` (M/L/H/V/C/S/Q/T/A + Z, beziers/arcs flattened); presentation `fill`/`stroke`/`stroke-width`/`fill-opacity`/`fill-rule` (named + `#rgb`/`#rrggbb` + `none`); `<svg width height viewBox>` user→viewport map; `<g fill>` inheritance. AA scanline fill (nonzero/even-odd), bounds-safe on untrusted input.
9e. SVG `transform=` on `<g>` and shapes — **DONE 2026-07-18 (gate `svg_transform`).** `lib/svg.ad` now carries an affine CTM (`a,b,c,d` Q16 + `e,f` Q8) applied in USER space in FRONT of the viewBox map (`_add_user`), so identity leaves the viewBox-only mapping bit-for-bit unchanged (both pre-existing SVG gates stay green). `_apply_transform_list` parses `translate(tx[,ty])`, `scale(sx[,sy])`, `rotate(deg[,cx,cy])`, `matrix(a b c d e f)`, `skewX(deg)`/`skewY(deg)`, composed LEFT-TO-RIGHT (`_ctm_mul`, apply-M-first). A `<g transform>` pushes its composed CTM onto a 64-deep stack (parallel to the `<g fill>` stack) and `</g>` restores it, so NESTED groups stack; a shape's own `transform=` composes on top for that element only (single-slot save/restore). Angles snap to the nearest degree (the table-free integer trig used by arc flattening). Verified HOST-ONLY render-to-PNG (`scripts/test_hambrowse_svg_transform.sh`, fixtures `hambrowse_svg_xform.svg`/`.html`) both via the standalone `svg_probe` AND the full inline-`<svg>` browser path, sampling EXACT pixels: `<g translate>` moves a rect (origin vacated), `<g translate+scale(2)>` grows it past its unscaled extent, per-shape `rotate(90 cx cy)` turns a vertical rect into a horizontal bar (pivot filled, original extent vacated), `matrix()` translate places a rect, and NESTED `<g translate><g translate>` compose. FIDELITY / LIMITATIONS (honest): stroke-width does NOT scale with the transform (stays viewBox-relative); `transform=` on the root `<svg>` is not applied (rare). DEFERRED across the SVG core (documented, not faked): `<text>`, gradients/patterns (`<defs>`/`<linearGradient>`), clipping/masks, `<use>`/`<symbol>`, CSS-on-SVG + animations, exponent-form numbers.
9f. object-fit / object-position (replaced-element sizing) + aspect-ratio — **DONE 2026-07-18 (gate `objectfit`).** `css/cascade.ad` `_box_decl` now parses `object-fit` (`fill`/`contain`/`cover`/`none`/`scale-down` → `_objfit_value`), `object-position` (`_parse_obj_pos`: `left/right/top/bottom/center` keywords + `<%>`/`<px>`, reusing the background-position token machinery) and `aspect-ratio` (`_parse_aspect_ratio`: `<w>/<h>` or a single number → milli-scaled `d_aspw/d_asph`), each flowing through the full inline+stylesheet cascade (`d_*`→`r_*`→`m_*` with per-property specificity trackers). Because `<img>` is a VOID element NOT pushed onto the element stack by the flow, `layout/box.ad _handle_img` resolves its cascade via a new `_img_cascade` helper (transiently `_el_push`/match/`_el_pop`, RESTORING the parent's `:nth-child` counters so counting the image does not perturb sibling selection) and stores the resolved fit + position per-segment (`seg_objfit/seg_objp{x,y}{,p}`, exposed via `he_seg_objfit`…). At paint time `htmlpage.ad` routes non-fill fits to `htmlpaint.ad htmlpaint_blit_image_fit`, which computes the drawn rectangle (contain = min-scale letterbox, cover = max-scale crop, none = natural, scale-down = min(none,contain)), offsets it by `object-position` (`_objpos_off`: `free*pct/100` or px), and samples only box-local pixels (so cover crops and contain/none leave the box background showing in the bars). `aspect-ratio` is applied in `_block_box_open`: when a box has a definite width (the caller-resolved `wd`) and AUTO height, the height is derived `wd*asph/aspw` and pinned exactly like an explicit `height` (`box_ht_stack`). Verified HOST-ONLY render-to-PNG (`scripts/test_hambrowse_objectfit_host.sh`, fixture `hambrowse_objectfit.html`, probe `hb_objectfit_probe.py`) with EXACT pixel/box-dim asserts on a 2:1 green-left/red-right `<img>`: `cover` fully covers a 40×40 box (no white), `contain` leaves WHITE letterbox bars top+bottom with the image band centred, `object-fit:none`+`object-position:left top`/`right top` show the image's green/red halves respectively, `aspect-ratio:2/1`+`width:200` lays out to the SAME pixel height as an explicit `height:100px` reference box, and the narrow `overflow:hidden;white-space:nowrap;text-overflow:ellipsis` block renders a trailing `...` with the long word clipped. HONEST SCOPE / LIMITATIONS: `aspect-ratio` derives HEIGHT from a definite width only (the reverse — width from a definite height — is not computed, since the width is resolved by the caller before the box opens; ratio is unset when no definite width exists); `object-fit`/`object-position` apply to raster/SVG `<img>` boxes (inline `<svg>` is drawn fill); CSS `width`/`height` on an `<img>` still size via the HTML width/height attributes (CSS box-sizing of images unchanged); percent/px object-position is honoured, 3/4-value offsets are not. text-overflow:ellipsis (item 9b) was CONFIRMED unchanged by this round.
10. FormData ctor + append/get/entries — **DONE (2026-07-18, gate `formdata`).** `FormData` is
    a global constructor whose instances reuse the EXACT `@@k/@@v` parallel-array multimap
    URLSearchParams already implements, so its prototype binds the SAME `NAT_URLSP_*` method ids
    (get/getAll/has/set/append/delete/forEach/entries/keys/values + `Symbol.iterator`) over the
    shared storage — only the constructor (`NAT_FORMDATA`) is distinct (`lib/web/js/builtins/url.ad`
    + `consts.ad`). `new FormData()` (empty) and a plain-object init (an extension) are supported;
    values coerce to strings; `for (const [k,v] of fd)` and `fd.getAll` (repeated keys) work; a
    missing `get` returns `null`. REMAINING: the spec's `new FormData(formElement)` DOM-scrape (the
    DOM layer's job, not the engine's) and Blob/File values + multipart serialization (http9 is
    GET/urlencoded-only).
11. Constraint Validation breadth — **DONE high-value subset (gate `formvalid`).** Beyond the pre-existing
    `checkValidity()`, controls now expose `reportValidity()`, `setCustomValidity(msg)`, a live `validity`
    ValidityState object (`valueMissing`/`typeMismatch`/`badInput`/`tooLong`/`rangeUnderflow`/`rangeOverflow`/
    `customError`/`valid` refreshed each validation), `validationMessage`, and `willValidate`. Enforcement:
    `required` empty → `valueMissing`; `type=email`/`url` basic shape → `typeMismatch` (rejects "abc");
    `type=number` non-numeric → `badInput` and `min`/`max` → `rangeUnderflow`/`rangeOverflow`; `maxlength`
    exceeded → `tooLong`. A `submit()` on an invalid form is now BLOCKED (no navigation) unless the form
    carries `novalidate`. `dom/bindings.ad` (NIDs + validity/customval state), `dom/query.ad`
    (`_attach_el_methods` binds reportValidity/setCustomValidity + the validity object), `dom/canvas.ad`
    (`_dom_control_valid`/`_form_check_all`/`_write_validity` + email/url/number validators, `_do_submit`
    gate). DEFERRED (documented): `pattern` regex (`patternMismatch` always false — no regex engine),
    `step`/`stepMismatch`, `tooShort`/`minlength`, `:invalid` CSS pseudo, live per-keystroke UI reporting.
12. grid-row / row-axis placement & spanning — **DONE (gate `gridspan`).** `grid-row: <n>`,
    `<n> / <m>`, and `span N` parse via `cascade.ad _grid_row_parse` (mirrors `_grid_column_parse`),
    and auto-placement now runs a real 2-D occupancy map (`box.ad flex_occ` + `_grid_rect_free`/
    `_grid_occupy_rect`): a `grid-row: span N` / explicit-row item reserves its whole rectangle so
    row-major auto-flow routes siblings around it (`lib/web/dom/forms.ad` grid item-open path).
    Row track sizing honours `grid-template-rows` for the spanned item's origin (`_grid_row_top_rows`).
    Still deferred: subgrid. (grid-template-areas/grid-area named — DONE round-6, gate `gridareas`;
    `grid-auto-flow: dense`/column — DONE round-9, gate `gridflow`.)
13. ES modules import/export — **DONE (R9, gate `modules` + browser gate `esm`).** Parser (`js/parser.ad`
    `parse_import`/`parse_export`), AST (`ND_IMPORT`/`ND_EXPORT`/`ND_IMPSPEC`/`ND_EXPSPEC`), and a full
    loader/linker/evaluator (`js/module.ad`): every import form (default, named `{a,b as c}`, namespace
    `* as ns`, side-effect `import "x"`) and export form (`const/function/class`, `default`, named list,
    re-export `{x} from`, `* from`); relative-specifier resolution via an embedder resolver seam
    (`js_set_module_resolver` — browser fetch / host file read, NO sockets), a specifier-keyed module cache
    (diamond deps evaluate once), dependency-ORDER (post-order) evaluation, and `<script type=module>`
    wired into `dom/canvas.ad:_run_scripts` (inline + `src` sibling imports). Exports live on a per-module
    namespace object (also the value of `* as ns`). **Coverage boundary (honest):** named/default imports
    are SNAPSHOT bindings (value copied at link time — correct for the near-universal
    export-then-never-reassign case; a later reassignment of an exported `let` is not reflected — true LIVE
    bindings deferred). `import * as ns` IS live (property reads hit the current export values). Import
    cycles are best-effort (function exports resolve across the edge via hoisting; a value import taken
    across a cycle back-edge before the exporter finished is undefined). Top-level `await` NOT supported.
    Specifier canonicalization is leading-`./` strip only (flat/single-base-dir graph; `../` collapse +
    query strings deferred). Next: true live bindings + top-level await + per-importer nested URL resolution.
14. class private `#fields` — DONE (R9, gate `es2022priv`). `#field`/`#method`/`get #x`/`static #n`, `this.#x` access, `#x in obj` brand check, hidden from Object.keys/for-in/JSON; plus `static { }` initializer blocks. `js/{lexer,parser,interp,value}.ad`.
15. specialized input types email/number/date/search/tel/url — degrade to plain text. `dom/forms.ad`.

Disjoint dispatch clusters (no write-collision): **JS** (`js/*` only: #13,#14) · **CSS-PAINT+LAYOUT**
(`css/*`+`layout/*`: #3-7,#9,#12) · **DOM-GEOMETRY** (`dom/query,bindings` + geometry half of `canvas.ad`: #2; #1 LANDED gate `geom`)
· **FORMS** (`dom/canvas,forms` + `js/builtins/native.ad`: #8 + #11 LANDED gate `formvalid`; remaining #10,#15 — shares canvas.ad with DOM-GEOMETRY, so serialize).

---

## High-impact worklist (56) — ORIGINAL 2026-07-16 pre-refactor audit (line numbers STALE; see fresh audit above)

- **[html5-parsing] Quote-aware tag tokenization ('>' inside an attribute value)** (buggy) — `lib/htmlengine.ad: _layout tag scan (7391-7394), _scan_start_tag (7913), _find_close (7860), _parse_fragment (10462)` — Every tag-boundary scan naively advances to the first '>' byte with no awareness of quoted attribute values. Markup like <a title="a>b">, <div data-x="if(x>1)">, onclick="if(a>b)...", or inline SVG pa
- **[html5-parsing] Optional / implied end tags (general)** (partial) — `lib/htmlengine.ad: _handle_tag block branches (~6700-7050); only <li> (4945/6779) and table cell (6984) auto-close exist` — There is no general implied-end-tag algorithm. <p> is never auto-closed by a following block (<p>a<div>...), and <dt>/<dd>, <option>/<optgroup>, <thead>/<tbody>/<tfoot>/<tr>/<td>/<th>, <colgroup>, <rp
- **[html5-parsing] Adoption agency / misnested formatting recovery** (DONE — see item 5 above; gates `adoption` VISUAL + `adoptdom` DOM tree) — VISUAL result via orthogonal `g_bold_n`/`g_italic_n` counters (`lib/web/dom/forms.ad`); DOM node tree via `lib/web/dom/domtree.ad` `_adoption_fixup()`/`_tx_insert`/`_tx_is_formatting` (list of active-formatting elements + reconstruct + reparent). `<b><i>x</b>y</i>` now yields the spec DOM `<b><i>x</i></b><i>y</i>` — the reconstructed `<i>` is a SIBLING of `<b>` in `childNodes`/`nextElementSibling`, not a child. Deferred first-cut edges documented in item 5. (The doc citation `lib/htmlengine.ad _find_close (7850)` was STALE: the live engine is the modular `lib/web/` tree; the real `_find_close` is `lib/web/dom/bindings.ad:571`.)
- **[css-selectors] Attribute selectors in the CSS cascade** (missing) — `lib/htmlengine.ad _parse_compound` — cascade skips brackets
- **[css-box-model] overflow / overflow-x / overflow-y** (DONE 2026-07-17, gate `overflow`) — `lib/web/css/cascade.ad _box_decl + _overflow_value + m_ovfx/m_ovfy + r_ovfx/r_ovfy cascade; lib/web/layout/box.ad _apply_overflow_clip at _block_box_close (captures seg/bfill/bbox high-water at _block_box_open)`. `overflow`/`overflow-x`/`overflow-y: hidden|scroll|auto|clip` are parsed and cascade per axis. A non-visible overflow box establishes a clip rect at its padding edge; descendant segments (text/images/marker/field), background fills, and border rectangles that fall outside are removed — a child taller than the box is clamped to the box bottom, a nowrap line wider than the box is truncated at its right edge, and an overflow:visible control keeps its full-height child. REMAINING (deferred + documented): interactive scroll OFFSET and scrollbars for auto|scroll (this is the static first paint — the clip, which is the high-value part), and clip on non-block (inline/table) containers.
- **[css-overflow] text-overflow (clip | ellipsis)** (DONE 2026-07-17, gate `ellipsis`) — `lib/web/css/cascade.ad text-overflow decl (_box_decl) + m_txtovf/d_txtovf/r_txtovf cascade; lib/web/layout/box.ad box_txtovf_stack (captured at _block_box_open), ellipsis branch in _apply_overflow_clip`. For the canonical single-line `overflow:hidden;white-space:nowrap;text-overflow:ellipsis` idiom whose inline content is wider than the content box, the overflow-x clip's right-edge truncation overwrites the last (up to 3) visible cells of the straddling text run with "..." (a `…` fallback — the bundled monospace/BDF+TTF glyph set is one byte per cell with no U+2026), so the visible text is pulled back to leave room and text+ellipsis fits the box. `clip` (the default, and unspecified) hard-cuts with no ellipsis; content that does not overflow is untouched. Only plain text runs are ellipsized (image/field/marker/box-art runs are left as a hard clip). REMAINING (deferred + documented): multi-line `-webkit-line-clamp`, the `<string>`/left/`fade` text-overflow variants (out of scope — the single-line ellipsis is the vast majority of real usage), and the rare case where the truncation boundary falls exactly between two segments rather than through a text run (no run straddles the edge, so no ellipsis is placed).
- **[css-box-model] margin vs padding separation (box edges)** (buggy) — `lib/htmlengine.ad _box_add_l/_r/_t/_b, _box_shorthand, _box_decl (~3462-3640)` — margin-left and padding-left (and each other side) are summed into a SINGLE inset column (_box_add_* is called for both). CSS treats padding as inside the border/background and margin as outside; fold
- **[css-box-model] border-width / border-style / border-color / per-side borders** (partial) — `lib/htmlengine.ad _box_decl border branch (~3552-3560); bbox_rgb (233), border draw` — Any border-* (except radius/image/collapse/spacing) collapses to a single boolean d_bd=1 that draws a fixed 1px rectangle on all four sides. Thickness (border-width, thin/medium/thick), style (dashed/
- **[css-box-model] box-sizing (content-box / border-box)** (DONE 2026-07-17, gate `boxsizing`) — `lib/web/css/cascade.ad`: `box-sizing` parses into `d_boxsz`/`m_boxsz`/`r_boxsz` (0 unset / 1 content-box / 2 border-box). Horizontal + vertical padding sums (`d_padx`/`d_pady`, cascade `m_padx`/`m_pady`, rule `r_padx`/`r_pady`) and border width px (`d_bordw`/`m_bordw`/`r_bordw`, from the leading length of `border`/`border-width`/`border-<side>`) are tracked so the border-box reduction is `content = width − padding − 2·border` on the inline axis and `height − padding − 2·border` on the block axis. The reduction runs ONCE per element: an all-inline `style=""` block resolves it in `_parse_inline_wa`, and the stylesheet path resolves it at cascade-match time against the WINNING width/padding/border, so the ubiquitous `*{box-sizing:border-box}` reset composes correctly when box-sizing, width, padding and border come from DIFFERENT rules (the reset is the whole point). content-box (default) is unchanged — padding+border still add to the outer size — so nothing shifts unless border-box is opted in. Percentage and auto widths still behave (the % resolves to px before the reduction). CAVEAT (documented): borders render as a fixed 1px/1-cell frame regardless of declared thickness, so the outer box of a THICK-bordered border-box element is a few px off the declared size in absolute terms; the content/outer DIFFERENTIAL between content-box and border-box is exact (= padding+border). `flex-basis` (main-size) is reduced on both the inline and cascade paths alongside width/height. REMAINING (documented): the rare cross-path where `width` is set inline but `box-sizing` comes only from a cascade `*` rule (each path reduces its own copy, so this specific split is not reduced — set box-sizing in the same origin as width to be safe).
- **[css-flexbox] flex-grow / flex shorthand (flex:1)** (DONE 2026-07-17, gate `flexgrow`) — `lib/web/layout/box.ad _flex_grow_scan/_flx_layout_grow, wired into _flex_container_open`. Each container's free space is now distributed to items in proportion to their inline flex-grow: three `flex:1` items split the row evenly, `flex:2` vs `flex:1` split ~2:1. Reuses the existing per-column `flex_child_x/_w` placement (the justify natural path). (Update: grow is now read from the CASCADE winner too — class/stylesheet `.col{flex:1}` — with inline `style="…"` overriding per-property; see the `flexclass` entry.)
- **[css-flexbox] flex shorthand / flex-basis parsing** (DONE 2026-07-17, gate `flexgrow`) — `lib/web/layout/box.ad _flx_parse_shorthand/_flx_scan_style`. `flex: G [S] [B]`, `flex: 0 0 200px`, `flex: 1 1 auto`, `flex-basis:Npx`, and the `flex:<n>` one-value form (which resolves flex-basis to 0 per spec) are parsed. An explicit flex-basis pins the item's main-size as the base for grow distribution; `auto`/unset falls back to the natural content-width measurement. (Update: class-resolved flex-basis is now honoured too — see the `flexclass` entry.)
- **[css-flexbox] flex-shrink / overflow handling** (DONE 2026-07-17, gate `flexshrink`) — `lib/web/layout/box.ad _flx_scan_style/_flx_parse_shorthand (flex-shrink → flex_grow_s, default 1000) + _flx_layout_grow (NEGATIVE free space distributed as shrinkage)`. When flex items' base sizes (flex-basis, else content) overflow the container, the overflow is distributed as SHRINKAGE weighted by each item's SCALED flex-shrink factor (`flex-shrink × flex-basis`, in int64 to avoid overflow): three `flex-basis:300px` items shrink EQUALLY to fit the row exactly (no overflow past the edge), a `flex-shrink:0` item HOLDS its basis while shrinkable siblings absorb the whole overflow, and mixed factors distribute proportionally (`flex-shrink:3` loses 3× a same-basis `flex-shrink:1` sibling → 350 vs 250 px from a 400px basis). The resolved-flexible-length path (`_flx_layout_grow`) now handles BOTH signs of free space, mirroring flex-grow; the trigger `_flex_grow_scan` fires it whenever any child declares an inline flex-grow>0 OR an explicit flex-basis. Boundaries (documented): flex-shrink read from INLINE `style="…"` (or the shorthand's 2nd number) only — class-resolved flex keeps the equal-column approximation, exactly like flex-grow; the min-content clamp floors at 0 rather than iterating frozen items (fine for the common cases; no item hits it in practice); single-level flex only — nested-flex shrink is deferred. (Class-resolved flex sizing now DONE — see the `flexclass` entry below.)
- **[css-flexbox] class / stylesheet-resolved flex sizing** (DONE 2026-07-17, gate `flexclass`) — `lib/web/css/cascade.ad _flx_child_cascade (new) + _flex_shorthand basis-0 fix; lib/web/layout/box.ad _flex_grow_scan`. The per-child flex pre-scan now resolves the CASCADE winners (`m_flexg`/`m_flexs`/`m_flexb`) for each flex item — so `flex-grow`/`flex-shrink`/`flex-basis` set by a class or stylesheet rule (`.col{flex:1}`, `.wide{flex:2}`) drive the proportional `_flx_layout_grow` path instead of falling back to the equal-column approximation. `_flx_child_cascade` temporarily pushes the child element onto the cascade element stack (so class/id/tag selectors — and descendant/ancestor combinators relative to the flex container already on the stack — match exactly as at real layout time), calls `_cascade_match_current`, then unwinds the push WITHOUT disturbing the parent's sibling counters (`el_nchild`/`el_rootcnt`) so the subsequent real element push still numbers `:nth-child` correctly; the container's winners are restored by re-matching once the scan hits the container's close. Each factor seeds the scratch (`flex_grow_g/s/b`) from the cascade winner FIRST, then an inline `style="flex…"` OVERRIDES per-property (inline > cascade, spec cascade order), so all prior inline-flex gates stay green. Also fixed: the cascade `flex` shorthand `_flex_shorthand` now resolves a numeric-only `flex:<n>` to flex-basis 0 (spec `flex:1` == `1 1 0`), matching the inline path, so equal class grow factors yield EQUAL widths. Verified via gate `flexclass` (NO inline flex: three `.col{flex:1}` → 205/205/205 even split; `.wide{flex:2}` vs `.col{flex:1}` → 400/208 ~2:1) + PNG; zero regressions across flexgrow/flexshrink/flexalign/aligncontent/flexwrap/flexgap/flexrowgap/flexjustify/flexmin/flexnav/dispflex + realsite. Boundary: min-content clamp floors at 0; single-level only (nested-flex deferred). NEXT flexbox gap: min-content-aware shrink clamping (frozen-item iteration) / column-direction wrap / class-resolved `order`.
- **[css-flexbox] align-content (multi-line cross-axis distribution)** (DONE 2026-07-17, gate `aligncontent`) — `lib/web/css/cascade.ad _align_content_value + full r_/m_/d_acontent plumbing; lib/web/layout/box.ad _flex_content_fixup (called from _flex_container_close), flex_ac/flex_cross per-frame state`. When a `flex-wrap:wrap` container WRAPS onto multiple lines AND has an explicit cross-size (height in `box_ht_stack`) taller than its packed lines, the extra cross-axis free space is distributed among the flex LINES per the keyword: `flex-start` (default packing), `center` (block offset by free/2), `flex-end` (packed at the bottom, offset by full free), `space-between` (first line pinned to top, last to bottom, free split into inter-line gaps), `space-around` (half-gap ends), `space-evenly` (equal gaps incl. ends), `stretch` (each line grows by free/nlines, item boxes grown). Reuses the align-items deferred-fixup bookkeeping (per-item [seg,bfill,bbox) ranges + line-top/bottom): the fixup groups items into lines by shared recorded line-top, then shifts each line's items down by its computed offset (and `_flex_stretch_item` grows boxes for stretch). Verified via gate `aligncontent` (four identical 2-line height:200px containers differing only in align-content — asserts space-between pins first/last, flex-end offsets by full free, center by free/2, ordering flex-start<center<flex-end) + PNG. Boundaries (documented): distributes only when the container has an explicit HEIGHT giving cross free-space (auto-height => no-op, spec-correct since there is no free space); `normal` maps to unset (line-packed) rather than its formal `stretch` alias to keep auto-height layouts byte-identical (harmless — stretch with no free space is a no-op); single-line containers ignore it (per spec); armed only for the online-wrap path, never grid (whose item count can exceed FLEX_MAX); combined align-items:stretch + align-content:stretch double-grow is not composed (rare). NEXT flexbox gap: class-resolved flex layout (grow/shrink/align read inline styles only) / min-content-aware shrink clamping / column-direction wrap.
- **[css-grid] percentage tracks** (DONE 2026-07-17, gate `gridpng`) — `lib/web/css/cascade.ad _grid_simple_track` (a `<n>%` track is encoded as the sentinel `-(100000+pct)`, distinct from fixed px `>=0` and fr weights `-1..`) + `lib/web/layout/box.ad _grid_resolve_tracks` (the sentinel is resolved to `avail*pct/100` against the GRID CONTAINER'S inline size, counted as fixed occupancy so fr tracks share only the remainder). Fixes the prior bug where `%` tracks were resolved by `_style_len` against the VIEWPORT: a nested `grid-template-columns:25% 75%` inside a 300px box now splits its own 300px container (col2 origin = +75px), not the 640px page (which put it near +160). Flows through the single-int32 `r_gtc`/`m_gtc`/`d_gtc` cascade pipeline unchanged (no new arrays). Verified at the PIXEL level (render-to-PNG probe `scripts/hb_grid_probe.py`): the yellow 75% cell's origin is exactly Fff+75 inside the grey container. Boundary: percentage ROW heights (`grid-template-rows:… %`) degrade to content-sized (no container height tracked); fractional-percent (`33.3%`) falls back to the viewport `_style_len` path (integer % is the common case).
- **[css-grid] repeat(auto-fill / auto-fit, ...)** (DONE, gate `gridr2`) — `lib/web/css/cascade.ad _grid_parse_template` repeat branch — the responsive `repeat(auto-fill|auto-fit, minmax(120px,1fr))` idiom derives the track count from the container inline size / the track's min size (`_grid_avail_px`). Verified: 8 items lay out 4-per-row at a 640 viewport.
- **[css-grid] minmax()** (DONE, gate `gridr2`) — `lib/web/css/cascade.ad _grid_one_track` — `minmax(MIN,MAX)` is parsed: a fixed MAX pins the track, a flexible MAX (fr/auto) with a fixed MIN pins to the MIN floor (the `minmax(200px,1fr)` idiom), two flexible bounds fall back to the MAX fr weight. (No intrinsic-sizing pass, so these are the lean approximations — documented.)
- **[css-grid] grid-template-areas / grid-area (named areas)** (OUT OF SCOPE) — `lib/web/css/cascade.ad (no parser)` — No parsing of grid-template-areas strings and no grid-area name resolution. Page-shell layouts (header/nav/main/aside/footer via named areas) do not lay out as authored; the equivalent explicit `grid-column`/track layout does work. Deferred with named lines + subgrid.
- **[css-grid] 8-track cap (GRID_MAXTRACK)** (OUT OF SCOPE) — `lib/web/layout/box.ad:245 (GRID_MAXTRACK=8, == FLEX_MAX) + cascade.ad r_gtc/r_gtr sizing` — Templates are capped at 8 tracks; extra tracks are dropped. The 12-column Bootstrap grid (`repeat(12,1fr)`) truncates to 8. Raising it ripples through the shared flex per-frame arrays keyed by `FLEX_MAX` (grid reuses the flex frame), so it is deferred rather than half-done.
- **[css-grid] grid-row / row-axis placement & spanning** (DONE) — grid-COLUMN and grid-ROW placement/spanning both work. `grid-row: <n>` / `<n> / <m>` / `span N` parse via `cascade.ad _grid_row_parse`; auto-placement uses a 2-D occupancy map (`box.ad flex_occ` + `_grid_rect_free`/`_grid_occupy_rect`) so an explicitly-placed or row-spanning item reserves its whole rectangle and row-major auto-flow routes siblings around it (dispatch in `lib/web/dom/forms.ad`). Verified by gate `gridspan` (fixture `hambrowse_grid_span.html`). Still deferred: subgrid, and forcing a row-spanning box's visual height to fill its spanned FIXED tracks (the box stays content-height, matching the existing column-span behaviour). (grid-template-areas/grid-area named areas — DONE round-6, gate `gridareas`; `grid-auto-flow: dense`/column-flow — DONE round-9, gate `gridflow`.)
- **[css-positioning] position:sticky** (DONE 2026-07-17, gate `sticky`) — `lib/web/css/cascade.ad` (`d_posfx`/`m_posfx`/`r_posfx` sub-kind `2`=sticky; new `pos_fx` run-table column + `pos_scroll_y` global), `lib/web/dom/forms.ad` (plumbs the winning `fposfx` through `_register_pos`), `lib/web/layout/flow.ad` `_apply_positioned` (sticky branch). A `position:sticky` box lays out IN FLOW (mode 1, keeps its flow space, byte-identical at scroll 0), and once the assumed scroll offset carries its in-flow row above the `top` threshold it PINS to that threshold (row = `max(in-flow, pos_scroll_y + top/LINE_H)`) — i.e. it stops at the scroll container / viewport top edge instead of scrolling away. The host paints the WHOLE page canvas with no live viewport crop, so a first paint has no scroll state; the resolver takes a STATIC scroll offset (`he_set_scroll_rows`, exposed via the driver's `scroll <rows>` knob) and the gate renders the fixture unscrolled AND scrolled past the threshold, asserting the sticky header moves from its in-flow row to the viewport-top row (coincident with the fixed bar) while the ordinary in-flow content does not move. Previously `sticky` mapped to `relative` and applied its offset UNCONDITIONALLY (wrong: a `top:20` sticky shifted down even unscrolled). DEFERRED (documented): `bottom`/`left`/`right` sticky, the container-bottom "unstick" release (element let go at the container's end — needs the containing block's bottom, not tracked in the run table), and live per-frame recompute on real scroll (this is a static resolution).
- **[css-positioning] position:fixed viewport pinning** (DONE 2026-07-17, gate `sticky`) — `lib/web/dom/forms.ad` (a `fixed` box, sub-kind `1`, now takes the VIEWPORT — page content box, top row 0 — as its containing block, skipping the nearest-positioned-ancestor `posctx` that `absolute` uses) + `lib/web/layout/flow.ad` `_apply_positioned` (adds the static scroll offset `pos_scroll_y` to a fixed box's resolved top so it tracks the viewport as the page scrolls). A fixed bar declared inside a pushed-down `position:relative` ancestor now paints at the page/viewport top (ABOVE the ancestor) and moves down to stay at the scrolled viewport top — the nav-bar / cookie-banner / back-to-top idiom. Previously `fixed` was folded into `absolute` and resolved against the nearest positioned ancestor. DEFERRED: live per-frame recompute (static scroll offset only).
- **[css-positioning] position:absolute `right` / `bottom` offset anchoring** (DONE 2026-07-18, gate `abspos`) — `lib/web/css/cascade.ad` (`pos_cbrun` run-table column recording an absolute box's containing-block run + `posctx_run` frame column + `g_last_posrun` scratch + new `pos_cb_rx`/`pos_cb_bot` columns holding the ancestor's padding-box right x / bottom row) + `lib/web/layout/flow.ad` (`_register_pos` tags each absolute run with its positioned-ancestor run; `_posctx_push` records the ancestor's padding-box RIGHT edge = `blk_right_cur + CELL_W` from its laid-out geometry; `_posctx_pop` records the padding-box BOTTOM row = `cur_row (+row_dirty)` at close; `_apply_positioned` absolute branch reads those geometry edges). Fixes an on-device visual-QA bug where a `position:absolute; top:8px; right:8px` badge anchored to the WRONG edge — it pinned by the box's TEXT-run width against the ancestor's CONTENT-right (over/undershooting), so the badge landed near the top-LEFT overlapping the heading instead of the top-RIGHT corner. Now a `right`-only box pins its BORDER-BOX right edge (its own background-fill width, not glyph width) `right`px inside the containing block's PADDING-box right edge, and a `bottom`-only box pins its bottom edge above the CB padding-box bottom the same way. **The CB edges come from the positioned ancestor's LAID-OUT BOX GEOMETRY (captured when its positioning context is pushed/popped), NOT from any paint/background record**, so `right:`/`bottom:` anchor correctly for ANY containing block — gradient, transparent, image, or solid background all resolve to the same box edges. (The earlier fix scanned the ancestor's background FILL for its right/bottom edge, which found nothing when the ancestor had a linear-gradient/transparent/image background and dropped the badge at content-LEFT — the on-device failure this replaces.) `fixed`/no-ancestor falls back to the page content-right / page bottom. The `top`/`left` paths are UNCHANGED (byte-identical; they keep resolving against `pos_ox`/`pos_orow`), so the `position` gate stays green. Verified HOST-ONLY render-to-PNG (`scripts/test_hambrowse_abspos_host.sh`, fixture `hambrowse_abspos.html`): a 400×160 solid `position:relative` card with four absolute corner boxes (`right:`-boxes pin at `cont_right-8`, `bottom:`-boxes anchor to the card bottom, `top:8;left:8` unchanged), PLUS a **linear-gradient-backed** parent and a **transparent** parent each with a `top:8;right:8` badge — both badges anchor at `parent_right-8` (NOT content-left), the two cases the paint-scan version could not do. LIMITATIONS (honest): vertical offsets are ROW-granular (an 8px `bottom` inset < 1 text row rounds to 0 rows — the box anchors to the correct EDGE but the sub-row px gap is lost, identical to how `top:8` rounds); `left`+`right` both set still uses the existing full-width-bar stretch path (honors `left`, stretches to `right`), not a computed shrink width.
- **[css-positioning] auto-width `position:absolute` box shrink-to-fits (CSS 2.1 §10.3.7)** (DONE 2026-07-18, gate `abswidth`) — `lib/web/css/cascade.ad` (new `pos_wauto` run column) + `lib/web/dom/forms.ad` (flags `pos_wauto[runa]=1` when an absolute box has NO explicit `width`, i.e. `fwd<0`) + `lib/web/layout/flow.ad` `_apply_positioned` (absolute branch: when `pos_wauto` and not the `left`+`right` both-set bar case, NARROWS the box's OWN background fill to content width — glyph run + symmetric left/right padding — before reading `box_right`, so right/bottom anchoring pins the SMALL box). Fixes the on-device visual-QA bug where a `top:8px; right:8px`, width:auto badge FILLED the whole containing block (its right edge anchored correctly by the `abspos` fix, but with no width it started at the far LEFT and overlapped the heading). Now the box sizes to its content and pins that content-sized right edge `right`px inside the CB, so the badge is a small top-RIGHT chip. Only ever SHRINKS (never grows past the CB); only for auto-width boxes (`pos_wauto`), so EXPLICIT-width absolute badges are byte-identical (the `abspos` gate's `width:90px` corner boxes stay green). Verified HOST-ONLY render-to-PNG (`scripts/test_hambrowse_abswidth_host.sh`, fixture `hambrowse_abswidth.html`): a relative gradient-padded panel whose first child is a width:auto `top;right` badge — badge POSFILL width dropped from 564px (x0=94→x1=658, spanning the card) to 40px (x0=618→x1=658, top-right chip), right edge still at `panel_right-8`. LIMITATIONS (honest): padding is assumed SYMMETRIC (right pad = measured left pad) since only the fill+glyph extents are known post-layout; `left`+`right` both set still uses the full-width bar-stretch path (unchanged).
- **[html-forms] default `<button>` rendering (push-button box + flex participation)** (DONE 2026-07-18, gate `button`) — `lib/web/layout/box.ad` (`_is_flexblk` + `_generic_block_wanted` now admit `<button>` as a flex ITEM alongside `<a>`/`<li>`; new `_mark_button_seg` = seg_field kind 2) + `lib/web/dom/forms.ad` (button branch reads the label from source, emits a centred button-face seg + skips the body for the NON-flex/inline case; the generic-block flex-item path stamps the UA push-button face — button-grey fill + 1px border — and pushes the button-grey as the effective bg so the LABEL run paints on the face) + `lib/htmlpage.ad` (seg_field kind 2 → button-grey face, label CENTRED, no text caret). Fixes the on-device visual-QA bug where a right-aligned `<button>Sign up</button>` in a `display:flex` nav rendered NOTHING (the element parsed but emitted no box/label). A `<button>` now paints a real push-button — a `0xe0e3e7` face, a `~0x969696` 1px border, the centred label — and, as a flex item, a nav CTA lands at the right. The label-run seg-bg is painted in the TEXT pass (above every block background) so the button stays visible even inside a coloured nav bar whose own fill would otherwise paint over a nested block background (a known z-order limit of the equal-z bfill sort). Verified HOST-ONLY render-to-PNG (`scripts/test_hambrowse_button_host.sh`, fixture `hambrowse_button.html`, probe `hb_button_probe.py`): a `display:flex; justify-content:space-between` nav with three links + a trailing `<button>Sign up</button>` — 805 button-grey face px + 15 border px present, on the nav's RIGHT half (face x0=613 > mid). Zero regressions across abspos/position/dispflex/flexgap/flexjustify/flexnav/flexwrap_qa/grid/objectfit/transform/realsite/landing + form/event gates (formsubmit/formvalid/fieldset/inputtypes/events/evttypes/domapi). LIMITATIONS (honest): a flex-item button's label centring is approximate (the block box left-aligns; the natural column width ≈ label width, so it reads centred) and the bordered box is ~1 text-row taller than the link items; the block-box background fill itself is z-overpainted by a coloured container (the visible face comes from the label seg-bg + the border rectangle); actual click/submit dispatch is the DOM/event layer's separate, deferred concern.
- **[css-filter] CSS `filter` — grayscale/brightness/invert/sepia/contrast/saturate/opacity/blur** (DONE 2026-07-18c, gate `filter`) — `lib/web/css/cascade.ad` (`_filter_value` packs `(type<<20)|amount`; full parallel property `d_filter`/`m_filter`/`r_filter[]` reset+specificity-tracked like `border-radius`) → `lib/web/layout/box.ad` (`box_filter_stack` + `bfill_filter[]` stamped at all four bfill emit sites, zero reflow) → `lib/web/dom/canvas.ad` (`he_bfill_filter`) → `lib/htmlpage.ad` (per-pixel POST-PASS over each filtered box AFTER all painting, so element+subtree filter together) → `lib/htmlpaint.ad` (`htmlpaint_filter_rect`: colour-matrix/scale transforms + separable box blur). Verified HOST-ONLY render-to-PNG (`scripts/test_hambrowse_filter_host.sh`, fixture `hambrowse_filter.html`, probe `hb_filter_probe.py` whole-image scan: each box's original colour GONE + expected filtered colour PRESENT). Zero regressions (borderradius/boxshadow/website green). LIMITATIONS (honest): ONE filter function per element (list collapses to first); `drop-shadow()` recognised but no-op; `opacity()` approximated as lighten-toward-white (no backdrop in static render); no `hue-rotate`/`backdrop-filter`; covers the element BORDER BOX; internal-fb path only. See the CSS-filter round header above for the full write-up.
- **[css-flexbox / css-backgrounds-borders] flex/grid CONTAINER background paints BEHIND its items (stacking fix)** (DONE 2026-07-18, gate `flexbg`) — `lib/web/layout/box.ad` (`_box_close`: a box whose own background fill ENCLOSED any descendant fill — `bf0 < n_bfills` at close — now grades its `bfill_z` to a NEGATIVE, depth-keyed value `box_sp - 1000` so shallower containers paint first / deepest behind, while leaf fills keep `z 0` on top) + `lib/htmlpage.ad`/`scripts/render_hambrowse_png.py` (the fill paint pass already stable-sorts by z; the host dump + dev visualizer now carry z so the PNG mirrors it). Fixes the on-device visual-QA bug where a `display:flex` nav with a `background` rendered its links but NOT its background band, and — the same root cause — a coloured flex/grid container over-painted the backgrounds of its own item chips (and any nested block background), because fills are emitted in CLOSE (post-)order (a child appends its fill BEFORE its parent) yet painted in array order, so the LAST-appended container fill landed on TOP. This is the "known z-order limit of the equal-z bfill sort" the `<button>` work noted, now resolved: CSS painting order is parent-background-first, child-backgrounds-on-top. Positioned boxes still get their real `z-index` stamped later in `lib/web/layout/flow.ad` (overriding this grade), so the `position`/`abspos` stacking gates are unaffected. Verified HOST-ONLY render-to-PNG on the REAL pixel backend (`scripts/test_hambrowse_flexbg_host.sh`, fixture `hambrowse_flexbg.html`, probe `hb_flexbg_probe.py` reading the framebuffer PPM): a dark flex nav paints a 600px-wide `#333333` band behind its links; a `justify-content:space-between` flex container whose two items carry `#cc0000`/`#00aa00` backgrounds shows the container band `#446688` (n≈9200 px) BETWEEN the chips AND both chip colours ON TOP; a `display:grid` container paints its `#224488` band behind its cells. Zero regressions across position/absinline/dispflex/flexgap/flexwrap_qa/grid/realsite/elembg/cssvalues/fidelity/button/transform. LIMITATION (honest): the depth grade is applied within the default (z 0) flow; a positioned subtree flattens to its own `z-index` (all its fills share that z), so container-behind-item ordering inside a positioned box relies on emission order there — the common flow/flex/grid nav case is correct.
- **[css-flexbox] flex-basis:auto = used `width` + padded flex items measured at BORDER-box** (DONE 2026-07-18, gate `website`) — QA-by-using round driving a REALISTIC modern SEARCH-RESULTS page (`tests/fixtures/hambrowse_website.html`: header logo+nav+search, breadcrumb, a 220px filter SIDEBAR beside a `flex:1` RESULTS column, product CARDS with thumb+title+desc+badge/pill/price META row, pagination) at a real 1000px window, comparing element-by-element to Firefox/Chrome on the HOST render-to-PNG. Two highest-visual-impact flexbox divergences fixed in `lib/web/layout/box.ad _flex_grow_scan`: **(1) TWO-COLUMN COLLAPSE** — a flex item with an explicit `width` but no `flex-basis` (the canonical `.filters{width:220px}` sidebar beside `.results{flex:1}`) was sized by its measured CONTENT width, which for a content-heavy sidebar overflowed the row and collapsed the `flex:1` sibling to a right-edge SLIVER (every product card became a thin vertical bar). Per CSS Flexbox §7.2.3 `flex-basis:auto` resolves to the item's used `width`; the scan now seeds `flex_grow_b[ci]` from the cascade winner `m_wd` when no explicit basis is set (explicit `flex-basis`/`flex` shorthand still wins). The sidebar now holds ~220px and the results column fills the remaining width. **(2) PADDED FLEX ITEMS TOUCH / `gap` VANISHES** — `_flex_measure_children` measured a flex item's main size as its bare GLYPH width (`chars*CELL_W`), excluding padding+border, but the item was then PAINTED wider (padding drawn outward), so a padded badge/pill/nav-link/pager control overran into the author `gap` and its neighbour (`Top ratedIn stock$199` jammed together). The scan now folds each item's horizontal padding + border (`m_padx + 2*m_bordw`, the same border-box quantities `box-sizing` uses) into `flex_meas_w[ci]`, so the natural/justify path and the grow path both space neighbours by their real border-box width PLUS the gap. Verified HOST-ONLY render-to-PNG (`scripts/test_hambrowse_website_host.sh`, PIXEL-asserted from the paint stream): the filter sidebar holds 236px (`width:220px`+border, was ~528px pre-fix) and the results card thumb sits to its RIGHT at 308px wide (was a ~24px sliver); a controlled `display:flex; gap:8px` row of two `padding:3px 9px` pills places the 2nd pill at +98px = 72px glyphs + 18px padding + 8px gap (was overlapping at +72px). Zero regressions across position/absinline/dispflex/flexbg/flexwrap_qa/flexgap/flexjustify/flexnav/grid/borderradius/fidelity/wordwrap/realsite/realarticle/nesttable/objectfit. DEFERRED (documented, not faked): the engine's UA **readable-measure cap** (`lib/web/state.ad` `MEASURE_MAX=584`) used to CENTRE all body content in a 584px column on windows wider than 600px, so full-bleed site chrome (edge-to-edge header/footer) and 2-per-row card grids rendered narrower/centred vs Firefox — **NOW FIXED for real sites, see the `fullwidth` entry below** (the cap is scoped to plain prose and disabled for any flex/grid/positioned page); `margin-left:auto` on a flex item (pushing a price to the far right) is not yet honoured in the natural/grow path; a flex item's `min-width:0` default (so a very long word can shrink a `flex:1` column below its content) is not modeled.
- **[css-display / rendering-fidelity] readable-measure cap SCOPED — real sites render FULL-WIDTH like Firefox/Chrome** (DONE 2026-07-18, gate `fullwidth`) — `lib/web/state.ad` (`g_layout_full` flag + `_page_uses_layout` source scanner + `_page_gutter` gate) + `lib/web/layout/flow.ad` (`_reset_layout` computes `g_layout_full` BEFORE baking the initial content column). Fixes the single biggest real-site divergence from Firefox/Chrome: the engine capped EVERY page's content to a **584px readable-measure column CENTRED in the window** (`MEASURE_MAX=584` via `_page_gutter`/`_left_margin`/`_content_right`), so a real website — full-bleed nav bar, multi-column flex/grid, sidebars, cards — rendered as a narrow strip floating in a wide window, nothing like a real browser (which renders the body at the FULL viewport width and lets the page's own CSS narrow any column). **The fix is SCOPED, not a blanket removal**: the readable gutter is KEPT for a plain-prose page (bare `<p>`/`<h*>`/`<ul>` text with no author layout) so long lines stay comfortable, but is DISABLED for any page that establishes an author layout context — `display:flex`/`display:grid` (incl. the inline-* variants) or `position:absolute`/`position:fixed` anywhere in its styles (the unambiguous signals the author is driving their own full-width layout). `_page_uses_layout` scans the raw source once per layout (matching both `<style>` rules and inline `style=""`) for those property:value pairs; `_reset_layout` sets `g_layout_full` from it before `pen_x`/`indent_x`/`blk_right_cur` are baked from `_left_margin()`/`_content_right()`, so there is NO chicken-and-egg with the gutter. A classic prose article with a `float`ed figure or an explicitly-sized image is NOT a trigger, so it keeps the readable measure (matching how it read before). Verified HOST-ONLY render-to-PNG (`scripts/test_hambrowse_fullwidth_host.sh`, fixtures `hambrowse_fullwidth_site.html` + `hambrowse_fullwidth_prose.html`, PIXEL-asserted): at a 1000px window the flex site's full-bleed nav background spans x=24..976 (952px wide, was the 208..792 / 584px centred strip pre-fix) and its 3-card flex row fills the width (cards at 24, 342, 660 → right edge 974); the plain `<p>`-only page STILL centres its text at the readable measure (left gutter ddx=208, right edge maxx=783 ≪ 1000). On the existing `hambrowse_website.html` the topbar nav band went from 224..776 (centred strip) to 24..976 (full-bleed) and the 3 cards from a single collapsed column to a full-width flex-wrap row. Gate coords updated for the same reason (more-correct = matches Firefox): `dispflex` (block/flex box FILL now `0..800` full-body-width, was `100..700`) and `grid` (the `1fr` tracks resolve against the full 624px content width so the fr step is 212px, was 192px; the fixed 200px rail step is unchanged at 220px). UNCHANGED (no trigger, byte-identical): `realarticle` (float+width only), `fidelity`, `objectfit`, `nesttable`, `border`, `wordwrap`/`flexwrap_qa` (render ≤600px where the gutter is already 0). `position`/`absinline`/`website`/`flexbg`/`realsite` shifted to full-width but their assertions are RELATIVE (deltas / right-of-sidebar / behaviour) so stayed green with no edit. Zero assertions weakened. LIMITATIONS (honest): the trigger is PAGE-LEVEL (one flex/grid/positioned element switches the whole page to full width) rather than per-subtree, and a page whose ONLY layout signal is an explicit `width`/`max-width` or a `float` (with no flex/grid/positioning) keeps the readable measure — a deliberate choice so classic prose-with-figure articles stay readable and the gate ripple is minimized; the tiny false-positive risk of a prose page literally containing the text `display:flex`/`position:absolute` (→ rendered full-width) is accepted.
- **[css-fonts / rendering-fidelity] `<em>`/`<i>` + CSS `font-style:italic` render FAUX-OBLIQUE** (DONE 2026-07-18, gate `emitalic`) — new `seg_italic` channel: `lib/web/layout/box.ad` (`seg_italic` array + `g_italic_n` nesting depth; folded into the seg at emit alongside `seg_strike`, from `g_italic_n>0` OR the CSS `g_css_italic`), `lib/web/dom/forms.ad` (`<em>`/`<i>` open/close bump `g_italic_n` instead of stripping the tag), `lib/web/css/cascade.ad` (full `font-style` plumbing mirroring `font-weight`: `d_fstyle`/`r_fstyle`/`m_fstyle`, a `fstyle_stack` + `g_css_italic`, parsed in the stylesheet + inline paths, pushed/popped in `_open_style`/`_close_style`), `lib/web/dom/canvas.ad` (new `he_seg_italic` accessor), and the PAINT: `lib/htmlpaint.ad` (`_blit_ttf_glyph` shears each glyph row by `(baseline - y)/4` when `g_ttf_shear` is armed — top pushed right of the baseline, descenders pulled left) wired by `lib/htmlpage.ad` (arms the shear around the text draw for `he_seg_italic` segs, resets after). Closes the `fidelity`-round DEFERRED item: emphasis no longer renders upright. **This is a synthesised faux-oblique — a horizontal SHEAR of the single shipped bitmap face — NOT a true italic typeface** (the engine ships one face; emphasis is conveyed by slanting the existing glyphs, ~0.25 / ~14°). Verified HOST-ONLY render-to-PNG on the REAL pixel backend (`scripts/test_hambrowse_emitalic_host.sh`, fixture `hambrowse_emitalic.html`, probe `hb_emitalic_probe.py`): the SAME `llllllll` rendered plain vs `<em>` vs `<i>` vs `font-style:italic` — the plain line's top-vs-bottom ink centroid slant is ~0 (−0.12) while every italic line slants a clear +1.88 px (top sheared right) and stays legible; a mixed `Normal <em>emphasized</em> normal` paragraph shows the emphasis slanted while surrounding text stays upright. Zero regressions across position/absinline/dispflex/flexgap/flexwrap_qa/grid/realsite/elembg/cssvalues/fidelity/button/decimlen/clearrebuild/liclass. LIMITATIONS (honest): faux-oblique (shear), not a real italic face — letterforms are the upright shapes slanted, not true italic glyph designs; the run's ADVANCE widths are unchanged (the shear is paint-only, so italic and upright text occupy the same horizontal box — no italic-specific kerning/metrics); the leftmost sheared column of the very first glyph after a hard left margin can clip by a pixel or two.
- **[css-backgrounds-borders] plain `<h1>`/`<h2>` has NO default underline/border** (DONE 2026-07-18, gate `h1noborder`) — `lib/htmlpage.ad` (heading/`<hr>` rule pass, section 5). Fixes an on-device visual-QA bug where a plain `<h1>`/`<h2>` (no CSS border/underline/hr declared) rendered a SPURIOUS thin full-width light-grey hairline directly beneath it, mimicking an unwanted default `border-bottom`. Root cause: the engine's legacy per-row "heading underline" flag (`row_rule==1`, set for h1/h2 in the DOM layer `lib/web/dom/forms.ad`) was painted by `htmlpage.ad` as a 1px `rgb(170,170,170)` rule across the content width. Real browsers give a heading only larger BOLD type + margins — no default border. The paint pass now DELIBERATELY skips `row_rule==1` (only the real `<hr>` divider `row_rule==2` still paints its 2px `rgb(120,120,120)` rule); the underline flag remains harmless in the record set and a GENUINE CSS heading border still draws via the box/border pass (`he_n_bboxes`, section 6), so nothing that should have a border lost it. Verified HOST-ONLY render-to-PNG (`scripts/test_hambrowse_h1noborder_host.sh`, fixture `hambrowse_h1noborder.html`, probe `hb_h1noborder_probe.py`): the empty pixel band below each heading has ZERO full-width dark rule (was 784/808 dark px at the underline row pre-fix, now 0), while the heading row itself stays large (taller than body rows) + inked (bold/large preserved). Zero regressions across position/realsite/elembg/borderradius/gradient.
- **[css-positioning] Negative top/left/right/bottom offsets** (buggy) — `lib/htmlengine.ad _style_len line 3237-3260` — _style_len never parses a leading '-', so `top:-10px`/`left:-9999px`/negative-margin centering return -1, which is the same sentinel used for `auto` (see d_top>=0 checks at 6227-6238 and pos_l[i]>=0 a
- **[css-positioning] CSS `clear` (clear:left / right / both)** (DONE 2026-07-18, gate `clear`) — `lib/web/css/cascade.ad` (new `r_clear`/`d_clear`/`m_clear` — parsed by the `clear` property branch: `both`=3 / `left`=1 / `right`=2 / `none`=0; stored per rule, resolved to the winner with its own specificity tracker `scl`, inline `style=` overriding the sheet exactly as `float` does) + `lib/web/layout/box.ad` (`_apply_clear` called at the top of `_block_box_open` and, before the float-top snapshot, in `_float_box_open`). A `clear`ed block drops `cur_row` to ONE ROW PAST the bottom of any active same-side float exclusion (`floatL_bottom`/`floatR_bottom` are the float's last occupied row, so the cleared box starts on the float's bottom edge), retires that exclusion, and resets `float_left_pad`/`float_redge` so the block flows at the FULL-WIDTH left margin instead of beside the float. `clear:both` drops below the deeper of a simultaneous left+right pair. Zero-cost for clear-free pages (`m_clear`/`d_clear` default 0 → `_apply_clear` returns after one compare; `m_clear` reset per element cascade so no stale carry). Verified by element-background FILL pixel-row asserts (fixture `hambrowse_clear.html`): a plain paragraph flows BESIDE a tall float:left (control) while a `clear:left` block drops to the float's bottom edge at the left margin; `clear:right` drops below a float:right aside; `clear:both` drops below both floats of a left+right sandwich. FIDELITY / LIMITATIONS (honest): applies to blocks that open a box (`div`/`p`/generic block — the common `<div style="clear:both">` idiom); a float already scrolled past (auto-retired by `_maybe_clear_float`) is not re-dropped-below (correct — flow already cleared it); no `clear` on inline / table-caption elements; the drop uses the float's laid-out bottom (no separate clearance/margin-collapse computation).
- **[css-tables / rendering-fidelity] `border="1"` per-cell grid lines + blockquote left accent bar** (DONE 2026-07-18, gate `fidelity`) — QA-by-using round driving a RICH realistic article (`tests/fixtures/hambrowse_fidelity.html`) toward how Firefox/Chrome render the SAME unstyled markup, comparing element by element on the HOST render-to-PNG. Two highest-visual-impact divergences fixed: **(1) TABLE GRID** — a bordered table used to stroke ONLY an outer frame (cells had no borders), which reads nothing like a real browser. Now EACH `<td>`/`<th>` registers its own border box, so the table paints full internal row+column grid lines (`lib/web/dom/forms.ad` latches the cell's `[col_x[cc], col_x[endc]]`×`cur_row` on open when `tbl_border_st[]` is set; `lib/web/layout/tables.ad` `_cell_close` strokes a `bbox` spanning `[srow, cur_row+1]`; the latch is saved/restored across the nested-table stack in `_table_push`/`_table_pop`). Cells stroke a **medium-grey `#808080`** grid; the outer table FRAME stays **black `#000000`** (drawn last → wins the shared edge) — the classic inset-cell/darker-frame look. **(2) BLOCKQUOTE ACCENT** — a `<blockquote>` now paints a thin (`BQ_BAR_W`=4px) **left accent bar** (`#8f9cb4` blue-grey `bfill`) down its full row span in the gutter (`lib/web/layout/box.ad` `bq_srow_st`/`bq_barlx_st` per-level stack, latched on open, emitted on close in `lib/web/dom/forms.ad`), the recognizable article/blog pull-quote treatment. Verified HOST-ONLY render-to-PNG (`scripts/test_hambrowse_fidelity_host.sh`, PIXEL-asserted from the framebuffer): ≥6 grey per-cell strokes + exactly 1 black frame that geometrically CONTAINS the cells + ≥2 distinct column x's (vertical rules); a 4px×44px accent bar in the accent colour; plus ul disc bullets inked & hanging, ol `1. 2. 3.`, and preserved `<pre>` leading whitespace. The pre-existing `nesttable` gate was updated (still green) to the new model: it now counts BLACK table FRAMES (inner+outer) and asserts grey cell grid strokes, instead of asserting every border edge is black. DEFERRED (documented, not faked): **row-height equalization** — each cell's border spans only its own content rows, so if ONE cell in a row wraps taller than its siblings the grid is locally uneven (fine for the common single-line/similar-height rows; true table row-height balancing is a bigger layout pass); **`<em>` italic** — NOW DONE (gate `emitalic`, 2026-07-18): a `seg_italic` channel + `he_seg_italic` accessor were added and emphasis renders as a faux-oblique shear (see the dedicated entry above); **heading dark-blue + pre/code teal** are deliberate opinionated UA-palette choices kept as-is (Firefox uses black), not regressions; `border-collapse`/`border-spacing`/per-side cell padding control and a darker beveled 3D frame are not modeled.
- **[css-positioning] Multiple simultaneous floats + float containment** (partial) — `lib/htmlengine.ad _float_box_open line 1462-1495 (float_active guard 1464)` — Only one float exclusion is active at a time; a second concurrent float (left+right pair, image grids, wrapping figures) falls back to an in-flow block, so multi-float layouts collapse. Also no `overf
- **[css-values-units] CSS custom properties + var()** (DONE, gates `cssvalues`/`varfallback`) — `lib/web/css/cascade.ad`: `--name: value` declarations are harvested from every `<style>`/external sheet in a pre-sweep (`_collect_css` ~6175 → `cvar_*` store, inherited down the tree) and `prop: var(--name[, fallback])` is resolved in `_apply_decl` (~4974): the custom property's value is spliced into a scratch buffer and re-dispatched as `<property>:<value>`, nested `var()` inside the value/fallback resolves, and an undefined name with no fallback drops the declaration. Colour AND length tokens both work (Bootstrap 5 / Material / GitHub design tokens). Verified host: `var(--pad-w:160px)`->176px box, `var(--ink:rebeccapurple)` text, `var(--missing,#cc0000)` fallback, and a child reading a parent custom property. The old monolith reference is stale; this lives in the modular tree.
- **[css-values-units] calc()** (DONE 2026-07-20, gates `calc`/`cssvalues`) — `lib/web/css/cascade.ad`: a recursive-descent evaluator `_calc_expr`/`_calc_term`/`_calc_factor` (~1616/1597/1574) reached from `_style_len` (~1642) on any `calc(...)` value. `expr := term (('+'|'-') term)*`, `term := factor (('*'|'/') factor)*`, `factor := '(' expr ')' | min()/max()/clamp() | var() | number`, so operator precedence (`*`/`/` before `+`/`-`), parentheses, and nesting all work. Each numeric operand resolves through the SAME unit machinery as plain lengths (`_len_number`→`_len_apply_unit`): px/%/em/rem/vw/vh/vmin/vmax/ch/pt; `%` resolves against the property's containing dimension (content column). `var()` occurring inside calc is substituted via `_calc_var` (~1574) BEFORE evaluation (swaps the cursor onto the custom-property value buffer and evaluates it as a sub-expression), so `calc(100% - var(--g))` and `var(--x, calc(1px + 2px))` both work. Host gate `scripts/test_hambrowse_calc_host.sh` (fixture `hambrowse_calc.html`) pins concrete pixels at 800x600 (584px content column): `100px+50px`->150, `100%-40px`->544, `10px*3`->30, `2rem+10px`->42, precedence `10px+2px*5`->20, nested `(10px+2px)*2`->24, `50vw-100px`->300, and `100%-var(--g:8px)`->576. SIMPLIFICATIONS (first cut): integer-pixel arithmetic (operands are resolved to px THEN combined, so fractional intermediates truncate, matching the engine's integer layout); NO CSS type checking — `*`/`/` do not enforce that one side is unitless and two lengths multiply numerically rather than erroring; the whitespace-around-`+`/`-` disambiguation is not enforced (a `-` is always an operator here, and a leading sign inside a term is consumed by `_len_number`), which is correct for spec-conformant input but does not reject the malformed `calc(100% -40px)`; on a parse failure the value falls back to auto/-1 without crashing. The old monolith reference is stale; this lives in the modular tree.
- **[css-values-units] rem unit** (DONE, gate `cssvalues`) — `lib/web/css/cascade.ad _len_apply_unit` (~1457) matches `rem` BEFORE `em` (longest-unit-first, so `rem` is not shadowed by the `em` prefix) and resolves it against the root font-size `EM_PX` (default 16px). Host-verified `width:10rem` -> 160px (176px painted box). The old monolith reference is stale; this lives in the modular tree.
- **[css-values-units] Viewport units vh/vw/vmin/vmax** (DONE 2026-07-20, gate `viewport`) — `lib/web/css/cascade.ad _len_apply_unit` (~1445-1471) resolves `vw`/`vh` against the render viewport dimensions `bw`/`bh` (`lib/web/state.ad` defaults 880x600), `vmin` against the smaller axis and `vmax` against the larger (`vmin`/`vmax` tested before their `vw`/`vh` prefixes). Host gate `scripts/test_hambrowse_viewport_host.sh` (fixture `hambrowse_viewport.html`) pins concrete pixels at 800x600: `50vw`->400px, `50vh`->300px (distinct axis), `50vmin`->300px, `50vmax`->400px. REMAINING: no small/large/dynamic `svh`/`lvh`/`dvh` variants (headless model has no dynamic UI-chrome viewport); a viewport-derived block width still clamps to its containing block (e.g. `100vh` as a width caps at the content column). The old monolith reference is stale; this lives in the modular tree.
- **[css-values-units] Fractional/decimal lengths (e.g. 1.5em, 12.5px, 0.5rem)** (buggy) — `lib/htmlengine.ad ~3243-3250 (_style_len integer loop breaks on '.')` — `_style_len` reads only [0-9]+ and stops at '.', so `font-size:1.5em`, `padding:0.75rem`, `width:33.3%`, `margin:1.5em` all truncate to the integer part (1.5em -> 1em -> 16px; 0.75rem -> 0). Only `_li
- **[css-text-fonts] font shorthand** (missing) — `lib/htmlengine.ad _apply_decl` — no font shorthand case
- **[css-backgrounds-borders] linear-gradient() / radial-gradient() background images** (DONE 2026-07-17, gate `gradient`) — `lib/web/css/cascade.ad _parse_gradient + gradient registry + _pack_gradient (marker in bfill_rgb bits 24-28==31) + lib/htmlpage.ad fill loop (grad_is detect -> gather stops -> htmlpaint_fill_gradient) + lib/htmlpaint.ad htmlpaint_fill_gradient per-pixel rasteriser`. `background`/`background-image: linear-gradient(<angle|to side>, c0..cN)` and `radial-gradient(<shape>, c0, cN)` now paint a real interpolated colour ramp (respecting border-radius) instead of leaving the element transparent. The gradient's direction + 2..8 stops live in a side registry whose ID rides through the untouched `layout/box.ad` record set under a marker that a solid fill can never emit. Verified by interpolated-pixel asserts (left-red/mid-purple/right-blue, vertical fade, a 135deg diagonal, a 3-stop lime midpoint, radial white->black). REMAINING: radial is centre->farthest-corner only (no `at`, size, or circle-vs-ellipse); the gradient must lead the `background` shorthand value; inline `style=` gradients unsupported; sRGB (not linear-light) interpolation; mixed explicit/auto stop positions approximated (autos spaced evenly). conic-gradient + repeating gradients now landed (gate `gradient2`, below).
- **[css-backgrounds-borders] border-color (per-element) honored on non-table boxes** (buggy) — `lib/htmlengine.ad line 1443 (bbox_rgb[n_bboxes] = 0) and the border branch in _box_decl (~3552-3560) which sets d_bd=1 but never captures the declared color` — The `border` shorthand is reduced to a boolean d_bd; the color token is discarded and the block-box registry hardcodes bbox_rgb=0 (black). Only tables (line 6951) pass a real color. Result: every `bor
- **[css-backgrounds-borders] border-radius (rounded corners)** (DONE 2026-07-20, gate `borderradius`) — MODULAR tree, NOT the stale `lib/htmlengine.ad`. Parsed in `lib/web/css/cascade.ad` (`_box_decl` `border-radius` + `_border_side_dispatch` `-radius` longhand → `d_bordr`; specificity-tracked through `m_bordr`/`r_bordr[]` like every other box property), threaded with zero reflow cost through `lib/web/layout/box.ad` (`box_bordr_stack` → the border-box `bbox_rad` and background-fill `bfill_rad` records) and `lib/web/dom/canvas.ad` (`he_bbox_radius`/`he_bfill_radius`), and rasterised by `lib/htmlpage.ad`'s fill + border-stroke loops calling `lib/htmlpaint.ad htmlpaint_fill_round_rect` (solid fill clipped to the rounded rect; gradients/images already clipped via `_hp_clamp_rad`) and `htmlpaint_stroke_round_rect` (the 1px frame traced around the arc). The radius is clamped to half the smaller box dimension (`_hp_clamp_rad`, spec overlap rule). A uniform `border-radius: <len>` in px/%/`em`/`rem`/`vw` (reuses `_style_len`) rounds BOTH the background fill and the border. Verified HOST-ONLY (`scripts/test_hambrowse_borderradius_host.sh`, fixture `tests/fixtures/hambrowse_borderradius.html`): FILL/BBOX radius+colour text dump PLUS a real corner-cut PIXEL probe (`scripts/hb_borderradius_probe.py`) — a large-radius solid `#0040ff` box has its four CORNER pixels at the page background (fill does not reach them) while the CENTRE and four edge midpoints ARE the fill, proving the corners are genuinely cut. DEFERRED: per-corner `border-radius: a b c d` and the `/` (horizontal `/` vertical) two-radii ellipse syntax parse only the FIRST length (uniform radius applied to all four corners); rounded PER-SIDE borders with DIFFERENT colours still stroke square-cornered arc segments (see the per-side deferral in the OPEN worklist).
- **[css-backgrounds-borders] box-shadow** (DONE v1 2026-07-17, gate `boxshadow`) — `lib/web/css/cascade.ad _box_shadow_class (class packed into the fill colour bits 29-30) + lib/htmlpaint.ad htmlpaint_shadow_round_rect + lib/htmlpage.ad fill loop (paints the shadow before the fill)`. A `box-shadow` declaration now paints a dark, feathered, offset drop shadow behind the element's border-box, giving cards/menus/modals depth. The feather stacks `blur`+1 alpha-blended concentric rounded rects (reusing the border-radius + `_blend_px` rasteriser). REMAINING: single shadow only (no comma-separated list), no `inset`, and the offset/blur/colour are a size-scaled default rather than the exact declared values — the per-element shadow geometry cannot be threaded through the `layout/box.ad` + `dom/canvas.ad` block-fill record set (off this track's ownership), so a 2-bit size class is smuggled through the packed fill colour instead; only elements that also carry a background get a shadow.
- **[css-backgrounds-borders] opacity / alpha compositing (including rgba()/hsla() alpha)** (DONE 2026-07-17, gate `boxshadow`) — `lib/web/css/values.ad (g_cv_alpha + _parse_alpha_frac capture rgba()/hsla()/#rrggbbaa alpha) + lib/web/css/cascade.ad (opacity decl + _pack_fill_rgb/_pack_bordc fold alpha×opacity into the packed colour) + lib/htmlpage.ad (_hpg_alpha unpack) + lib/htmlpaint.ad (htmlpaint_fill_round_rect_a / stroke_round_rect_a over _blend_px)`. `opacity:X` and rgba/hsla alpha are honoured: the element's fill and border composite at the declared alpha over the background instead of rendering opaque (`opacity:.5` #3060c0 → the (155,178,225) 50% blend; `rgba(255,0,0,.5)` → pink, not red). The alpha rides bits 24-28 of the existing `bfill_rgb`/`bbox_rgb` int (bit 31 kept 0 so it survives every `>=0`/`-1` guard) so NO layout-record schema change was needed. REMAINING: dims the element's own fill+border only, not its text/descendants (no stacking-context group opacity); 32-level alpha quantisation; opacity must be declared in the same rule as the background it dims; inline `style=` background/border alpha not folded.
- **[css-backgrounds-borders] conic-gradient + repeating-linear/radial/conic-gradient + background-image: url(...)** (DONE 2026-07-17, gate `gradient2`) — `lib/web/css/cascade.ad (_parse_gradient extended: conic gtype 2 via _conic_dir `from <angle>`, a grad_repeat flag for repeating-*; _parse_bg_url: gtype 3 url registry) + lib/htmlpaint.ad (_hp_conic_t diamond-angle, _hp_gwrap stop tiling, a repeat param on htmlpaint_fill_gradient, htmlpaint_blit_image_bg image background) + lib/htmlpage.ad (grad_type 3 -> htmlimg_find -> blit; repeat plumbed to fill_gradient)`. `conic-gradient` paints an angular ramp around the box centre; `repeating-*-gradient` tiles its stop pattern; `background-image: url(<path>)` blits a decoded raster/SVG image (scaled-to-fill, border-radius clipped) matched by name against the shared image store. All ride the existing `GRAD_MARK` packing through the untouched `layout/box.ad`. Verified by pixel asserts: a conic red/lime/blue wheel (four distinct angular hues), a 4-tile repeating-linear ramp (recurs one 25% period later), a 2x2 quadrant PNG background (decoded red/green/blue/yellow). REMAINING: a CSS-only `url()` only appears once its image is decoded (the `layout/box.ad` image-record pass, off this track's ownership, scans only `<img src>`, so a url currently needs a matching `<img>` to trigger decode); conic `at <pos>` offset + non-`%` stop positions unapplied; radial still centre->farthest-corner. `background-size`/`position`/`repeat` now landed (gate `bgsize`, below). SUPERSEDES the missing-item below.

- **[css-backgrounds-borders] background-size / background-position / background-repeat** (DONE 2026-07-17, gate `bgsize`) — `lib/web/css/cascade.ad (_parse_bg_size/_parse_bg_pos/_parse_bg_repeat + per-gid grad_bg_* placement registry, folded onto the url gid at rule-store time; _bg_num/_bg_next_tok token parsers) + lib/htmlpaint.ad (htmlpaint_blit_image_bg extended with size/position/repeat params + _bg_map per-axis tile mapping) + lib/htmlpage.ad (grad_bg_*_of accessors plumbed into the blit)`. `background-size: cover|contain|<w> [<h>]|auto`, `background-position: <keyword|%|px> [<...>]` and `background-repeat: repeat|no-repeat|repeat-x|repeat-y` now size, place and tile a decoded background image inside the padding box (border-radius clipped). Verified by pixel asserts on a wide solid-red image (cover fills, contain letterboxes, no-repeat+center centres one block, repeat-x tiles a top band). A plain `background-image` with none of the three keeps the legacy scale-to-fill (preserving `gradient2`). REMAINING: single background layer only (no comma-separated multi-layer); no 3/4-value position offsets; percentages resolved simply against the box.
- **[css-images / css-sizing] object-fit + object-position (replaced-element sizing) + aspect-ratio** (DONE 2026-07-18, gate `objectfit`) — `lib/web/css/cascade.ad (_box_decl parses object-fit/object-position/aspect-ratio; _objfit_value/_parse_obj_pos/_parse_aspect_ratio; d_*→r_*→m_* cascade with per-property specificity; _img_cascade transient void-element match) + lib/web/layout/box.ad (_handle_img resolves the <img> cascade + stores seg_objfit/seg_objp{x,y}{,p}; he_seg_objfit… accessors; _block_box_open derives height from aspect-ratio) + lib/htmlpaint.ad (htmlpaint_blit_image_fit sizes/positions the bitmap: contain letterbox / cover crop / none / scale-down + _objpos_off) + lib/htmlpage.ad (routes non-fill fits to the new blit)`. `object-fit: fill|contain|cover|none|scale-down` + `object-position: <keyword|%|px>` size and place an `<img>`'s bitmap within its content box (fill = legacy stretch; contain letterboxes with the box bg in the bars; cover crops; none/scale-down natural). `aspect-ratio: <w>/<h>` (or a single number) derives a definite-width box's AUTO height (pinned like an explicit `height`). Verified HOST-ONLY render-to-PNG (2:1 green/red image: cover fully covers, contain shows white bars, object-position:left/right shows green/red halves; aspect-ratio:2/1+width:200 matches an explicit height:100px box; ellipsis block renders `...`). HONEST SCOPE: aspect-ratio derives height-from-width only; object-fit/position apply to `<img>` (inline `<svg>` draws fill); `<img>` box sizing still comes from HTML width/height attrs; no 3/4-value object-position offsets. See item 9f above for full detail.
- **[css-backgrounds-borders] background-image: url(...) (raster/SVG image backgrounds), background-position/size/repeat** (blit + placement DONE, see the `gradient2` and `bgsize` entries above; only CSS-only decode-recording REMAINS) — `lib/htmlengine.ad background-value parse (~2922-2953) + image path already exists via htmlimg for <img> in lib/htmlpaint.ad` — background-image:url() is now recognized, placed (size/position/repeat) and blitted when its image is in the store. Logos, hero photos, and CSS-only iconography still vanish when referenced ONLY from CSS (no `<img>` to drive the decode). The engine already decodes images for <img
- **[html-canvas] Canvas 2D API (`<canvas>.getContext('2d')` + drawing)** (DONE, gates `canvas` + `canvas2d_host` 2026-07-18) — `lib/web/dom/canvas.ad` (JS-facing context: `_canvas_make_ctx` builds the 2D context object + native methods; the `NID_CV_*` dispatch reads style props back and forwards resolved ints/colours) + `lib/htmlcanvas.ad` (the pixel backend — each `<canvas width= height=>` gets an RGBA backing buffer that is an `lib/htmlimg` slot, so the page renderer composites it exactly like an `<img>` at the canvas box origin) + `lib/htmlpaint.ad`/`lib/htmlpage.ad` (the backing buffer blits into the page raster; `_handle_canvas`/`_emit_img_seg` reserve the box). `canvas.getContext('2d')` returns a context bound to a fresh backing bitmap sized to the width/height attributes. Implemented: **rects** `fillRect`/`strokeRect`/`clearRect`; **style** `fillStyle`/`strokeStyle` (named colours, `#rgb`/`#rrggbb`; `rgb()/rgba()` fall through the CSS colour path), `lineWidth`, `globalAlpha` (translucent fills + drawImage src-over-composite); **paths** `beginPath`/`moveTo`/`lineTo`/`rect`/`arc`/`closePath`/`fill` (scanline polygon fill via `htmlpaint_fill_poly`)/`stroke` (`arc`→64-step line-segment approximation, documented); **text** `fillText`/`strokeText`/`measureText` (AA TrueType via `htmlpaint_text_ttf`, `font` px+bold parsed, `textAlign` best-effort); **`drawImage(img, dx, dy[, dw, dh])`** (NEW 2026-07-18) — nearest-neighbour blit of an already-decoded source scaled to the dest rect, src-over honouring source alpha × globalAlpha. drawImage source is resolved to an htmlimg slot from a `<canvas>` element (its backing buffer), an `<img>` element (its `src=` attribute → decoded slot), or an `Image()`/any object carrying a resolvable `.src`. Verified HOST-ONLY render-to-PNG (`scripts/test_hambrowse_canvas2d_host.sh` + `scripts/hb_canvas_probe.py`, fixture `tests/fixtures/hambrowse_canvas2d.html`): a page `<canvas width=200 height=150>` draws a red fillRect square, a blue hollow strokeRect, a green path-filled triangle, AA `fillText`, and a sprite drawImaged (a 20×20 `<canvas>` scaled 2× to 40×40); the gate SAMPLES the framebuffer so each primitive lands its exact colour at its page-relative coord (canvas box compositing origin applied — e.g. box origin (28,62), the cyan sprite marker scales to its dest sub-rect, magenta body fills the rest, clipped to the dest rect). DEFERRED at the core round, NOW DELIVERED in the advanced round below: gradients as `fillStyle`, context transforms, `getImageData`/`putImageData`. STILL DEFERRED (documented, not faked): patterns (`createPattern`), the 9-arg drawImage source-sub-rectangle form + image smoothing, shadows, clipping paths, and bezier/quadratic curves.
- **[html-canvas] Canvas 2D ADVANCED — gradients, transforms, imageData** (DONE, gate `canvas2d_adv_host` 2026-07-18) — the three features the core round deferred, in `lib/htmlcanvas.ad` (pixel backend) + `lib/web/dom/canvas.ad` (`NID_CV_*` dispatch, new NIDs 73-86 in `dom/bindings.ad`). **Context transforms**: each canvas carries a 2×3 current transformation matrix (CTM); `save()`/`restore()` push/pop the CTM (backend stack) AND the JS style state (`fillStyle`/`strokeStyle`/`lineWidth`/`font`/`globalAlpha` handles, JS-side stack keyed by canvas index); `translate`/`scale`/`rotate`/`transform`/`setTransform`/`resetTransform` post-multiply the CTM (spec order; `sin`/`cos` via a self-contained range-reduced Taylor series — no libm, compiles host + native). Every path point (`moveTo`/`lineTo`/`arc`/`rect`) is mapped through the CTM before storage, and `fillRect`/`strokeRect`/`clearRect` map their corners — so a rotated/scaled rect or path draws correctly (the axis-aligned identity-CTM solid fillRect keeps the fast `htmlpaint` path, byte-identical to the core round → zero regression). **Gradients**: `createLinearGradient(x0,y0,x1,y1)` / `createRadialGradient(x0,y0,r0,x1,y1,r1)` return gradient objects (a numeric `__cvgrad` backend index + `addColorStop(offset,color)`); usable as `fillStyle` on `fillRect`, where a CTM-aware general rasteriser inverse-maps each device pixel to USER space, projects it onto the gradient (linear: onto the axis; radial: distance from the end circle's centre between r0/r1), and interpolates the sorted colour stops (named/`#rgb`/`#rrggbb` via the shared colour parser). **Pixel access**: `getImageData(x,y,w,h)` returns `{width,height,data}` with `data` a plain JS array of clamped RGBA bytes copied from the backing buffer; `putImageData(imgData,dx,dy)` writes them back (device space, CTM-ignored per spec); `createImageData(w,h)` (or from an ImageData) returns a zero-filled buffer. Verified HOST-ONLY render-to-PNG (`scripts/test_hambrowse_canvas2d_adv_host.sh` + `scripts/hb_canvas_adv_probe.py`, fixture `tests/fixtures/hambrowse_canvas2d_adv.html`): a linear-gradient fillRect samples red near stop0 / blue near stop1 / a blended midpoint; a radial gradient samples yellow at centre / purple at the edge; a `translate(150,90)`+`rotate(π/2)` green `fillRect(0,0,40,12)` lands at its transformed device quad (x≈138-150,y≈90-130) while the untransformed origin stays white; an orange rect drawn after `restore()` lands at its literal identity-CTM position (proving save/restore isolated the transform); and a cyan block read via `getImageData` and written via `putImageData` — plus a `createImageData`-built magenta block — render at their target coordinates. FIDELITY / LIMITATIONS (honest): translate/scale/rotate/skew of rects, paths, and gradient fillRects are correct; but glyph (`fillText`) and image (`drawImage`) RASTERS stay axis-aligned — their ANCHOR is mapped through the full CTM and their SIZE scaled by the CTM's per-axis scale, but the run/image itself is not rotated. A gradient used as `strokeStyle`, or as the paint for a path `fill()`/`fillText()`, falls back to the gradient's FIRST colour stop (only `fillRect` does true per-pixel interpolation). `imageData.data` is a plain array, not a real `Uint8ClampedArray` (values are stored unclamped — callers that write in-range bytes round-trip exactly). Backend caps: 16 gradients × 8 stops, CTM save-stack 32 deep, per page render.
- **[dom-core] Layout/geometry APIs: getBoundingClientRect(), offsetWidth/Height/Top/Left, clientWidth/Height, scrollTop/scrollHeight/scrollLeft, offsetParent, getComputedStyle()** (missing) — `lib/htmlengine.ad _attach_el_methods (~9433) / _js_build_document (~11276); no NID_* for any geometry op` — None of these exist on element or window/document. Accessing them yields undefined; el.getBoundingClientRect() throws 'not a function'. Sticky headers, dropdown/tooltip positioning, lazy-load, carouse
- **[dom-core] document.body, document.documentElement, document.head** (missing) — `lib/htmlengine.ad _js_build_document (~11276)` — document exposes getElementById/createElement/querySelector*/forms/title but not body/documentElement/head. Extremely common patterns (document.body.appendChild(...), document.documentElement.classLis
- **[dom-core] Element.closest(selector) and Element.matches(selector)** (missing) — `lib/htmlengine.ad _attach_el_methods (~9433); would reuse _match_chain / tx tree` — Neither is attached to elements. Event-delegation code (e.target.closest('.item'), if(el.matches(sel))) is ubiquitous in modern sites and frameworks and will throw. The selector/tree machinery (_match
- ~~**[dom-events] Event type coverage (keydown/keyup/keypress, mousedown/up/move/over/out/enter/leave, focus/blur/focusin/focusout, scroll, resize, load, DOMContentLoaded, wheel, contextmenu, touch)**~~ **RESOLVED** — line numbers dead (pre-refactor). ANY string event type is now interned into the `GEN_KIND` listener table (`lib/web/dom/bindings.ad:113`); `addEventListener`/`removeEventListener`/`dispatchEvent` resolve string types and bubble the source tree exactly like the 4 legacy kinds. No `ek=-1` drop. Gates: `evttypes` (keydown/mouseover/custom fire + no cross-fire + removeEventListener), `domlifecycle`.
- ~~**[dom-events] document/window addEventListener (DOMContentLoaded, load, scroll/resize)**~~ **RESOLVED (document + window, round 9)** — `document.addEventListener` records under the `DOC_EL` sentinel and `DOMContentLoaded`/`load` FIRE once, in spec order, after the page scripts run (`lib/web/dom/canvas.ad` `_fire_document`, tail of `_run_scripts`). `window.addEventListener('load'|'DOMContentLoaded', fn)` now ALSO fires: `window` (the JS global) is given `addEventListener`/`removeEventListener` in `_js_build_document` and its listeners record under `DOC_EL` too, so they fire with the document lifecycle; `window.onload = fn` fires once after them via `_fire_window_onload` (`this === window`). Gates: `domlifecycle` (document + window sections). REMAINING: `window.dispatchEvent` of a synthetic event is a no-op; `scroll`/`resize` are never dispatched (no scroll/viewport-resize source in the host render).
- ~~**[dom-events] Event and CustomEvent constructors (new Event(type,{bubbles,cancelable}), new CustomEvent(type,{detail}))**~~ **RESOLVED** — `Event`/`CustomEvent`/`MouseEvent`/`KeyboardEvent` are global constructors (`_make_event`, `canvas.ad`); reflect type/bubbles/cancelable, CustomEvent `detail`, MouseEvent clientX/Y/button, KeyboardEvent key. Gates: `domr6`, `domlifecycle` (`new CustomEvent('foo',{detail:42})` round-trip).
- ~~**[dom-events] dispatchEvent of a custom/unknown event type fires no listeners**~~ **RESOLVED** — `NID_DISPATCH` routes an unknown type through `_dispatch_generic`, bubbling the `GEN_KIND` listeners up the source tree. Gates: `evttypes`, `domlifecycle`.
- **[html-forms] FormData constructor + API (new FormData(form), append/get/entries)** (missing) — `lib/htmlengine.ad (no FormData symbol anywhere; would add a native constructor + methods near the DOM/native-method registration ~9431-9477 and JS-side object plumbing)` — There is no FormData at all. Modern forms submit via fetch(url,{body:new FormData(form)}) or read fields with fd.get(name). Every AJAX/single-page form and multipart upload depends on it; its absence 
- **[html-forms] HTTP method=POST submission (and enctype)** (missing) — `lib/htmlengine.ad _serialize_form / _do_submit ~12549-12660` — Submission is hardcoded to GET: _serialize_form always builds an action?name=value query string into the nav buffer regardless of the form's method attribute, and enctype is ignored. Login/signup/chec
- **[html-forms] Constraint Validation API (required, checkValidity(), reportValidity(), setCustomValidity(), validity, willValidate, :invalid, novalidate, pattern, min/max/step, maxlength enforcement)** (missing) — `lib/htmlengine.ad _do_submit ~12640 and element property attach ~9812-9850` — No validation exists. required does not block submission, there is no validity object or checkValidity/reportValidity/setCustomValidity, and maxlength/pattern/min/max/step are not enforced. Many real 
- **[html-forms] Specialized input types (email, number, date, range, color, password, search, url, tel, file) and file upload** (partial) — `lib/htmlengine.ad _emit_input ~5690-5760 and he_dom_is_textfield ~12700` — Only checkbox/radio/submit/button/reset/hidden are special-cased; every other type (including password, email, number, date, range, color, file) falls through to the generic text box. password is not 
> **STALE — ALREADY IMPLEMENTED (verified 2026-07-17, R8 audit).** The block below (all `[ecmascript-*]` entries that follow, through `Object.create / defineProperty / ...`) predates the `lib/web/js/` modular refactor and still cites the retired `lib/jsengine.ad` monolith. Every feature it lists as "missing" is in fact IMPLEMENTED and covered by a passing host gate: for-of (`ND_FOROF`, iteration protocol), optional chaining `?.` / nullish `??` / `??=`, Map/Set (+WeakMap/WeakSet), accessor get/set (object literals, classes, `Object.defineProperty`), generators `function*`/`yield`/`yield*`, class fields + private `#x` + computed/get/set/generator methods, `Object.prototype.hasOwnProperty`/`isPrototypeOf`/`propertyIsEnumerable`, `Array.from`, `Symbol` + well-known symbols, `Object.create`/`defineProperty`/`getPrototypeOf`/`setPrototypeOf`/`getOwnPropertyDescriptor`/`getOwnPropertyNames`. Confirmed by `test_jsengine_es2020_host.sh` + `test_jsengine_generators_host.sh` + direct probes. STILL genuinely missing (next JS round): true arbitrary-precision BigInt (the `n` suffix parses but keeps a float64 Number). (`for await...of` async iteration and the `JSON.stringify` `replacer` argument are now IMPLEMENTED — R10, `test_jsengine_asynciter_host.sh`.) (ES modules `import`/`export`, class private `#fields`/`#methods`, `static{}` initializer blocks, and `Symbol.for`/`Symbol.keyFor` are now IMPLEMENTED — R8 modules gate, R9 `es2022priv` gate.)

- **[ecmascript-language] for-of loops (for (x of iterable))** (missing) — `lib/jsengine.ad parse_for (~line 3295); need ND_FOROF node + iterator-protocol eval near ND_FORIN handler (~line 4915)` — Only for-in (ND_FORIN) exists; grep for TK_OF/ND_FOROF/for_of returns zero matches. for-of over arrays/strings/Maps/Sets/NodeLists is pervasive in hand-written and transpiled code. Spec: invoke Symbol
- **[ecmascript-language] Optional chaining (?.) and nullish coalescing (??)** (missing) — `lib/jsengine.ad lexer (~line 2150, TK_QUESTION only) and parse_call_member (~2571) / parse_assign_impl (~2905)` — TK_QUESTION is used only for the ternary; there is no ?. or ?? / ??= token or parse path. obj?.prop, arr?.[i], fn?.(), and a ?? b are extremely common in modern and minified code; their absence is an 
- **[ecmascript-language] Map / Set / WeakMap / WeakSet** (missing) — `lib/jsengine.ad builtin setup (~line 9384 where g_object/g_array natives are registered)` — No Map/Set constructors or prototypes exist. Frameworks and app code use new Map()/new Set() constantly (dedup, keyed caches, reactivity). Missing constructor throws at runtime and breaks the script.
- **[ecmascript-language] Accessor properties (getters/setters)** (missing) — `lib/jsengine.ad named get/set path (comment at line 665 'jsengine has no accessor properties'); object-literal parse (~2362) and class body parse (~2519)` — No get/set in object literals, no Object.defineProperty accessor descriptors, no get/set class methods. Many libraries and DOM-shim patterns define computed accessors; property reads silently miss the
- **[ecmascript-language] Generators and yield (function*, yield, yield*)** (missing) — `lib/jsengine.ad parse_function/parse_class (~2440, 2519) and evaluator` — No generator parsing or suspension machinery; yield/function* are unrecognized. Transpiled async helpers (regenerator), lazy sequences, and many iterables rely on generators. Syntax error aborts the f
- **[ecmascript-language] Class fields, private (#x), computed/get/set/generator/async methods, static blocks** (partial) — `lib/jsengine.ad parse_class (~line 2519-2560)` — Class body parser only handles plain and static METHOD entries with an identifier name; it explicitly skips keyword names and has no branch for `x = value` fields, `#private`, `[computed]()`, get/set,
- **[ecmascript-language] ES modules (import / export)** (missing) — `lib/jsengine.ad token list (no TK_IMPORT/TK_EXPORT) and parse_statement (~3114)` — No import/export tokens or parsing and no module loader. <script type=module> and any bundle shipped as ESM cannot execute. Many modern sites serve native ESM entry points.
- **[ecmascript-builtins] Map / Set / WeakMap / WeakSet constructors and methods** (missing) — `lib/jsengine.ad NAT_* table (~lines 286-335) + global setup (~9330-9500); no NAT_MAP/NAT_SET collection constants exist (the 'SET_' names are setTimeout/fetch)` — No keyed-collection types at all: new Map()/Set()/WeakMap()/WeakSet() with get/set/has/delete/add/size/clear/forEach and iteration are entirely absent. Map/Set are ubiquitous in modern app code and fr
- **[ecmascript-builtins] for...of statement (iteration protocol)** (missing) — `lib/jsengine.ad parse_for() ~lines 3294-3330 (only handles for-in and classic for)` — parse_for detects 'in' for for-in else pexpect(TK_SEMI); `for (const x of arr)` fails to parse. Spec requires ForOfStatement driving the [Symbol.iterator] protocol. for...of over arrays/strings/Maps i
- **[ecmascript-builtins] Object.prototype.hasOwnProperty / isPrototypeOf / propertyIsEnumerable** (missing) — `lib/jsengine.ad object method dispatch and prototype setup ~9380-9390` — Only the `in` operator exists (OP_IN). obj.hasOwnProperty(k) is called constantly by defensive/library code and for-in filtering; calling it currently fails to resolve a method. Spec: return true iff 
- **[ecmascript-builtins] Array.from** (missing) — `lib/jsengine.ad Array static setup ~line 9394 (only NAT_ARRAY_OF and isArray registered)` — Array.from(iterable/arrayLike[, mapFn]) is absent. Heavily used to materialize NodeLists, Sets, arguments, strings; its absence breaks common DOM and data-processing code.
- **[ecmascript-builtins] Symbol and well-known symbols (Symbol.iterator, Symbol.asyncIterator, etc.)** (missing) — `lib/jsengine.ad global/native setup (no NAT_SYMBOL, no symbol value type)` — No Symbol() constructor, no unique symbol values, no Symbol.iterator. Transpiled ES5 output, iterators, and many libraries reference Symbol; absence breaks feature-detection and custom-iterable code.
- **[ecmascript-builtins] Object.create / defineProperty / defineProperties / getPrototypeOf / setPrototypeOf / getOwnPropertyDescriptor / getOwnPropertyNames / getOwnPropertySymbols / hasOwn / seal / isSealed / preventExtensions / is** (missing) — `lib/jsengine.ad Object setup ~9380-9395 (only assign/keys/values/entries/freeze/fromEntries present)` — Object.create and defineProperty underpin nearly all prototypal inheritance and getter/setter definitions emitted by Babel/TypeScript and used by libraries; their absence breaks class/accessor-heavy b
