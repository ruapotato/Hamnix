# int64 addition overflowing past INT64_MAX (the signed two's-complement case:
# both addends positive, sum negative).
def main(argc: int32, argv: Ptr[uint64]) -> int32:
    a: int64 = 0x7FFFFFFFFFFFF000 + cast[int64](argc)
    b: int64 = 0x2000
    c: int64 = a + b
    return cast[int32](c)
