# Fixtures whose post-script, post-event LIVE DOM was byte-identical to
# `chromium --headless` when the floor was banked. One name per line, no
# extension. A name listed here that stops matching is a REGRESSION and fails
# scripts/test_livedom_functional_host.sh.
#
# BANKED 2026-08-04 on main a5e53862: ZERO of 15. That is the honest measured
# number, not a placeholder — see the "WHAT THE FIRST RUN FOUND" section of the
# harness for the seven distinct causes. Add a name here the moment it goes
# green so the win cannot silently rot.
#
# RAISED 2026-08-04 to THREE, by the D1 fix (created-element attribute
# enumeration unified onto one enumerator, _cre_all_names). Every attribute-
# valued line in all 15 diffs now agrees with chromium; these three fixtures
# diverged on NOTHING ELSE, so they went byte-identical. The other twelve still
# fail on D2-D7 (stale source-anchored text read-back, DocumentFragment
# insertBefore, parentNode after removeChild, checkbox activation behaviour,
# select.selectedIndex, script source leaking in as a text node) — none of which
# is an attribute problem.
#
# RE-MEASURED 2026-08-04 on livedom/land-d1-and-display a80dc449 (tree fp
# 14f71548), clean tree, chromium 147.0.7727.137 — pass=3 fail=12, the SAME
# three names. Both ends of that measurement were produced in one worktree: the
# before run on 0ad6acf1 gave pass=0 fail=15, so 0 -> 3 is D1's alone. The
# el.style.display REVEAL fix landed alongside it moved this lane by ZERO, and
# that is expected, not a disappointment: this harness diffs the LIVE DOM, and
# the reveal bug was never a DOM bug — the DOM already read back "block"
# correctly. It was a RENDER bug in _emit_merged_style(). A live-DOM lane is
# structurally blind to it, so its coverage lives in the render-asserting gate
# scripts/test_hambrowse_style_host.sh (REVEALWORD / STAYHIDDEN) instead. A
# candidate 16th fixture for it was written, measured, and DELETED: it failed on
# D2 plus the style= attribute mirror, both of which fixtures 04/05/06 already
# cover, and it never touched the code under test.
# RAISED 2026-08-04 to SEVEN, by the D2 text half: `el.textContent = x` on a
# SOURCE-parsed element never ran the "replace every child with one Text node"
# algorithm. It stashed the string as an own property — a store only the render
# read-back looks at — so the LIVE child list kept the parse-time Text node and
# every reader that walks the tree (childNodes, firstChild.data, this harness)
# saw the ORIGINAL source text while `.textContent` itself answered the new one.
# Created nodes already ran the real algorithm; unifying both onto it took the
# lane 3 -> 7. Verified as a real guard: all four of the names added below FAIL
# on the pre-fix engine at 7f82b640 (clean-tree run, pass=3 fail=12).
01_create_click
02_innerhtml_subtree
03_delegation
04_settimeout_mutate
05_form_input
11_counter_chain
15_innerhtml_rerender
# RAISED 2026-08-04 to TWELVE, by D2b + D3 + D4 and two neighbours of the same
# shape. Every one of them was ONE piece of DOM state with TWO writers, only
# one of which sat on the path the readers walk — the third time running that
# this lane's answer has had that shape:
#   D2b  classList.add/remove/toggle wrote the reflected className property and
#        NOT the sattr_ content-attribute store, which is the store
#        _el_get_attribute consults FIRST. setAttribute('class') had always
#        written both. So on a source-anchored element getAttribute('class')
#        and el.attributes answered the PARSE-TIME class forever.
#        Fixed in _cls_write_o — one writer, both stores.
#   D3   insertBefore(DocumentFragment, ref) inserted NOTHING. A fragment is
#        deliberately not a created-node-pool member, so _insert_before_v's
#        _find_cre_by_obj() test failed and the whole staged batch was dropped
#        on the floor. appendChild had had the spec's "insert the children,
#        leave the fragment empty" step since it was written; insertBefore
#        never got it.
#   D4   node.parentNode still named the old parent after removeChild(). TWO
#        causes, one per branch: the created-node branch never nulled it, and
#        the source branch nulled it BEFORE the child-list surgery — and that
#        surgery reads parent.children, a LAZY accessor that materialises the
#        subtree from the parse tree and re-writes parentNode on every child,
#        overwriting the null it had just written. NID_REMCHILD carried a
#        second copy of _remove_child_v; there is now one, and it unlinks
#        AFTER the wiring.
#   +    appendChild() of a SOURCE element was a total no-op (only created
#        nodes had a branch), so the remove+re-add every reconciler does left
#        the node dom_detached and dead. That is 08.
#   +    `el.textContent = x` a SECOND time on a source element did nothing to
#        the child list: the first set installs an own data property for the
#        render read-back, and an own data property shadows the prototype
#        accessor in member_set as well as member_get, so later assignments
#        never ran the replace-children algorithm. The D2 tail.
#   +    `el.dataset.foo = 'x'` wrote the dataset object and no attribute at
#        all. That is 13.
# 16/17/18 are the D2b/D3/D4 regression fixtures, each verified RED on the
# pre-fix engine at ea3627ef (clean-tree run, pass=7 fail=8 -> the three new
# names all FAIL) before being banked here.
#
# STILL FAILING and NOT ours: 06 diverges on the style= attribute alone — the
# inline-style store is a third instance of this same shape, but closing it
# also needs CSSOM serialisation to match chromium (declaration order and
# "prop: value;" spacing, plus colour normalisation), which would move
# scripts/test_hambrowse_style_host.sh's banked expectation. 07 and 12 are D7
# (the <script> element's own source leaking in as a sibling Text node), 09/10
# are D5/D6, 14 is the inline onclick= content attribute never running.
08_remove_readd
13_attr_dataset
16_classlist_attr_mirror
17_insertbefore_fragment
18_parentnode_after_remove
# RAISED 2026-08-04 to SIXTEEN, by D7 + D5 + D6. The shape held for a FOURTH
# round: every one was one piece of state with more than one writer, and the
# reader walked the wrong one.
#   D7  (07_fragment_insert, 12_nested_deep_click) — the <script> element's own
#       JS source came back as a sibling TEXT NODE. TWO writers for "where does
#       a raw-text body end": _rawtext_skip (a plain byte scan for the first
#       literal </script — correct, and what the element walks use to skip the
#       body) and _find_close (a MARKUP scan — wrong, and the one that fed
#       tx_ce, which is what every tree reader walks). In _find_close's scan the
#       `<` of `for (i = 0; i < 3; i++)` is not '</', '<!' or '<?', so it was
#       taken for a start tag and the skip-to-'>' swallowed the element's own
#       </script>. No close found => empty content span => "just past the close
#       tag" resolved to the script's content START. Fixed by giving both
#       writers one scanner, _rawtext_close.
#       17 passed all along by ACCIDENT: its script has the same `i < 3` but no
#       '>' before the injected driver's </script>, so the bogus span ran past
#       the leak instead of into it. A green fixture proved nothing here.
#   D5  (09_checkbox_radio) — element.click() ran no activation behaviour. Two
#       IMPLEMENTATIONS of "activate a checkable input": he_dom_set_checked (the
#       real-input path — flips checked, enforces radio exclusivity, fires input
#       + change) and NID_CLICK (the scripted path — dispatched and stopped).
#       Only the one a mouse can reach was complete. Both now call
#       _click_activate_pre / _click_activate_post, which carry the spec's
#       pre-click / canceled / activation-behaviour steps. A checkbox with no
#       value= attribute also answers .value === "on" now (the spec's
#       "default/on" value mode), which is the same store form submission reads.
#   D6  (10_select_change) — select.selectedIndex was a SECOND, INDEPENDENT
#       store: an own data property seeded once from the source scan that no
#       .value write ever touched, plus a THIRD writer in the render read-back.
#       `sel.value = 'c'` moved .value and left .selectedIndex at 0 — the exact
#       split every dependent dropdown does. selectedIndex is now a pure VIEW
#       over .value (accessor on HTMLSelectElement.prototype); the two native
#       writers are deleted. HTMLOptionElement .value / .text did not exist at
#       all, so `opt.value = 'c'` on a created option parked a property no
#       attribute reader could see; they are proper reflections now.
#
# STILL FAILING: 06 (CSSOM serialisation — deliberately deferred, closing it
# would move a green render gate). 14 is closed by D8, below.
07_fragment_insert
09_checkbox_radio
10_select_change
12_nested_deep_click
# RAISED 2026-08-04 to FOURTEEN, by the event-handler content/IDL attribute
# fix. The SIXTH instance of the two-writers shape in this lane: an event
# handler lived in dom_on*_src (parse-time, four types only, readable only by
# _fire_handler) AND as whatever setAttribute() stashed on the JS object — a
# STRING, which is precisely what created-node dispatch reads and precisely
# what it can never call. So `createElement` + `setAttribute('onclick', …)`,
# the only way to give a created node an inline handler, wrote to a store no
# dispatch path could reach. Per spec the content attribute and the IDL
# attribute (el.onclick) are ONE piece of state: setting the content attribute
# compiles it into a function with the element as `this` and installs it as the
# IDL attribute. There is now one store ("@H<name>" own property) and one
# reader (an accessor pair on HTMLElement.prototype, 20 handler names).
# 19 is the wider regression fixture: an onmouseover= on a SOURCE element (no
# non-kind type had ANY attribute-handler route), typeof el.onclick read before
# any dispatch, setAttribute of onmouseover/onkeydown on a created node,
# el.onclick winning over an earlier setAttribute, and String(el.onclick) after
# removeAttribute. BOTH names verified RED on the pre-fix engine at a6ce1da3
# (`git archive` into a scratch tree, clean-tree run): 14 fails on the created
# node's handler never running, 19 fails on FIVE of the six lines above.
14_inline_handler_onclick
19_handler_idl_reflection
# RAISED 2026-08-05 to TWENTY-ONE, by rescuing three regression fixtures off the
# orphaned branch worktree-agent-af65fb2b6bac6d09f and closing the two gaps they
# still measured on main. The SEVENTH and EIGHTH instances of the two-writers
# shape, and the first ones found by a fixture main did not have.
#
# THE FIXTURE-NUMBER COLLISION, recorded because it nearly ate three fixtures.
# That branch was cut before D2b/D3/D4 landed and added its own 16/17/18
# (activation_behaviour / select_selectedness / script_rawtext) while main
# independently landed a DIFFERENT 16/17/18 (classlist_attr_mirror /
# insertbefore_fragment / parentnode_after_remove). Resolving this file by
# taking either side of that conflict deletes three landed fixtures and their
# BASELINE rows while the gate still reports a plausible count — a coverage
# loss wearing a green badge, which `git merge` cannot see because both sides
# are textually valid. The incoming three are renumbered 20/21/22 and this file
# is the UNION: 19 + 3 = 22 fixtures, 18 + 3 = 21 banked names.
#
# 22_script_rawtext PASSED ON UNMODIFIED MAIN — D7 (_rawtext_close) had already
# landed by a different route, so it is pure added coverage and no code moved
# for it. The other two were RED on main (983b1335, clean-tree run, pass=18
# fail=1 -> pass=19 fail=3 with the three added) and each named a real gap:
#
#   20_activation_behaviour — clicking an ALREADY-CHECKED radio fired a change
#       event. _click_activate_post ran the activation behaviour's "fire input,
#       then change" unconditionally, but those are steps that run only when the
#       checkedness actually CHANGED. chromium logs `r2`; we logged `r1 r2`. A
#       change handler is where a page reloads dependent data, so the ordinary
#       re-click-your-current-choice gesture did the expensive thing twice.
#
#   21_select_selectedness — the selection was TWO stores again, and the fix
#       that was supposed to have unified it only went half way. selectedIndex
#       had been made a view, but over `select.value`, which was itself a
#       stamped own data property seeded from the selected option's TEXT; the
#       option elements' own `selected` was never reflected for a source option
#       and never written by anything. So `<option value="c" selected>Cherry`
#       answered s.value === "Cherry" (chromium: "c"), every option read back
#       unselected, and `s.value = 'nope'` stored a string nothing offers
#       instead of deselecting (chromium: value "", selectedIndex -1).
#       The ONE store is now each option's `selected` property, reflected at
#       registration; selectedIndex is a view over it, value is a view over
#       selectedIndex, and the select's shadowing `value` data property is gone
#       from _dom_register_el. The render read-back stops reading select.value
#       (the renderer needs the option's TEXT, .value is its VALUE attribute —
#       different strings for the ordinary <option value="a">Apple</option>,
#       which is exactly why the two were allowed to drift) and reads the
#       derived selectedIndex instead.
#
# The FOUR commits this rescue was briefed to rebase are SUPERSEDED, not
# landed: main had already reached D5/D6/D7 by a different route (3aec4142),
# and their perf commit's premise — select accessors sitting on
# HTMLElement.prototype — was never true of main, which put them on
# HTMLSelectElement.prototype. Only the fixtures and the two gaps above
# survived contact with a measurement.
20_activation_behaviour
21_select_selectedness
22_script_rawtext
